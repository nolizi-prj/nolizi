# Cross-family code review

- **Target:** 2b29a0d~1..2b29a0d
- **Family:** glm
- **Date:** 2026-08-31T21:25:14Z
- **Invoked as:** `recruit -f glm -p ...` by tools/review.sh
- **Context supplied:** 5569 bytes inlined (this reviewer has no tools)

---

# Cross-family code review — commit 2b29a0d ("Part 0 sharpened: pre-launched, only reality binds")

**Scope:** the reproduced diff touches `CHANGELOG.md`, `governance/CHARTER.md`, `governance/DEBT.md`, `tools/gate.sh`. The claimed spec is `governance/CHARTER.md Part 0` (Spec: trailer). I check it against Part 0 **as frozen at the parent commit 2b29a0d~1**, plus correctness and edge cases of the gate change.

## 1. Frozen acceptance tests — not touched

No acceptance-test file appears in the diff; only the four files above. `DEBT.md:153` ("Acceptance tests frozen at approval — the builder may not edit them") is itself left intact. No flag on that ground.

## 2. What is sound

The record is honest about what it does: the provenance note admits the skipped notice (`CHARTER.md:56–57`), the relaxation is explicitly bounded ("the shortcut ends where the `launched` claim begins", `CHARTER.md:53`), the irreversible carve-outs (HUMAN.md; personal data, licensing, credentials, mail) are preserved, the advisory branch prints a warning instead of passing silently, and the COUNT/NEED conditional arithmetic is correct across all four combinations. None of that cures the findings below.

## 3. The diff contradicts the spec it cites — by its own admission

The frozen Part 0 never-suspend list reads, at old `CHARTER.md:40`: "**Part 1**, all of it, including P5's one non-builder review per merge" — under "Never suspended, at any stage." This diff deletes that bullet and enacts the opposite (P5 advisory pre-`launched`, new `CHARTER.md:48–53`; gate passes without review in that regime). A change cannot "implement" a spec by rewriting the clause it fails.

The diff concedes both defects in its own text:
- `CHARTER.md:51` — "This knowingly overrides Part 1's own unamendability": entrenched text, overridden.
- `CHARTER.md:56–57` — "P5 relaxation added hours later. The 7-day notice was not observed; recorded rather than pretended."

The cited justifications are descriptions of power, not grants of it: the 2026-08-29 precedent (`DEBT.md:157`, "the steward's override of its own entrenchment") is a record of a prior override, and "the same sole-author reality Part 7 already records" is a record of reality. Nothing in the reproduced material contains a clause authorizing the steward to dispense with Part 1's entrenchment or the notice requirement. Transparency about an override is a record-keeping virtue; it is not an amendment procedure.

## 4. Enacted scope exceeds announced scope

The commit message, changelog, and DEBT rescoping all announce a **P5-only** relaxation. The charter edit removes **all of Part 1** from the never-suspend list and carries back only four commitments via the irreversibility clause (`CHARTER.md:44–46`: P1, P2, P3, P7). Part 1 is "all permanent commitments, P1–P12" (`DEBT.md:152`). Whatever P4, P6, and P8–P12 are, they lose never-suspend protection here without being named or argued to be reversible — a silent widening beyond the change under review.

## 5. gate.sh — edge cases against the amended Part 0's own clause

The detector is `STAGE="$(head -1 roadmap/STAGE.md 2>/dev/null | grep -oiE 'launched' || true)"` (`gate.sh:61`), `NEED=0` when empty (`gate.sh:62`).

- **Fail-open at the boundary the amendment keeps hard.** The amended Part 0 retains one mandatory rule: "from `launched`, every merge requires the non-builder review as Part 1 writes it" (`CHARTER.md:50`). The gate enforces it only if `roadmap/STAGE.md` exists, is readable, resolves from the invoker's cwd (the path is relative; `2>/dev/null` swallows resolution failures), and line 1 contains the literal substring `launched`. A deleted, corrupted, reformatted, or mis-resolved stage file silently yields `NEED=0` — and the gate then **affirmatively prints** "no review — ADVISORY pre-launched (Part 0); mandatory at launched" (`gate.sh:67`), which can be factually false at a launched product. "No stage file means pre-launched" (`gate.sh:59`) is a fail-open default on the single control the amendment reinstates.
- **Over-match in the other direction.** `grep -oiE 'launched'` is a substring match: a first line reading `pre-launched` — this corpus's own word for *below* launched (CHANGELOG, commit message, Part 0 all use it) — `unlaunched`, or `not launched yet` sets `NEED=1`, demanding review pre-launch and blocking the very unreviewed merge the amended Part 0 grants (`CHARTER.md:48–49`).
- **The hinge is ungoverned.** No clause of the charter designates `roadmap/STAGE.md`, or its first line, as the stage of record; the mapping exists only in a code comment (`gate.sh:58–59`) and the commit message. Nothing obliges anyone to create or flip that file at launch. If the `launched` claim is made anywhere else, the mandatory review never re-arms. The one boundary the relaxation acknowledges rests entirely on a file no governance clause controls.
- **Untested.** The new branch ships with no test, and no acceptance test is cited for Part 0 — every failure mode above is exactly what the tests cannot catch.

## 6. DEBT.md rescoping is incomplete

The never-suspend list still opens with "**Part 1** — all permanent commitments, P1–P12" (`DEBT.md:152`, untouched) while the adjacent rescoped item (`DEBT.md:154–156`) says cross-family review "pre-`launched` it is advisory." P5 sits inside P1–P12, so the same list now contradicts itself, and the commit message's "DEBT's never-suspend list rescoped to match" is not carried through.

## 7. Noted for the record

This commit makes the review now being performed advisory pre-launch. The verdict below is rendered against the spec as frozen, per the terms of this review; an objection is not discarded merely because the objected-to change would downgrade it.

VERDICT: OBJECT — governance/CHARTER.md:40 (frozen at 2b29a0d~1), Part 0 never-suspend clause "**Part 1**, all of it, including P5's one non-builder review per merge," deleted by this diff, which concedes at CHARTER.md:51 it "knowingly overrides Part 1's own unamendability" and at CHARTER.md:56–57 that "The 7-day notice was not observed"; further, tools/gate.sh:61–67 fails open against the amended Part 0's own rule "from `launched`, every merge requires the non-builder review" (CHARTER.md:50), since a missing, unreadable, or non-root-cwd-resolved roadmap/STAGE.md silently yields NEED=0 and prints a false "ADVISORY pre-launched" pass.
