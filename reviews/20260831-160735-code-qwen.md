# Cross-family code review

- **Target:** 2b29a0d~1..2b29a0d
- **Family:** qwen
- **Date:** 2026-08-31T21:07:35Z
- **Invoked as:** `recruit -f qwen -p ...` by tools/review.sh
- **Context supplied:** 5569 bytes inlined (this reviewer has no tools)

---

I reviewed the reproduced diff. It does not touch files that appear to be frozen acceptance tests. The policy change is recorded consistently in `CHANGELOG.md`, `governance/CHARTER.md`, and `governance/DEBT.md`, and `tools/gate.sh` broadly implements the pre-`launched` advisory / post-`launched` mandatory switch.

However, the stage detection in `tools/gate.sh` is too coarse:

- The added line uses a substring match for `launched`.
- That means first lines such as `pre-launched`, `unlaunched`, or `not launched` would still set `NEED=1`.
- Under the added Part 0 language, work below `launched` is supposed to be advisory, so this can make the gate block an unreviewed merge that the charter permits.

A stricter test would be to anchor the stage line to the actual launched marker rather than searching for the substring `launched` anywhere in the first line.

VERDICT: OBJECT — tools/gate.sh added line `STAGE="$(head -1 roadmap/STAGE.md 2>/dev/null | grep -oiE 'launched' || true)"` overmatches “pre-launched”, violating governance/CHARTER.md added line “builder may merge its own unreviewed work while the product's stage is below `launched`”
