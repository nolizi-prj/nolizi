# Cross-family code review

- **Target:** 23bbc64..5154715
- **Family:** kimi
- **Date:** 2026-09-01T19:48:17Z
- **Invoked as:** `recruit -f kimi -p -  (prompt on stdin, 8731 bytes of context)` by tools/review.sh
- **Context supplied:** 8731 bytes inlined (this reviewer has no tools) — git log 2490, git diff 6163

---

## Review of 23bbc64..5154715 (tools/review.sh, tools/test-review.sh)

**What the change does.** For `role=code`, the bundle sent to completion-only families used `git log $TARGET` unconditionally. For a range (`A..B`) that is bounded; for a single ref (`HEAD`) it is the entire history while `git diff HEAD` is only the working tree — the 0086 failure mode (87 % of bytes were log, qwen timed out). The fix branches on `*..*`: ranges keep the full `--format='commit %H%n%an <%ae>%n%n%B'` log (preserving the `Spec:` trailers the reviewer prompt points at), single refs get a one-line `git log -1` base naming. Byte counts are split log/diff in both the run output and the transcript `CONTEXT_NOTE`.

**Correctness checks.**

- The insertion point is after the empty-diff validation (new code begins right after `exit 2` at the former line 227), so `$TARGET` is known to resolve and `$DIFF` is non-empty; `git log -1 "$TARGET"` therefore cannot fail for any commit-ish target, and `|| true` covers the exotic tree-ish case exactly as the old code did.
- Branch names cannot contain `..` (git refname rules), so `*..*` cannot misfire on a single ref. `A...B` also lands in the range branch, but there `git log` and `git diff` are both bounded — same behavior as before, no regression.
- The range heading is byte-identical to the old format (`LOG_HEADING="git log $TARGET"` → `===== git log A..B =====`), so anything parsing that header (including this very review packet's format) is unaffected.
- `BUNDLE_SPLIT` defaults to `""` and is only set in the code branch; the spec-role transcript and run output are untouched via `${BUNDLE_SPLIT:+…}` — verified by inspection.
- Off-by-one between `printf '%s'` (counting) and `printf '%s\n'` (emitting) is cosmetic and correctly labeled as content bytes.

**Tests.** 17a is a genuine guard test: `! grep -q "$FIRST"` and `! grep -q '^commit '` fail against the old build_bundle, while `^+three$` and `$SECOND` pin the diff and base line. 17b pins the range path (`commit $SECOND`, the `Spec: spec/0001` trailer via `%B`, exclusion of `$FIRST`). 17c pins both reporting surfaces. Hex hashes are regex-safe unquoted. Order of 17c between 17a and 17b is odd but harmless.

**What the tests cannot catch, checked by hand:** the removed full history for a working-tree target takes nothing with it — there is no new commit, hence no `Spec:` trailer to lose; the new heading explicitly states the semantics ("the diff is the working tree against this commit"). Reviewing a single *committed* commit via a single ref was already impossible (clean tree → empty diff → exit 2), so no workflow is silently broken.

**Frozen tests:** the diff touches only `tools/review.sh` and `tools/test-review.sh`; nothing under `spec/` is touched, and the commit message documents why test-review.sh is not frozen. No flag.

No failing acceptance test and no spec/charter clause violation found; the claimed 27/27 is consistent with the test logic as written.

VERDICT: APPROVE
