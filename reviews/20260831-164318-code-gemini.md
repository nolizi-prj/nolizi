# Cross-family code review

- **Target:** HEAD
- **Family:** gemini
- **Date:** 2026-08-31T21:43:18Z
- **Invoked as:** `agy --dangerously-skip-permissions -p ...` by tools/review.sh
- **Context supplied:** none — this reviewer runs `git diff` and opens files itself

---

I have started running the test suite to verify the deterministic test suite for `tools/review.sh` and `tools/families.sh`. Waiting for the results.
Waiting for the test suite execution to finish...
I am observing the execution of `tools/families.sh` to verify probe output.
# Cross-Family Code Review: `tools/families.sh` & `tools/review.sh`

**Review Target:** `HEAD`  
**Repository:** `pumasi` (`/home/m/dev/pumasi`)  
**Reviewer Family:** `gemini`  
**Governing Clauses:** [governance/CHARTER.md](file:///home/m/dev/pumasi/governance/CHARTER.md) Part 3 Requirement 3, Part 1 Commitment P5  

---

## 1. Frozen Acceptance Tests & Spec Check
- **Files Modified:** [`tools/families.sh`](file:///home/m/dev/pumasi/tools/families.sh) and [`tools/review.sh`](file:///home/m/dev/pumasi/tools/review.sh).
- **Finding:** No frozen specification or acceptance test files are modified or deleted by this diff.

---

## 2. Correctness & Conformance with Governance

1. **Self-Review Prevention (CHARTER Part 1 P5 & Part 3 Requirement 3):**
   - [`review.sh`](file:///home/m/dev/pumasi/tools/review.sh#L159-L165) explicitly checks whether any explicitly specified reviewer matches `BUILDER_FAMILY` and terminates with exit code 2.
   - When default reviewers are derived ([`review.sh:L153-L157`](file:///home/m/dev/pumasi/tools/review.sh#L153-L157)), `BUILDER_FAMILY` is excluded so that a builder never reviews its own work.
   - If neither a reviewer nor a builder is declared, [`review.sh:L131-L150`](file:///home/m/dev/pumasi/tools/review.sh#L131-L150) refuses to guess and halts with exit code 2.

2. **Single Source of Truth ([lessons/L-007](file:///home/m/dev/pumasi/lessons/L-007-restating-a-rule-forks-it.md)):**
   - [`families.sh`](file:///home/m/dev/pumasi/tools/families.sh#L32-L41) directly queries `review.sh --families` and `review.sh --driver <family>`, eliminating duplicated family lists and preventing rule drift.

3. **Classification of Unreachable vs. Objections:**
   - Both [`review.sh`](file:///home/m/dev/pumasi/tools/review.sh#L316-L322) and [`families.sh`](file:///home/m/dev/pumasi/tools/families.sh#L51-L54) distinguish billing/quota/connectivity failures (such as HTTP 402 or command timeouts) as `UNREACHABLE` rather than misclassifying them as substantive review objections or silent approvals.

4. **Context Bundling for Non-Agentic Families:**
   - Non-agentic completion families (`qwen`, `kimi`, `glm` via `recruit`) receive the context bundle directly inside their prompt on stdin ([`review.sh:L277-L292`](file:///home/m/dev/pumasi/tools/review.sh#L277-L292)), preventing the generation of empty, findings-free transcripts from tool-less completions while respecting Linux `MAX_ARG_STRLEN` limits.

5. **Diff & Target Validation:**
   - [`review.sh:L217-L227`](file:///home/m/dev/pumasi/tools/review.sh#L217-L227) verifies `TARGET` and halts with exit code 2 if `git diff` produces an empty output or if the range is invalid, preventing spurious approvals over empty diffs.

---

## 3. Test Verification & Edge Cases
- All 23 deterministic test cases in [`tools/test-review.sh`](file:///home/m/dev/pumasi/tools/test-review.sh) execute and pass (`23 passed, 0 failed`).
- Running [`tools/families.sh`](file:///home/m/dev/pumasi/tools/families.sh) against live CLIs confirms active reachability across 5 of 6 configured families (`claude`, `gemini`, `qwen`, `kimi`, `glm`), reporting `grok` as `UNREACHABLE` without crashing.

---

VERDICT: APPROVE
