# Cross-family code review

- **Target:** HEAD
- **Family:** qwen
- **Date:** 2026-09-01T23:31:55Z
- **Invoked as:** `recruit -f qwen -p -  (prompt on stdin, 18142 bytes of context)` by tools/review.sh
- **Context supplied:** 18142 bytes inlined (this reviewer has no tools) — git log 93, git diff 17905

---

I reviewed the reproduced diff only, as instructed. The substantive policy change is internally coherent: the charter/README text moves from “one approving cross-family review” to “ask two non-builder families, require at least one real approval,” and `tools/review.sh` / `tools/gate.sh` are largely adjusted to count real verdicts, skip unreachable/no-verdict attempts, and require one approval.

However, the diff edits what appears to be the frozen acceptance-test table for the review tooling: `tools/test-review.sh`. That file contains the numbered acceptance checks and the comment “the table, and the single source of truth.” The diff changes the expected outcomes of the acceptance tests rather than implementing against them, including:

- `tools/test-review.sh` hunk `@@ -73,8 +80,8 @@`: test 1’s expected family list is changed.
- `tools/test-review.sh` hunk `@@ -113,7 +120,7 @@`: test 6’s expected pass string is changed from `ALL FAMILIES APPROVE` to `REVIEW PASSED`.
- `tools/test-review.sh` hunk `@@ -121,24 +128,24 @@`: test 7 is changed so that a cited objection no longer fails the run; tests 8/9 are changed so an unreachable reviewer plus fallback becomes passing.

The freeze rule in `governance/CHARTER.md` Part 3 requirement 2, shown in the diff at hunk `@@ -243,10 +243,14 @@` (“The freeze is the control: the standard is fixed before anyone knows whether the code will meet it...”), prohibits the builder from editing frozen acceptance tests. Because the diff itself edits the acceptance-test table, this is a blocking governance violation.

Non-blocking observations, not independently cited as the final objection:

- `tools/gate.sh` now matches approval by exact string `"VERDICT: APPROVE"`; trailing whitespace or CR could make a valid approval become “no real verdict.”
- `tools/gate.sh` counts any `VERDICT:*OBJECT*` as a real verdict even if uncited, while the charter/review instructions say uncited objections are discarded.
- `tools/gate.sh`’s builder-family check uses only the first lowercased word of the `Model` trailer; unusual model names could fail to map to the correct family.
- Pre-launched, `tools/gate.sh` can pass even when a recorded objection exists, because objections no longer set `FAIL`.

The blocking issue is the edited frozen acceptance-test suite.

VERDICT: OBJECT — governance/CHARTER.md Part 3 requirement 2 (freeze, @@ -243,10 +243,14 @@): frozen acceptance tests edited in tools/test-review.sh (@@ -113,7 +120,7 @@, @@ -121,24 +128,24 @@)
