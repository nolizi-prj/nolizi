# Cross-family code review

- **Target:** HEAD
- **Family:** kimi
- **Date:** 2026-09-01T23:41:13Z
- **Invoked as:** `recruit -f kimi -p -  (prompt on stdin, 18142 bytes of context)` by tools/review.sh
- **Context supplied:** 18142 bytes inlined (this reviewer has no tools) — git log 93, git diff 17905

---

# Cross-family code review

**Scope.** The diff amends the governance review rule (from "≥1 cross-family approval" to "2 real non-builder verdicts, ≥1 approval") across `README.md`, `governance/CHARTER.md`, `tools/gate.sh`, `tools/review.sh`, and — critically — `tools/test-review.sh`.

## Correctness of the implementation

The `tools/review.sh` changes are largely coherent with the new rule:

- Fallback candidates are appended after explicit reviewers, and the loop breaks at `ANSWERED >= WIDTH` — this implements "replace an empty/unavailable/timed-out family until two real verdicts are obtained." UNREACHABLE and NO-VERDICT no longer fail the run outright, which is correct per the new policy.
- The final pass/fail is recomputed from `ANSWERED == WIDTH && APPROVED >= 1`, so the removal of `FAIL=1` side-effects inside the loop is safe.
- `REVIEWED_FAMILIES` is populated but never read (dead variable; cosmetic only).
- `tools/gate.sh` correctly requires `ANSWERED >= 2 && APPROVALS >= 1` post-launch, skips the builder family, de-duplicates families, and takes the last `VERDICT:` line.

A real weakness in `gate.sh`: builder identification is `trailer Model | head -1 | ... | awk '{print tolower($1)}'` — it only matches a family like `claude` if the `Model` trailer's first word literally is the family name. A `Model: claude-opus-4-5` trailer would let a same-family review count. That is a spec-relevant hole (CHARTER req. 3: "Same-family review does not count"), but it is a heuristic weakness rather than a definite failure, so I note it without grounding the verdict on it.

## The blocking finding: frozen acceptance tests were edited

`governance/CHARTER.md` Part 3, requirement 2: acceptance tests "are frozen when spec review completes, *before* implementation. **The builder may not edit them.** If a test is wrong, amend the spec in the open and take a fresh review" (also restated in the current README point 2).

This diff edits `tools/test-review.sh` — the acceptance suite for the only executable enforcement of the review rule (`review.sh`) — *in the same working-tree change as the implementation*, and inverts expectations to match the new code:

- Test 7: flipped from "a cited objection **fails** the run" (`RC -ne 0`) to "one approval plus one cited objection **passes**" (`RC -eq 0`) (test-review.sh hunk, test 7).
- Test 8: flipped from `RC -ne 0` on a 402 to `RC -eq 0` ("a fallback supplies the second verdict") (test 8).
- Tests 1, 6, 9, 11a, 11b rewritten; `reset()` now pre-stubs every provider (`for cli in claude agy codex-p grok recruit; do stub ... "test provider unavailable"`).

The charter's freeze discipline requires the tests to be frozen before the builder implements against them, and any needed change must go through an openly amended spec with a *fresh* spec review — not be amended together with the code in the builder's commit, with pass/fail polarity reversed to fit the implementation. My review instructions are explicit that a diff touching frozen acceptance tests must be flagged; this diff does.

VERDICT: OBJECT — governance/CHARTER.md Part 3 requirement 2 (acceptance tests frozen before implementation; "The builder may not edit them"), violated by the diff's modification of `tools/test-review.sh` (tests 7 and 8's expected exit codes inverted; tests 1, 6, 9, 11a–b and `reset()` rewritten) in the same change as the `tools/review.sh`/`tools/gate.sh` implementation.
