# Cross-family code review

- **Target:** 2b29a0d~1..2b29a0d
- **Family:** kimi
- **Date:** 2026-08-31T21:21:45Z
- **Invoked as:** `recruit -f kimi -p ...` by tools/review.sh
- **Context supplied:** 5569 bytes inlined (this reviewer has no tools)

---

Scope of the change: CHANGELOG.md, governance/CHARTER.md, governance/DEBT.md, and tools/gate.sh. The spec trailer points to CHARTER Part 0, and the three doc edits consistently record the same relaxation (P5 per-merge review advisory below `launched`, mandatory from `launched`; the never-suspended list rescoped to `HUMAN.md` + irreversible acts, with Part 1's substance claimed to survive only "wherever it is irreversible by nature"). The diff touches no acceptance-test files, so the frozen-test injunction is not violated on its face; gate.sh is the enforcement tool, not a frozen acceptance test, though it is the §3 review gate.

The implementing logic in tools/gate.sh is:

```
NEED=1
STAGE="$(head -1 roadmap/STAGE.md 2>/dev/null | grep -oiE 'launched' || true)"
if [ -z "$STAGE" ]; then NEED=0; fi
```

Findings:

1. **Unanchored substring match cannot implement the spec's distinction.** The new comment in gate.sh states the trigger precisely ("It becomes mandatory when roadmap/STAGE.md opens with `# STAGE — launched`"), but the code greps the first line for the bare substring `launched`. The antonym the commit itself coins — "pre-launched" (commit title; CHANGELOG: "pre-`launched`") — contains that substring, as would `# STAGE — pre-launched`, `# STAGE — not-yet-launched`, or any first line that merely mentions "launched". For such a stage the gate sets NEED=1 and demands the review, i.e. the relaxation never takes effect. That contradicts the amended CHARTER Part 0, which relaxes P5 "while the product's stage is below `launched`". The match needs anchoring to the documented form (e.g. `grep -iE '^# STAGE — launched\b'` or an equality test on the stage name). The failure is fail-closed (spurious mandatory review), but it is still a mismatch between the stated trigger and the behavior, and it defeats the very stage name the commit's vocabulary makes most likely.

2. Missing stage file downgrades to advisory ("No stage file means pre-launched"). Given the charter's framing that the `launched` claim is what binds, that default is self-consistent; not an objection.

3. The `elif [ "$NEED" -eq 0 ] && [ "$COUNT" -eq 0 ]` advisory message and the FAIL logic are otherwise coherent: NEED=0/COUNT=0 warns and passes; NEED=1/COUNT≥1 passes; NEED=1/COUNT=0 fails. No issues there.

4. DEBT.md's rescoping and CHANGELOG.md's rewording match the amended Part 0 text; the sole self-merge loop (builder merging the relaxation that permits self-merge) is explicitly recorded in the charter text as a knowing override, so it is at least documented rather than hidden.

The objection stands on finding 1: as written, the gate does not implement the below-`launched`/`launched` distinction specified in CHARTER Part 0 and restated in gate.sh's own comment.

VERDICT: OBJECT — tools/gate.sh (`STAGE="$(head -1 roadmap/STAGE.md 2>/dev/null | grep -oiE 'launched' || true)"`): the unanchored substring match also matches `pre-launched`, so the relaxation required by governance/CHARTER.md Part 0 ("relaxed ... while the product's stage is below `launched`") never engages for that stage name, contradicting the spec and the gate's own stated trigger `# STAGE — launched`.
