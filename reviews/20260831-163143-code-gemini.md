# Cross-family code review

- **Target:** HEAD
- **Family:** gemini
- **Date:** 2026-08-31T21:31:43Z
- **Invoked as:** `agy --dangerously-skip-permissions -p ...` by tools/review.sh
- **Context supplied:** none — this reviewer runs `git diff` and opens files itself

---

# Cross-Family Code Review: `tools/families.sh` & `tools/review.sh`

**Review Target:** `HEAD` (uncommitted changes on `main`)  
**Repository:** `pumasi` (`/home/m/dev/pumasi`)  
**Reviewer Family:** `gemini`  
**Governing Clauses:** [governance/CHARTER.md](file:///home/m/dev/pumasi/governance/CHARTER.md) Part 3 Requirement 3, Part 1 Commitment P5  

---

## 1. Frozen Acceptance Tests Check
- **Files touched:** [`tools/families.sh`](file:///home/m/dev/pumasi/tools/families.sh) and [`tools/review.sh`](file:///home/m/dev/pumasi/tools/review.sh).
- **Finding:** No frozen specification or acceptance test files were modified in this diff.

---

## 2. Correctness & Conformance with Charter

1. **Self-Review Prevention (CHARTER Part 1 P5 & Part 3 Requirement 3):**
   - [`review.sh`](file:///home/m/dev/pumasi/tools/review.sh#L158-L165) explicitly rejects any explicit review request where `FAMILY == BUILDER_FAMILY` with exit code 2.
   - When deriving default reviewers ([`review.sh:L153-L157`](file:///home/m/dev/pumasi/tools/review.sh#L153-L157)), the builder's family is omitted so that a builder cannot review its own work.

2. **Single Source of Truth ([lessons/L-007](file:///home/m/dev/pumasi/lessons/L-007-restating-a-rule-forks-it.md)):**
   - [`families.sh`](file:///home/m/dev/pumasi/tools/families.sh#L32-L41) queries `review.sh --families` and `review.sh --driver <family>` directly rather than maintaining a duplicate family list.

3. **Distinguishing Unreachable vs. Objections:**
   - Both [`review.sh`](file:///home/m/dev/pumasi/tools/review.sh#L285-L291) and [`families.sh`](file:///home/m/dev/pumasi/tools/families.sh#L56-L59) accurately capture CLI / billing failures (e.g. HTTP 402, command not found, timeout) as `UNREACHABLE` rather than misclassifying them as review objections or silently ignoring them.

4. **Completion-Only Context Bundling:**
   - Non-agentic families driven via `recruit` (`qwen`, `kimi`, `glm`) receive inlined context bundles ([`review.sh:L238-L265`](file:///home/m/dev/pumasi/tools/review.sh#L238-L265)) containing the git diff and log, preventing empty transcripts from non-tool-calling models.

5. **Empty Diff & Invalid Range Validation:**
   - [`review.sh:L217-L227`](file:///home/m/dev/pumasi/tools/review.sh#L217-L227) halts immediately with exit 2 if `git diff` produces an empty output or if the range is invalid, preventing findings-free pass transcripts on empty ranges.

---

## 3. Edge Cases & Observations

- **Hermetic Test Environment:**
  - In [`tools/test-review.sh`](file:///home/m/dev/pumasi/tools/test-review.sh#L66-L68), `run()` inherits the ambient `BUILDER_FAMILY` environment variable if set. While `test-review.sh` passes completely in a clean environment (`21 passed, 0 failed`), resetting `BUILDER_FAMILY` in the test runner function (`run()`) ensures full hermeticity when run from inside an agent runner shell.
- **`families.sh` Notice Text:**
  - The warning notice in [`tools/families.sh:L78`](file:///home/m/dev/pumasi/tools/families.sh#L78) references "two families" for Part 4's can-hurt gate, which is historical prose from prior revisions (Part 4 was amended to 1 family on 2026-08-29); this does not affect the script's logic or exit code.

---

VERDICT: APPROVE
