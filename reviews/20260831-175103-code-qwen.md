# Cross-family code review

- **Target:** HEAD
- **Family:** qwen
- **Date:** 2026-08-31T22:52:18Z
- **Invoked as:** `recruit -f qwen -p -  (prompt on stdin, 120955 bytes of context)` by tools/review.sh
- **Context supplied:** 120955 bytes inlined (this reviewer has no tools)

---

Review of the reproduced `git diff HEAD` changes only.

## 1. Frozen acceptance tests

The diff does touch a test file: `tools/test-review.sh`, hunk `@@ -187,12 +187,36 @@`, including the rewritten case `12d` and the new case `12e`.

I am not flagging this as an edit to frozen acceptance tests. The immediately prior commit `133d337` says:

> `Spec: none -- governance tooling, no spec cycle in this packet`

and the added note in `tools/test-review.sh` explicitly addresses Charter Part 3 req 2, stating that no spec review froze these tests because there is no `spec/` tree and no spec cycle for this packet. On the reproduced material, that predicate is not met, so the tests touched here are not shown to be frozen acceptance tests under Part 3 req 2.

## 2. Correctness of the change

The diff removes the pre-call warning block in `tools/review.sh`, hunk `@@ -241,22 +241,6 @@`, including the former condition:

```sh
if [ "$BUNDLE_BYTES" -gt 131072 ]; then
```

That warning was advisory only; it did not itself set `FAIL`. Removing it does not by itself soften the gate. The surrounding retained code still computes `BUNDLE_BYTES`, and the new test `12e` pins that the transcript still records the byte accounting:

```sh
grep -qE 'Context supplied:.*[0-9]{6,} bytes inlined' "$TR"
```

The replacement test `12d` now asserts that the old ceiling warning does not appear:

```sh
if printf '%s' "$OUT" | grep -qiE 'recruit\.sh:86|narrower range|past the ~?131072-byte ceiling'
```

That regex covers the old warning’s distinctive strings: `recruit.sh:86`, `narrower range`, and the `131072-byte ceiling` wording. The existing case `12c` still asserts that a bundle past 128 KiB reaches the reviewer stub.

## 3. Edge cases and things the tests cannot catch

- The tests use stub reviewer CLIs. They cannot prove that the real `recruit` / `openrouter` path on the review host no longer has the argv ceiling. The diff’s justification relies on external `pumasi-ops` state cited as `2208b5e`, which is not visible in this repository. If that external fix is absent on the machine running reviews, large bundles may now fail as `UNREACHABLE` without the prior pre-call warning. That is an operational risk, but on the reproduced material it is not a violation of a frozen spec or acceptance test.

- If the real transcript line later uses commas, different capitalization, or a different phrase than `Context supplied: ... bytes inlined`, case `12e` may fail even though accounting is present. That is a test brittleness issue, not a charter violation.

- Case `12d` could false-fail if legitimate non-warning output ever contains the phrase `narrower range`. In the deterministic stubbed test environment shown here, that is not a problem.

- The removed warning used to warn before calls were spent. If the external ceiling ever regresses, the script will now discover that only by the reviewer failing or returning no verdict. The commit history’s `UNREACHABLE IS NOT AN OBJECTION` behavior still distinguishes unreachable from verdicts, so this does not silently count an unreachable family as breadth.

## 4. Conclusion

No frozen acceptance test is shown to have been edited. The code change is internally consistent with the test changes: the warning is removed, the absence of the warning is pinned, and the byte accounting is still asserted. The main unverifiable point is the external `pumasi-ops` fix, which the stubbed tests cannot prove, but that is not a cited failure of a frozen acceptance test or a specific charter/spec clause in the material provided.

VERDICT: APPROVE
