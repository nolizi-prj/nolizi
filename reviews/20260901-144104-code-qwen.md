# Cross-family code review

- **Target:** 23bbc64..5154715
- **Family:** qwen
- **Date:** 2026-09-01T19:42:50Z
- **Invoked as:** `recruit -f qwen -p -  (prompt on stdin, 8731 bytes of context)` by tools/review.sh
- **Context supplied:** 8731 bytes inlined (this reviewer has no tools) — git log 2490, git diff 6163

---

I reviewed only the reproduced diff.

**Frozen spec / acceptance-test status**
- The commit trailer is `Spec: none - governance tooling, no spec cycle in this packet`, so it does not claim to implement a frozen spec cycle.
- The diff touches only `tools/review.sh` and `tools/test-review.sh`. No `spec/` acceptance-test path appears in the diff.
- The commit message explicitly says `tools/test-review.sh` is not a frozen acceptance test. From the reproduced diff, there is no evidence that a frozen acceptance test was edited.

**Correctness against the stated change**
- The range path is preserved: in `tools/review.sh`, hunk `@@ -225,9 +227,30 @@`, the `*..*)` arm still runs  
  `git --no-pager log --format='commit %H%n%an <%ae>%n%n%B' "$TARGET"`.  
  This matches the claim that a range still carries its own commits and messages.
- The single-ref path is bounded: in the same hunk, the `*)` arm replaces full-history `git log "$TARGET"` with  
  `git --no-pager log -1 --date=short --format='%H %s (%an, %ad)' "$TARGET"`, which supplies one base line rather than the entire history.
- The byte split is computed and reported:
  - `BUNDLE_SPLIT="git log $LOG_BYTES, git diff $DIFF_BYTES"` in `tools/review.sh`, hunk `@@ -225,9 +227,30 @@`.
  - Run output reports it in `tools/review.sh`, hunk `@@ -239,7 +262,10 @@`:  
    `echo "── context bundle for the completion-only families: $BUNDLE_BYTES bytes${BUNDLE_SPLIT:+ ($BUNDLE_SPLIT)}"`.
  - The transcript note reports it in `tools/review.sh`, hunk `@@ -264,7 +290,7 @@`:  
    `CONTEXT_NOTE="$BUNDLE_BYTES bytes inlined (this reviewer has no tools)${BUNDLE_SPLIT:+ — $BUNDLE_SPLIT}"`.

**Test coverage in the added non-frozen tests**
- `tools/test-review.sh`, hunk `@@ -246,6 +246,38 @@`, adds:
  - `17a`: verifies a working-tree target includes `+three`, includes the HEAD/base hash, excludes the previous commit hash, and excludes full `^commit ` log blocks.
  - `17c`: verifies both the transcript header and run output contain the `git log N, git diff M` split.
  - `17b`: verifies a range target includes the range commit, includes the `Spec: spec/0001` trailer, and excludes the parent commit.
- These tests correspond to the behavior implemented in `tools/review.sh`.

**Non-blocking observations**
- For a non-`HEAD` single ref, the diff may include committed changes since that ref, while the new log section only names the base commit. That is consistent with the commit’s “single ref gets one line naming the base” design, but it is a context reduction relative to a ranged review.
- The `*..*)` pattern would also match three-dot syntax; `git log A...B` and `git diff A...B` are not perfectly analogous. The tests and commit message discuss two-dot ranges, and no frozen spec requirement is cited that covers three-dot targets.
- `LOG_BYTES` and `DIFF_BYTES` are computed from command-substituted variables, which strip trailing newlines, so the split counts may differ by newline bytes from the actual bundle byte total. This does not appear to violate any stated requirement and does not affect the added tests’ regexes.

VERDICT: APPROVE
