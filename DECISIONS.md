# Decision queue

Every open veto window and every queued human-only action, in one place, with
its deadline and its default. **Agents append new entries and mark expired
windows closed; only the steward moves a deadline, changes a default, or
vetoes** (CHARTER §2, §7). A veto is an entry edit by the steward, in the open,
with reasoning.

An entry without a stated default is a defect — silence must always select a
named outcome (CHARTER §2.1). **While a product is pre-`launched` (CHARTER
Part 0), an open window does not hold reversible work: agents proceed on the
entry's default at once, and a veto reverts. Irreversible acts still wait.**

## Open

### Q-001 · Calendar integration intent statement — veto window
| | |
|---|---|
| **What** | `spec/0003-calendar-sync/INTENT.md`, published 2026-08-03. Window restarted at v0.4 adoption because the old model's signature boxes were never signed. |
| **Window closes** | **2026-08-29** (48h from 2026-08-27) |
| **Default on silence** | Proceed with the intent statement's own recommendations: Google first, then Microsoft; busy-time reading first, write-back as a separate optional grant; Google and Microsoft named as subprocessors before any token is held; the connection token treated as the most protected datum in the system. |
| **Status** | **CLOSED 2026-08-29** — the window ran to its deadline with no veto, so the stated default took effect. Marked closed by an agent, which CHARTER §2 permits for an expired window; the outcome is the one the entry already named, not a new one. |

### Q-002 · D-105 — lawful basis for bookers' personal data
| | |
|---|---|
| **What** | Human-only (`HUMAN.md`): decide the controller's lawful basis, approve the privacy notice. No default can proceed on silence — this is the one queue entry with no route around a public launch. |
| **Deadline** | none set. **The blocking clause this row used to carry is no longer true and is corrected here rather than left standing:** it said this entry blocks public signup and freezes the ceilings permanently until answered. The steward removed that red line from `MANDATE.md` on 2026-08-29, the ceilings are raisable, and public signup is a deployment decision. Correcting a statement of fact is not moving a deadline or softening a default; there is no deadline here to move, and the entry is not thereby resolved. |
| **Agent next step** | Draft the privacy notice, basis analysis, and deletion-reach statement, and attach them here for one-step approval. |
| **Status** | open — but **substantively superseded**, and it should probably be closed by the steward rather than by an agent. What it asked for exists: the basis is written and in force (`service/src/legal.ts` v1.0), the privacy notice is published and live, and deletion reach is stated. What is genuinely left has moved to D-105's own row — the transfer mechanism and a review by counsel. Left open rather than closed because closing it is a decision about whether the answer is *good enough*, and that is the steward's, not an agent's. |

### Q-004 · Steward edits that the privacy-posture revision requires
| | |
|---|---|
| **What** | `CHARTER.md` Parts 4, 5.1 and 5.2, `DEBT.md` D-105/D-107, `REPORTING.md`, `SPEC-0002` and its service code were revised on 2026-08-29 so that strictness follows harm rather than discomfort (branch `privacy-posture`). Three files that agents may never edit (CHARTER §2, §7) still name the old posture and now contradict the rest. **Agents may not fix them. This entry is the proposal; the edit is the steward's.** |
| **Deadline** | *(steward to set)* |
| **Default on silence** | *(steward to set — agents may not. Until it is set, the contradiction stands and `MANDATE.md` governs, which means the ceilings remain frozen in practice however the spec now reads.)* |
| **Status** | **item 1 DONE 2026-08-29** — the steward removed the D-105 red line from `MANDATE.md`. Items 2 and 3 remain open. |

**1 · `MANDATE.md` → Red lines. — DONE 2026-08-29 by the steward.** The red line
below is deleted; public signup and the ceilings are no longer forbidden by the
mandate. The steward made the edit in their local working copy; an agent
transported the deletion verbatim into this repository and authored none of it.
Two editor artifacts in that copy were deliberately **not** carried over: a
whole-file reflow, and the `L-003` link rewritten to a `file:///home/m/dev/...`
path that would be broken for every reader but its author.

The deleted text was:

> - **D-105 binds absolutely**: no public signup, and the 5-owner / 200-booking
>   ceilings cannot be raised, while that debt entry is open.

with:

> - **D-105 is DEGRADING, not blocking.** The lawful basis is stated and in force
>   (`service/src/legal.ts` v1.0). The 5-owner / 200-booking ceilings are
>   defaults an operator may raise, and public signup is a deployment decision.
>   What remains human-only is the entity name, governing law, transfer
>   mechanism, and a review by counsel.

*Why this one matters most:* `MANDATE.md` is a steward instrument and outranks the
spec. While that line stands, the ceilings are still frozen no matter what
`SPEC-0002` D1 now says — so this edit is the one that actually lifts the block,
and everything else committed on 2026-08-29 is inert without it.

**2 · `DECISIONS.md` Q-002** — set a deadline and a default. Q-002 currently has
neither, which §2.1 calls a defect: *"An entry without a stated default is a
defect — silence must always select a named outcome."* It has been open since
2026-08-02 on that footing. Proposed default on silence: *proceed on the basis
already stated in `legal.ts`, with the three blanks tracked in D-105 until
counsel fills them.*

**3 · `HUMAN.md` → Legal accountability.** The D-105 entry describes agents
drafting a basis that has since been drafted and put in force. Proposed
replacement scope: *filling the legal entity name and registered address, the
governing law and supervisory authority, and the international transfer
mechanism; and commissioning the review by counsel.* The drafting is done.

**Also carried forward, unrelated to privacy:** Q-003's still-unactioned proposal
to strike "OAuth consent screen / test accounts" from `HUMAN.md`.

### Q-005 · Pumasi Booking public sign-up — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-29-pumasi-booking-public-signup.md`](releases/2026-08-29-pumasi-booking-public-signup.md), published 2026-08-29. Opening booking.pumasi.ai to public sign-up. Classed **can-hurt** (CHARTER §4): the people exposed are bookers, who have no account and never chose this project. |
| **Window closes** | **2026-09-05** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline.* |
| **Default on silence** | *(steward to set.)* Proposed: the release proceeds as written in the note. |
| **Still outstanding at publication** | Part 4 requires **two approving reviews from two model families other than the builder's** for a can-hurt release. Neither has been obtained. The window may run concurrently; the release may not proceed without them. |
| **Named non-compliance — now a recorded decision** | CHARTER §5.1 requires a working reporting path and opt-out before an in-scope item releases. `PUMASI_REPORTING` is read by nothing, so this release does not meet §5.1. **The steward decided on 2026-08-29 to release anyway**, with building it first offered and declined; that decision, its reasoning, what it costs, and the trigger that ends it are recorded as **`DEBT.md` D-108**. It is an exception for this release, not a standing permission — any release after this one clears it or reopens the question. |
| **Status** | **CLOSED 2026-08-29 — approved by the steward before the window's end.** The reviews stand as recorded in `DEBT.md` D-110: Gemini approved the final range; Grok's two earlier objections were both fixed and test-covered; the second non-builder approval is waived by the steward while Grok's balance is exhausted. |

### Q-006 · `MANDATE.md` deleted — **CLOSED 2026-08-29, decided**
The steward deleted `MANDATE.md`. Its **sequencing** job was already gone: work
comes from the product's own roadmap. Its **authorising** job is not replaced —
the steward decided that can-hurt surfaces need no human authorisation step,
*"if there is genuinely big risks, agents will report them."* Per-change steward
sign-off was offered as the alternative and declined.

`charter.yaml` was brought back into line with the prose it had drifted from —
Part 4 already read *"No human sign-off at any point: the classification is a
boolean per path in `RISK_ZONES.yaml`"* after `0ccfc39`, while the config still
named the mandate. **That file was then deleted the same day**: nothing read it,
and it had drifted twice in one day. Part 4's prose is now the only copy.

The residual is recorded as [`DEBT.md` **D-109**](governance/DEBT.md), including
what it does not fix and the bet being made, so that if it fails it fails on the
record. The can-hurt **gate** itself is untouched: two extra families, a
published release note, the 7-day veto window, ceilings first.

### Q-007 · Video conferencing integration — roadmap scope addition
| | |
|---|---|
| **What** | [`pumasi-booking#4`](https://github.com/pumasi-ai/pumasi-booking/issues/4) asks for video chat integration — "at least Zoom, Google Chat, Teams." Escalated from feedback intake rather than accepted: conferencing is **not on the GAP-0004 §3 sequence**, that sequence records steward-decided scope (including explicit exclusions), and two of the three named providers require **new third-party developer accounts** (Zoom; Microsoft/Entra app registration for Teams) — an account is escalation ground in the product-manager brief. The parity intent itself covers the *capability*: Cal.com and Calendly both attach a conferencing link to a booking as part of the product. |
| **Evidence added 2026-08-30** | [`pumasi-booking#30`](https://github.com/pumasi-ai/pumasi-booking/issues/30): a user on `/app/integrations?zoom_needed=1` expected a Zoom login screen — *"i expected to see a login screen of Zoom. is it impossible to use that UX?"* A second, independent request naming Zoom specifically, from the live integrations page rather than the tracker. Appended here by feedback intake instead of opened as a duplicate question; it does not change the default below, but it is the first evidence that users reach the integrations surface expecting Zoom before Google Meet. |
| **Window closes** | **2026-09-01** (72h from 2026-08-29) |
| **Default on silence** | Accept in principle under GAP-0004's parity intent, as a new sequence item **after 2b (calendar providers)**, adjacent to item 8 (workflows) since both are outbound-integration surface. **Google Meet first**: it can use the existing `pumasi-commons` Google Cloud project and `admin@pumasi.ai` (Q-003) with an added Calendar scope, so no new account is needed. **Zoom and Microsoft Teams follow only when their provider accounts/app registrations exist** — created by the steward or explicitly delegated, as Q-003 was. No provider credential is held before the subprocessor list names the provider, matching the Q-001 default's convention. |
| **Status** | open |

### Q-008 · Reporting path and opt-out — intent statement, veto window
| | |
|---|---|
| **What** | [`pumasi-booking` `service/spec/0004/INTENT.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/service/spec/0004/INTENT.md), published 2026-08-30. BACKLOG item 1: the CHARTER §5.1 reporting path and one-step opt-out, with the D-107 retention schedule published alongside. |
| **Window closes** | **2026-08-31** (24h from 2026-08-30) |
| **Default on silence** | Proceed with the intent statement's own assumptions: intake address defaults to `https://report.pumasi.ai/v1/reports`, documented as not yet live; held-tier (operating) reports retained twelve months, deletable earlier on request; the Cloudflare deployment sends nothing yet, revisited no later than the `launched` promotion; no report ever carries owner or booker data, or counts derived from them. |
| **Status** | open — pre-`launched` (CHARTER Part 0), so work proceeds on this default at once and a veto reverts. |

### Q-009 · Pumasi Booking reporting path — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-30-pumasi-booking-reporting-path.md`](releases/2026-08-30-pumasi-booking-reporting-path.md), published 2026-08-30. The CHARTER §5.1 reporting mechanism and opt-out (BACKLOG item 1, `spec/0004`), with the D-107 retention schedule published. Classed can-hurt by `RISK_ZONES.yaml` (service paths). |
| **Window closes** | **2026-09-06** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline; this is the charter's own duration applied, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. Nothing egresses in practice today: the live Cloudflare deployment is not wired and the documented intake is not live. A veto reverts the merge or sets `PUMASI_REPORTING=false` guidance, whichever the veto states. |
| **Reviews** | Gemini approved spec and code (transcripts in `pumasi-booking/reviews/`). Grok unreachable — D-104 condition live; pre-`launched`, review is advisory (Part 0) and the single non-builder bar (P5) is met. |
| **Status** | open — pre-`launched` (Part 0): the window does not hold the (reversible) release; a veto reverts. |

### Q-010 · Zoom connect stores the connection, and the PMI stops leaking — intent statement, veto window
| | |
|---|---|
| **What** | [`pumasi-booking` `service/spec/0005/INTENT.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/service/spec/0005/INTENT.md), published 2026-08-31. BACKLOG item 1, parts (b) and (c): the OAuth connect flow stamps the owner's *personal meeting URL* onto every Zoom event type and the public booking page prints it to anyone before they book; per-booking meeting creation is suppressed by that same stamp, so the card's "unique room for every booked session" is false for exactly the people who pressed the button. |
| **Window closes** | **2026-09-01** (24h from 2026-08-31) |
| **Default on silence** | Proceed with the intent statement's own assumptions: no joinable link on the public pre-booking page for **any** conferencing event type (Zoom, Meet, Teams, Google Chat), not Zoom alone; the personal meeting room kept as a **last-resort** fallback behind a per-booking room and an owner-typed link, and disclosed on the integrations card; **no data migration** of already-stamped links — they are neutralised (never printed publicly, never suppressing a per-booking room) rather than deleted, because they are indistinguishable from fallback links owners typed on purpose. |
| **Scope** | Correctness of an already-shipped surface. **Not** a provider-scope change: no new provider, no new developer account or app registration, no enlarged permission — so it does not act ahead of **Q-007**, whose window (closes 2026-09-01) governs exactly that. |
| **Status** | open — pre-`launched` (CHARTER Part 0), so work proceeds on this default at once and a veto reverts. |

### Q-011 · Pumasi Booking Zoom connect correctness — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-31-pumasi-booking-zoom-connect.md`](releases/2026-08-31-pumasi-booking-zoom-connect.md), published 2026-08-31. BACKLOG item 1 parts (b) and (c), `spec/0005`: the connect flow stores the OAuth connection instead of stamping the owner's personal meeting URL onto every Zoom event type; no public pre-booking page renders a joinable link for any conferencing kind; per-booking meeting creation actually runs, with a disclosed fallback order. Classed can-hurt by `RISK_ZONES.yaml` (service paths) — it handles a third-party credential and changes what an anonymous visitor is shown. |
| **Window closes** | **2026-09-07** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline; this is the charter's own duration applied, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. It is a net removal of exposure: a personal meeting room that any visitor could read is no longer printed, and no new provider, account or permission is introduced. A veto reverts the merge. |
| **Reviews** | Gemini approved spec (`reviews/20260831-001546-spec-gemini.md`) and code (`reviews/20260831-002759-code-gemini.md`), transcripts in `pumasi-booking/reviews/`. Grok unreachable — D-104 condition live; pre-`launched`, review is advisory (Part 0) and the single non-builder bar (P5) is met. `GATE: PASS` at `16c3fd4`. |
| **Relation to Q-007** | None claimed. This is correctness of an already-shipped surface: no provider added, no developer account or app registration created, no OAuth scope enlarged. Q-007's window (closes 2026-09-01) still governs whether conferencing scope widens. |
| **Status** | open — pre-`launched` (Part 0): the window does not hold the (reversible) release; a veto reverts. |

### Q-012 · Who deploys a merged fix, and by when
| | |
|---|---|
| **What** | Raised by the `pumasi-booking` product-manager evaluation of 2026-08-31 (ops `DIGEST.md`), from a concrete failure rather than a theory. The Zoom personal-meeting-room leak went through the full charter flow — intent, frozen acceptance cases, cross-family review, `GATE: PASS`, can-hurt release note (Q-011) — and **is still live on `booking.pumasi.ai`**, because nothing carried the build to the worker. `wrangler deployments list` puts the last deployment at 2026-08-30 16:55 UTC; the fix is 2026-08-31 05:27 UTC. `4f56df4` (the §5.1 reporting mechanism) is undeployed for the same reason. The gap is structural, not an oversight by any run: the flow in CHARTER §2.1 ends at a published release note, no role in `pumasi-ops/roles/` names deployment as a duty, and `HUMAN.md` does not reserve it — so it is agent work with no assignee, and a release note written in the present tense ("a public booking page never shows a joinable link") describes the branch while users meet the deployment. |
| **Why this is the steward's and not this seat's** | The product-manager role's strongest verb is *propose*, and it is disqualified from publishing. Assigning a duty is a change to the role register. This entry is the proposal; the assignment is the steward's. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **The coder role deploys, as the last step of the job that merged.** After `GATE: PASS` and the published release note, the same run deploys the reviewed build and records the deployment in its digest entry; a job that cannot deploy says so in the digest instead of leaving it unsaid. Nothing here widens what an agent may touch: `HUMAN.md` reserves only signatures, payment, and edits to itself, and deployment credentials are already held by the tooling. Two riders, so the default is not a licence: **(a)** it applies to a build that passed the gate and carries a release note, never to an unreviewed tree; **(b)** it does not cover raising ceilings, enabling public signup, or rotating secrets, which stay where they are today. |
| **Named alternative, if the default is wrong** | A separate operator/steward step, deliberately manual — in which case the cost is explicit: a fix to a live user-facing defect waits on a human, and `roadmap/STAGE.md` must say so under "known gaps" rather than implying merged means shipped. Either answer is honest; the current state, where the duty exists but has no owner, is the one that is not. |
| **Evidence added 2026-08-31 — recorded by the `pumasi-sign` product-manager seat (job `0045`); this seat does not close this window, may not deploy, and did not set a date** | This entry was raised from `pumasi-booking`. **`pumasi-sign` has now paid it in the same coin, and the *kind* of cost changed.** At that product's first two evaluations the undeployed set was page copy nobody had ever seen. As of `d18d534` it is **four merged changes deep, and one of them repairs a live user-facing defect**: issue #7, a signed-out user's *Sign in again* button, which returns `404 {"error":"Endpoint not found"}` as a rendered page on `sign.pumasi.ai` — measured 2026-08-31 20:10 UTC, with the deleted helper still present in the shipped bundle. A release note is published (**Q-027**) and its 7-day window is running over a fix nobody can reach. **This entry's own sentence — that a release note *"describes the branch while users meet the deployment"* — now has a second product and a sharper instance:** here the note is written in the past tense about a branch, deliberately and correctly, and the user still meets the defect anyway. Also recorded, because a run could get it wrong while willing: shipping this product means `wrangler deploy` from `service/`, **not** the Railway path `pumasi-sign/CLAUDE.md`'s Deployment section still describes (Q-018). Nothing here proposes a deployer or a date. |
| **Evidence added 2026-08-31 — recorded by the `pumasi-booking` coder seat (job `0049`); this seat did not deploy, did not set a date, and does not close this window** | **The cost this entry names has now been paid by a defect that was live while the entry was open.** `booking.pumasi.ai`'s workflow emails and webhooks have been undelivered since 2026-08-28 (**Q-029**). The repair merged at `0a35ddc` with `GATE: PASS` and a published release note, and it is the **sixth** merged build waiting: re-measured by this seat rather than quoted, `npx wrangler deployments list` still puts the newest deployment at **2026-08-30 16:55:37 UTC** (version `d73c05b5`, a *Secret Change*) — **more than 28 hours** before this measurement at 2026-08-31 21:30 UTC — while `https://booking.pumasi.ai/` answers 200 and `/healthz` still reports `"commit":"unknown"`. What is new is not the count. It is that this entry's opening example was a *leak*, which merging at least stopped growing; here merging changes nothing at all for the affected user, because the thing that is broken is a delivery that simply never happens and will not start until somebody runs a deploy. The release note says so in those words rather than being written in a present tense that describes the branch. Nothing here proposes a deployer or a date. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-booking` product-manager seat (job `0061`); this seat did not deploy, did not set a date, did not soften a default and does not close this window** | **A defect report arrived from a real user while this entry was open, and it is the first thing behind this window that deploying would *not* fix — which sharpens what the window is costing rather than adding to the count.** **(1) The wait, re-measured rather than quoted.** `npx wrangler deployments list` for the `pumasi-booking` worker, run by this seat at **2026-09-01 00:27:49 UTC**, still puts the newest deployment at **2026-08-30 16:55:37.479 UTC** (version `d73c05b5`, `Source: Secret Change`) — **31 h 32 m**. Curled at 00:27:29–30 UTC: `/` **200**, `/healthz` `{"status":"ok","commit":"unknown","sharded":true}`, `/version` **404**, `/readyz` `{"status":"ready","owners":11}`. **(2) The count's basis is corrected downward in precision, not in size.** Previous rows said "six merged builds" by counting commits on `main`, which included review transcripts and roadmap edits. Counted here as *commits that change `service/src/`*, the set behind the deployment is **six**: `4f56df4`, `16c3fd4`, `7ea730a`, `3f2947c`, `0a35ddc`, `2453adc`. Thirty-six commits sit on `main` behind it in total. **(3) The gap is now visible from outside without credentials, for the first time.** `2453adc` made both entry points answer `/healthz`, `/version` and `/readyz` with a `version` key. The live `/healthz` and `/readyz` have no such key and `/version` is a 404 page — so the deployment's age can now be demonstrated with `curl` rather than with a Cloudflare API round trip. Five evaluations needed the round trip; this one did not. **(4) A `PRODUCT-RULES.md` clause has joined the queue behind this window.** PR-1's *user-visible* clause — *"a person using the product can find the version without reading source"* — is a claim about the product, not the branch. It is met in `main` at `2453adc` (footer, `/version`, diagnostics row, both builds) and unmet where a user is. It has no remedy of its own; the only thing that closes it is a deploy. **(5) And the clause's stated cost was paid inside this evaluation.** [Issue #32](https://github.com/pumasi-ai/pumasi-booking/issues/32) was filed on the live product at **2026-09-01 00:36:56 UTC** — a booker shown no times for a day the page offered. It carries **no product version**, because the row that would have supplied one exists in `main` and not in the build its reporter used. PR-1's own sentence is *"A defect report without a version is a request to guess."* That is the first observed instance of it, and it arrived in the same hour the fix for it merged and failed to ship. **(6) What this row deliberately does not claim.** Issue #32 is **not** another item behind this window: the booking page's client-side slot renderer is **byte-identical** on the live build and in `main` (5151 characters, diffed at this evaluation), so deploying would not close it. It is ordinary product work and is ranked as that product's `BACKLOG.md` item 2. It is recorded here only because it shows this entry is not a superset of that product's defects, which a reader of the last three rows could reasonably have started to assume. Nothing here proposes a deployer or a date. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-sign` product-manager seat (job `0071`, sixth product evaluation); this seat did not deploy, did not set a date, did not soften a default, does not close this window, and names no person** | **Every previous row on this entry records the same failure: merged work not reaching users. This row records the other half of the same gap, and it is the more dangerous one — a deploy that happened with no release note and no entry naming the actor.** **(1) What happened, measured rather than reported.** `npx wrangler deployments list` for the `pumasi-sign` worker, run by this seat at **2026-09-01 02:00 UTC**: **four deployments in sixteen minutes** — `2026-09-01T00:46:02.975Z`, `00:58:51.828Z`, `01:01:10.912Z` and `01:02:16.876Z` — after a gap since `2026-08-30T22:03:15.660Z`. The live version is `96ff7004-45f0-439d-a4e0-68e35f739462`, created **01:02:16.132Z**. **Every one is `Source: Unknown (deployment)`**, i.e. a `wrangler deploy` from a workstation; `.github/workflows/ci.yaml` defines four jobs and none of them deploys. **(2) What is measurable about the actor, and nothing beyond it.** `wrangler` reports `Author: atxapplellc@gmail.com` on every deployment and version, which is the Cloudflare account identity and not a claim about a person. Separately, issues [#8](https://github.com/pumasi-ai/pumasi-sign/issues/8), [#7](https://github.com/pumasi-ai/pumasi-sign/issues/7), [#13](https://github.com/pumasi-ai/pumasi-sign/issues/13) and [#14](https://github.com/pumasi-ai/pumasi-sign/issues/14) were closed `completed` by the GitHub account `pumasiAI` at **00:59:11**, **00:59:12**, **01:01:19** and **01:02:24 UTC** — **each closure trailing one of the four deployments by seconds**. And [issue #15](https://github.com/pumasi-ai/pumasi-sign/issues/15) was filed from `https://sign.pumasi.ai/dashboard` at **01:21:39 UTC** by a signed-in session on Chrome 151 / Windows / `America/Chicago`. **Mapping either account, or that timezone, to a person is the steward's and this seat asserts nothing about it.** **(3) The build deployed corresponds to no pushed commit at the moment it was deployed.** Dated by chunk fingerprint: the served `SendView` chunk is post-`1338f68`, the served `SignedOutView` chunk matches `0e26917` exactly, and `wrangler versions view` reports **`Handlers: fetch`** with no `scheduled`, which puts it before `2471a29`. So the artefact is `0e26917`'s tree — and the deploy at **01:02:16.132Z** precedes that commit's own timestamp of **01:02:21Z** by five seconds. It was built from a working tree. **This is not a complaint about haste; it is the concrete form of what this entry's default addresses.** That default names *"the coder role deploys, as the last step of the job that merged"*, with rider (a) limiting it to *"a build that passed the gate and carries a release note"*. What happened here has neither property: no gate run is recorded against the deployed artefact and no release note describes it. **(4) What the deploy did, stated fairly, because it was not harmful in the main.** It delivered four merged repairs to users at once — #7's sign-in path, #8's landing page, `68e5d08`'s three envelope guards, and the login-page presentation batch — emptying `pumasi-sign` `roadmap/STAGE.md` §5's state (iii), which had held two occupants for five evaluations. **Three rows on this entry described those as costs of this window. They are paid.** **(5) And it did one thing no seat may undo, which is why this row is here rather than in a digest alone.** The same single bundle published *"Apache-2.0 (Open Source)"* from a repository with `licenseInfo: null` — **Q-021**, whose premise was that the claim was not yet public, and **Q-028**, which forecast this exact artefact. **An unowned deploy duty does not mean the deploy never happens. It means nobody knows when it happens, and no open question gets consulted before it does.** For five evaluations `pumasi-sign`'s registers said `NOT MET`, *"never public"* and *"still live to a user"* about a product where all three had stopped being true, and would have gone on saying so had a marketing seat not re-`curl`ed a bundle filename outside its own packet. **(6) What this row does not do.** It proposes no deployer, no date and no revert; it does not ask for the deployment to be rolled back; and it takes no position on the licence. Nothing here widens or narrows this entry's named default or its named alternative. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-booking` product-manager seat (job `0082`, seventh product evaluation); this seat did not deploy, did not name a deployer, did not set a date, did not soften a default and does not close this window** | **Every previous row on this entry records merged work not reaching users. This row records the first deploy that reached them from this product — and the one thing it did not carry.** **(1) The deploy, measured rather than reported.** `npx wrangler deployments list` for the `pumasi-booking` worker, run by this seat at **2026-09-01 05:15:44 UTC**: **three deployments** — `2026-09-01T00:38:41.738Z` (`aef92528`), `00:39:03.300Z` (`bf4934bf`) and `00:40:44.505Z` (`b6a2e64b`) — after six evaluations finding nothing since `2026-08-30T16:55:37.479Z`. All three `Author: atxapplellc@gmail.com`, all `Source: Unknown (deployment)`, no tag and no message. The live host now names its own build without credentials: `/version` → `{"version":"0.2.0","commit":"2453adc"}`, `/healthz` and `/readyz` likewise, and `https://booking.pumasi.ai/` serves `<span class="foot-v">v0.2.0</span>` — curled at 05:11:40 and 05:20:06 UTC. **(2) What it delivered, and it is this entry's whole backlog of costs.** `git merge-base --is-ancestor <sha> 2453adc` answers **yes** for all six: `4f56df4` (reporting), `16c3fd4` (**the Zoom PMI leak**), `7ea730a` (the OAuth callback), `3f2947c` (the sign-in doors), `0a35ddc` (**the alarm import**) and `2453adc` (the version). The leak named in this entry's opening paragraph is closed for the people it exposed. Reminders, follow-ups and webhooks are alive in production for the first time. PR-1's *user-visible* clause and its `GIT_COMMIT` half — which the row above records as having no remedy but a deploy — are met in the product. **The costs three rows of this entry describe are paid.** **(3) And the one thing it did not carry is the only one with a named person behind it.** [Issue #32](https://github.com/pumasi-ai/pumasi-booking/issues/32) was filed on the live product at **00:36:56 UTC** — **two minutes before the first of those three deploys began**. Its repair merged at `d7bd490` at 03:35 UTC, after them. `git merge-base --is-ancestor d7bd490 2453adc` answers **no**, and this seat re-measured the page in headless Chrome at **2026-09-01 05:12:11 UTC**, `America/Chicago`: 24 slots served, day cells 1 and 2 marked available, `#times.children.length` **0** on both, 0 errors captured. **The reporter still meets the defect.** `git log 2453adc..HEAD -- service/src/` returns three commits: `d7bd490`, `c000feb` (the subprocessor register, whose Zoom row is still absent from the served `/subprocessors`, re-measured 05:19 UTC) and `36f286c`. **(4) Why this row does not read as an answer to this entry.** A deploy happened; that is an **event**, not a **route**. Nothing in `pumasi-ops/roles/` gained a deployment duty, no release note records these three deployments, and no seat on the work that merged `d7bd490` was able to carry it — so the next merged repair queues exactly as the last six did. **The gap this entry names is unchanged in kind and is now demonstrated in both directions**: for five refreshes it cost users repairs that never arrived, and on 2026-09-01 it cost one user a repair that arrived four minutes too late to be included in a deploy nobody scheduled against it. **(5) One thing measured and one not.** The alarm import is on the deployed build; **whether the Durable-Object alarm actually re-armed after the deploy is not established** — that needs a booking against a real owner's page, which this seat did not make. Shipping fixed code is not the same as the feature running. **(6) And one commit on the deployed path that no flow produced, recorded because the next deploy carries it.** `36f286c` edits `service/src/pages.ts` with no trailer block, no intent, no spec, no entry here and no review transcript, authored **00:40:51 UTC** — seven seconds after the last of the three deploys. This seat found nothing wrong with its four lines and the suite is green at `d7bd490`; reviewing or reverting it is not this seat's. It is named so that a reader counting what a deploy carries does not have to discover it. **(7) What this row does not do.** It proposes no deployer, no date and no revert; it neither widens nor narrows this entry's named default or its named alternative; it takes no position on who ran the three deploys or on whether that answers this entry's question. |
| **Status** | open. **Not** covered by CHARTER Part 0's proceed-on-default rule — that rule releases *reversible* work from an open window, and this entry proposes assigning a duty, which is a register change and not this seat's to make. The undeployed fix meanwhile sits at the top of `pumasi-booking/roadmap/BACKLOG.md` as item 1, marked operator action. |


### Q-013 · `/oauth/*/callback` works without a calendar hub, and the OAuth state is always signed — intent statement, veto window
| | |
|---|---|
| **What** | [`pumasi-booking` `service/spec/0006/INTENT.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/service/spec/0006/INTENT.md), published 2026-08-31. `roadmap/BACKLOG.md` item 2: on a deployment with no calendar integration configured, `if (!hub) return html(404, …)` (`app.ts` ~999) answers the OAuth callback *before* the `zoom` branch, so the Zoom connect flow can never complete — while `/oauth/zoom/authorize` and the integrations POST start it happily. The same absent hub makes three call sites build the OAuth state as `Buffer.from(JSON.stringify({purpose, owner_id, tag})).toString('base64url')` instead of `hub.sealState(…)`. |
| **Why both halves in one change** | The unsigned state is unreachable today and that is the whole of its safety: `openState` only ever opens a sealed value, so the fallback is dead, and the 404 is what keeps it dead. Removing the 404 alone would leave a callback whose obvious next "fix" is to accept an attacker-chosen `owner_id` in an unsigned string. The state seal is therefore made the gate, and the unsigned fallback is deleted, in the same change. |
| **Window closes** | **2026-09-01** (24h from 2026-08-31) |
| **Default on silence** | Proceed with the intent statement's own assumptions: the OAuth state becomes its own provider-independent facility sealed with `TOKEN_KEY` (one implementation — `CalendarHub.sealState`/`openState` delegate to it rather than restating it, L-007); a deployment with no `TOKEN_KEY` **refuses to start** a Zoom connect with the reason, instead of building an unsigned state, matching the refusal the connection storage already gives after the round trip (Z1c); every other flow riding that callback — Google sign-in, Microsoft sign-in, org OIDC, calendar connect — keeps exactly the reachability it has today, each still guarded by its own credentials; and the neighbouring defect found while reading (**`/auth/microsoft/start` is gated on a *Google* calendar hub, so Microsoft sign-in is off on any deployment without Google Calendar credentials**) is recorded for the roadmap owner to rank, not folded in here. |
| **Scope** | Correctness of an already-shipped surface. **No** new provider, **no** new developer account or app registration, **no** enlarged OAuth scope — so it does not act ahead of **Q-007**, whose window closes 2026-09-01. |
| **Status** | open — pre-`launched` (CHARTER Part 0), so work proceeds on this default at once and a veto reverts. |

### Q-014 · Deploying to the tunnel relay disconnects its only real user — does Q-012's default extend to a stateful host?
| | |
|---|---|
| **What** | Raised by the `pumasi-tunnel` product-manager evaluation of 2026-08-31 (ops `DIGEST.md`), from the next fix rather than a theory. `roadmap/BACKLOG.md` items 1 and 2 both change relay code, and item 1's second half puts a TLS terminator in front of `*.pumasi.link`. Every one of those needs the relay process on the Vultr host restarted or fronted — and **the relay keeps no durable state**: its subdomain registry and TCP port pool are in-memory maps (`core/route.go`, `core/portpool.go`), so a restart drops every live tunnel. Today that set is exactly one: `sshsteward`, `pumasi.link:20000` → port 22, open 7 h 50 m at the time of measurement, which is `RESOURCES.md` §4's `ssh -p 20000 m@s.pumasi.link` — how this machine is reached. **Q-012** names the coder as its default deployer; it was raised from `pumasi-booking`, where a deploy is a Cloudflare Workers upload that displaces no session. Applying the same default here means a coder job restarting the host that carries the steward's remote access, with no reconnect guarantee for the name or the port it hands back. |
| **Why this is the steward's and not this seat's** | The product-manager role's strongest verb is *propose*, and it is disqualified from deploying. This entry does not ask whether TLS should exist — that is ordinary agent work: `HUMAN.md` reserves signatures, payment and edits to itself, an ACME subscriber agreement is a click-through it explicitly permits, and Let's Encrypt costs nothing. It asks only whose hand restarts a host whose sole live tunnel is the steward's own access, and under what precautions. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Q-012's default extends: the coder deploys as the last step of the job that merged**, with three riders specific to this host. **(a)** Tailscale is verified reachable first — `RESOURCES.md` §3 keeps it as the independent fallback for `m-gtr` precisely so that a bug in this young product costs an inconvenience rather than access, and that is worth nothing unverified. **(b)** The restart, and the state of `sshsteward` after it, are named in the job's digest entry; the keepalive cron (`tools/pumasi-tunnel-keepalive.sh`, `@reboot` + 5-minutely) is confirmed to have re-established the tunnel on the same address before the job reports done. **(c)** Nothing here covers `ufw` changes, raising ceilings, or rotating the relay's ssh host key, which stay where they are today. |
| **Named alternative, if the default is wrong** | A steward-only restart window for this host. The cost is explicit and should then be written into `roadmap/STAGE.md`: relay fixes queue behind a human, and a product whose stage notes already say a restart drops every tunnel would add that it also waits. |
| **What retires this entry** | `BACKLOG.md` item 3 — durable registry and port reservations. Once a restart no longer costs anyone their address, this stops being a question and becomes an ordinary deploy under Q-012. |
| **Evidence added 2026-08-31, post-`0041` — recorded by the product-manager seat, which does not close this window and did not deploy** | Nothing here answers the question; it updates what waiting costs. **(a)** `1d9505c` merged a second relay change (bind before announce), so there are now **two** merged-and-undeployed changes behind this one restart, not one — the scheme fix from `83fd9f7` and this. **(b)** Re-measured read-only at 20:06 UTC 2026-08-31, no host touched: `http://pumasi.link/_pumasi/status` still reports `"url":"https://sshsteward.pumasi.link"` with 443 refused and 80 at 200, and the `sshsteward` tunnel is **open 49 671 s (13 h 48 m) unbroken** (`"opened_at":"2026-08-31T06:18:13Z"`) — the same single live user this entry is about, now a longer-running one. **(c)** The same read shows the deployed relay's `"tcp_range"` is **20000–20099**, below the host's ephemeral floor; that is what bounds `pumasi-tunnel` `BACKLOG.md` item 4 in production and it is worth having on the record before any redeploy changes it. **(d)** This entry's retirement condition is unchanged and untouched: `BACKLOG.md` item 3, durable registry and port reservations, which this pass kept at rank 3. |
| **Evidence added 2026-09-01, post-`0060`/`0066` (`pumasi-tunnel` `1853218`) — recorded by the product-manager seat (job `0076`), which did not deploy, did not restart anything, did not propose a deployer and did not touch the window** | Nothing here answers the question; it changes one of the facts the question was written on. **(a) The live set is no longer *exactly one*.** Re-measured read-only at 02:48 UTC 2026-09-01, no host touched: `GET http://pumasi.link/_pumasi/status` reports **`"count":2`**. Beside `sshsteward` (`pumasi.link:20000` → this machine's port 22, `"fixed":true`, `"opened_at":"2026-08-31T06:18:13Z"`, **`"age_secs":73789` — 20 h 30 m unbroken**, the same connection as at the last two passes) there is **`skk6g7tyrs`**, `pumasi.link:20002` → a `"local_port":3389`, `"fixed":false`, opened `2026-09-01T01:48:23Z` and 59 minutes old at the read. **This seat did not establish who opened it and does not guess.** It is anonymous by design: `cmd/pumasi-relay` defines eleven flags and none of them is an auth flag, so `AllowAll` is the only authenticator that binary can run. So a restart now costs **two** parties their address rather than one, and this entry's own text — *"its one live tunnel is `sshsteward`"* — describes a state that no longer holds. That makes the question larger, not smaller. **(b) The undeployed queue is now five merges, of which three change behaviour** — `83fd9f7` (scheme), `3480990` (bind before announce) and `fd523e8` (session before announce); `e40a224` and `b3d251d` touch tests and specs only and would change nothing on the host. `fd523e8` is new since the last row here. **(c) The rest of the read is unchanged**: 443 still refuses, 80 still answers `200`, `pumasi.link:2222` still greets `SSH-2.0-pumasi-tunnel`, and `"tcp_range"` is still **20000–20099**, below the ephemeral floor. **(d) The retirement condition is unchanged and untouched** — durable registry and port reservations, which after this pass's re-rank is `pumasi-tunnel` `BACKLOG.md` **item 2** (was item 3), and which is now the **highest build entry** on that list. That is a renumbering, not a promotion: it reached the top because the three-line entry above it was delivered, not because it became more urgent. |
| **Evidence added 2026-09-01, post-`0081` (`pumasi-tunnel` `9e2de66`) — recorded by the `pumasi-tunnel` product-manager seat (job `0084`), which does not close this window, did not set a date, did not soften a default, did not deploy, did not restart anything and does not propose a deployer** | **Half of this entry's retirement condition's backlog item merged, and it is the half this entry does not name.** **(a) The retirement condition is untouched.** The row above says what retires this entry is *"durable registry and port reservations"*. `pumasi-tunnel` `4489fbe` + `c12d11a` + `20e9d57` (job `0081`, `spec/0004-names-with-owners`) delivers **slice 1, ownership** — a name and its public TCP port held for their owner across a *disconnect*, refused to everyone else, **in memory only**. **Slice 2, durability, is specified in full and not built**, and the spec says so twice: §7, *"the relay-restart half of item 2 is **not** delivered"*, and §4, *"**Saying slice 1 retires Q-014 would be false and this spec does not say it.**"* Verified in the tree at `9e2de66` by this seat rather than taken from the build report: **no store** (`os.WriteFile` / `os.Rename` / `os.Create` / `encoding/json` over `core/` and `relay/` excluding tests returns two hits, both wire encoding), **no `LastSeen`**, **eleven flags on `cmd/pumasi-relay` and none of them `-reservations`**, and **the acceptance case for the property does not exist in the tree** — `grep -rn "func TestAReservationOutlivesTheRelay(" --include=*_test.go .` returns **0**, against **1** for each of the other nineteen cases the same spec names. **(b) What did merge narrows this entry's premise — and the narrowing is conditional on the very act this entry gates.** Persistence cannot shorten an outage: a live TCP connection cannot outlive the process at either end, and what already bounds the steward's outage is `pumasi-ops/tools/pumasi-tunnel-keepalive.sh` on a five-minute cron plus the agent's 1 s → 30 s backoff. What a reconnect cannot recover is the **loss of the name or the port to somebody else**, and ownership is what prevents that — so a restart would cost the route *only if* somebody took `sshsteward` or port `20000` during the reclaim window. **But none of that is true of the running relay**, which does not have the change, and deploying it is the act this entry gates. Until a deploy happens the steward's route is exactly as exposed as it was before the merge. **(c) And on the day of a deploy it would still gain nothing, which is an action for `pumasi-ops` rather than a question for this entry.** `pumasi-tunnel-keepalive.sh` passes **no `--token`** — `grep -c token` over it returns **0**, and its invocation at lines 39–42 is `--relay` · `--subdomain sshsteward` · `--tcp --tcp-port` · the local port and nothing else — so `sshsteward` would be *unclaimed* under the new binary and takeable exactly as today. Adding one is harmless against the binary running now, which ignores the field, and it should land **before** any deploy rather than after. One implementation note for whoever takes it, so it is not rediscovered: the script's own health check is `pgrep -f "pumasi-tunnel --relay $RELAY .* --tcp "`, so a `--token` inserted between `--subdomain` and `--tcp` still matches and a token placed after `--tcp` would break it. Named as a precondition in `pumasi-tunnel` `roadmap/BACKLOG.md` item 1(i) and handed up in job `0084`'s return block; not reached for from here. **(d) The live set is still two, and the undeployed queue is now eight.** Re-measured read-only at **05:51:04 UTC 2026-09-01**, no host touched: `"count":2` — `sshsteward` `"age_secs":84771` (**23 h 32 m** unbroken) and `skk6g7tyrs` `"age_secs":14561` (**4 h 02 m**), both on the same `opened_at` as the last pass, so persisting connections rather than new ones; 443 still refuses, 80 answers `200`, `2222` greets `SSH-2.0-pumasi-tunnel`, `"tcp_range"` still `20000–20099`. **Eight commits touching the Go tree now sit on `main` that the host does not have, six of them changing non-test code and delivering a fourth distinct capability** (was five and three). **This pass dated the running binary without touching it:** slice 1 puts a `"reserved"` key on every tunnel in `/_pumasi/status` (`relay/dashboard.go:42`, a plain `bool` with no `omitempty`), and the read above carries **no `reserved` key on either tunnel** — so the host predates `4489fbe`. **(e) The retirement condition is unchanged and untouched.** After `pumasi-tunnel`'s re-rank of 2026-09-01 (`d436ef1`) it is that repository's `BACKLOG.md` **item 2**, *the relay-restart half — a reservation that outlives the process*; the ownership half is under *Delivered*. This entry's own text still describes the live set as *"exactly one"*, which it has not been since 2026-09-01 01:48 UTC. |
| **Status** | open. Like Q-012, **not** claimed under CHARTER Part 0's proceed-on-default rule: that rule releases reversible work from an open window, and this asks who may take an action that disconnects a live user. Meanwhile `BACKLOG.md` item 1's build half (a) — announcing the scheme the relay actually serves — needs no deploy decision to be written, reviewed and merged. |


### Q-015 · Pumasi Booking OAuth callback reachability — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-31-pumasi-booking-oauth-callback.md`](releases/2026-08-31-pumasi-booking-oauth-callback.md), published 2026-08-31. `roadmap/BACKLOG.md` item 2, `service/spec/0006`: `/oauth/*/callback` gates on being able to open a sealed OAuth state rather than on the presence of a calendar hub, so a deployment with a Zoom app and no calendar integration can complete a Zoom connection; and the unsigned `base64url` state three call sites built in that case is deleted. Classed can-hurt by `RISK_ZONES.yaml` (service paths) — it changes the authentication of the value that says whose third-party connection is arriving. |
| **Window closes** | **2026-09-07** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline; this is the charter's own duration applied, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. It is a net removal of exposure: the only unauthenticated state construction in the service is gone, and the gate it replaces is a stronger one (a signature, not a configuration check). No new provider, account or permission. A veto reverts the merge. |
| **Reviews** | Gemini approved the spec (`reviews/20260831-090554-spec-gemini.md`), the amendment to it (`reviews/20260831-091352-spec-gemini.md`), and the code (`reviews/20260831-092229-code-gemini.md`; an earlier run at `reviews/20260831-091903-code-gemini.md` reviewed the same tree under hashes that a trailer-format rewrite then replaced — kept, and explained in the commit). Grok unreachable — D-104 condition live; pre-`launched`, review is advisory (Part 0) and the single non-builder bar (P5) is met. `GATE: PASS` at `4f6ddf0`. |
| **Relation to Q-007** | None claimed. Correctness of an already-shipped surface: no provider added, no developer account or app registration created, no OAuth scope enlarged. Q-007's window (closes 2026-09-01) still governs whether conferencing scope widens. |
| **Relation to Q-012** | **Not deployed, and not deployable by this run.** Q-012 (who carries a merged build to `booking.pumasi.ai`) is open and is explicitly outside Part 0's proceed-on-default rule, so this seat did not deploy and did not take `BACKLOG.md` item 1. Unlike Q-011's release, the undeployed state costs little here: the defect this closes cannot occur on `booking.pumasi.ai`, which has a calendar integration. Those affected are self-hosters who deploy their own copy from the repository. |
| **Status** | open — pre-`launched` (Part 0): the window does not hold the (reversible) release; a veto reverts. |

### Q-016 · Pumasi Sign multi-document envelopes — does "just like DocuSign" mean one merged PDF or many documents?

| | |
|---|---|
| **What** | Raised by the `pumasi-sign` feedback intake of 2026-08-31 from customer issue [#4](https://github.com/pumasi-ai/pumasi-sign/issues/4): *"often users want to add multiple documents for one envelope just like docusign."* The ask itself is not in question — it is already `roadmap/BACKLOG.md` item 4. What is in question is what counts as delivering it, because that file records two different products under one line: *"multi-document envelopes as separate files with per-file thumbnail cards (today: merged to one PDF at upload — the card UI can front the merge first, true multi-doc needs backend)."* Option A shows a user several cards and stamps, certifies and delivers **one** merged PDF. Option B keeps documents separate end to end: separate files, separate stamping, per-document integrity in the certificate, separate download. |
| **Why this is the steward's and not this seat's** | It is a change to what the product promises, not a scheduling choice, so the product-manager role escalates rather than decides. The public landing page added in `10a523d` sells this product against DocuSign by name and promises "cryptographic SHA-256 tamper-evident certificates". A customer who asks for DocuSign's behaviour and is given a merge gets a certificate that attests to a concatenation — one hash over a document set that the user believes is several documents. That may be perfectly acceptable; it is not something an agent should decide quietly inside a UI ticket, because the difference only becomes visible to the customer at the moment they need one document out of an envelope and cannot get it. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Option A, with the limitation stated in the UI.** Build the per-file thumbnail cards over the existing upload-time merge, and say plainly on the send screen and in the certificate that the documents were combined into a single signed PDF. Reasons: it is the smaller change, it needs no backend rework, it delivers the ergonomics the customer actually described (adding several files without merging them by hand first), and stating the limitation keeps the certificate honest — the failure mode this default guards against is a certificate that implies more separation than exists. Option B stays on the backlog as item 4's second half rather than being dropped. |
| **Named alternative, if the default is wrong** | **Option B first**, accepting that item 4 stops being a frontend task: separate storage per document, per-document stamping in `backend/app` and the worker, per-document hashes in the certificate, and a download picker that can return one document or a zip. Materially larger, and it would displace items 2 and 3 in the current ordering. Worth choosing if the intended buyer is one who sends mixed document sets — a contract plus its exhibits — where "give me exhibit C" is a routine request. |
| **What retires this entry** | A decision recorded here, and `roadmap/BACKLOG.md` item 4 rewritten to name one option rather than describing both. |
| **Status** | open. Pre-`launched` (CHARTER Part 0), so the frontend card work under Option A may proceed on the default; a veto reverts. Nothing here authorizes the Option B backend rework. |


### Q-017 · `PRODUCT-RULES.md` exists only on an unmerged branch — merge it, or amend the role file that requires it

| | |
|---|---|
| **What** | Raised by the `pumasi-booking` product-manager evaluation of 2026-08-31 (ops `DIGEST.md`), from a file that three consecutive evaluations have flagged and nothing has moved. The product-manager role file opens with **"Read first, every packet: `PRODUCT-RULES.md` in the `pumasi` repository"** and adds that it is read fresh each run, never cached or vendored, because a stale copy is L-007. That file is **not on `pumasi` main**. It exists only on the pushed branch `worktree-product-rules` (`0115758`, v1.0, 2026-08-30, created by steward direction), which is one branch deletion away from taking PR-1 and PR-2 with it. Every packet that obeys the role file today reads a rule register from a branch that `main` does not know about, and each one has had to say in its own commit that absence from `main` is not compliance. |
| **Why this is the steward's and not this seat's** | The file was created by steward direction, and merging it is what makes PR-1 (version numbers, binds always) and PR-2 (in-app feedback, binds at `beta`) bind on every product — a change to what the products promise, which the role file's own duty 1 defines as escalation ground. This seat may write a `BACKLOG.md` entry against a rule; it may not decide that the rule register is policy. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Merge `worktree-product-rules` into `pumasi` main as it stands**, with no rule edited: v1.0, PR-1 and PR-2 with the stages they already name, the change log intact. The merge is what the file's own header already assumes — it calls itself "Live, continuously updated" and tells the product-manager role to read it every packet. Nothing here adds a rule, changes where one binds, or creates a compliance table; PR-1's version work stays where it is, `pumasi-booking/roadmap/BACKLOG.md` item 3. |
| **Named alternative, if the default is wrong** | The register is **not** yet policy — in which case the cost is explicit and lands somewhere else: `pumasi-ops/roles/product-manager.md` must stop instructing every packet to read first a file that `main` does not contain, and the `BACKLOG.md` entries citing PR-1 across the products must be re-grounded or dropped. Either answer is workable; the current state, where a mandatory read points at an unmerged branch, is the one that is not. |
| **What retires this entry** | `PRODUCT-RULES.md` present on `pumasi` main, or the role file amended to match reality. |
| **Status** | open. **Not** claimed under CHARTER Part 0's proceed-on-default rule: an agent merging the steward's own rule register into `main` would be deciding that the rules are policy, which is not a reversible product change and is not this seat's to make. Meanwhile the role file is obeyed as written — the branch copy is read fresh each packet, and `pumasi-booking`'s PR-1 gap stays ranked on it. |

### Q-018 · Which implementation is Pumasi Sign — the tested FastAPI backend, or the deployed Cloudflare Worker?

| | |
|---|---|
| **What** | Raised by the `pumasi-sign` coder job of 2026-08-31 (ops `DIGEST.md`, job 0018), from a live reproduction rather than a theory. The repository carries **two complete, independent backends for one product**. `backend/` is a FastAPI + Postgres app: it is what `CLAUDE.md` documents, what `Dockerfile` builds, what Alembic migrates, what the `backend` CI job's 545 tests cover, and what the `e2e` job Dockerizes and drives with Playwright. `service/` is a Cloudflare Worker with a Durable-Object SQLite store and R2 documents, and `service/wrangler.jsonc` claims `sign.pumasi.ai` as a `custom_domain`. **The worker is what users meet.** Verified against the live host this tick: `POST /api/auth/dev-login` and `POST /api/auth/email/request` — both FastAPI routes — return `404 {"error":"Endpoint not found"}`, which is the worker's error body (`service/src/durable.ts:1533`); FastAPI would answer `{"detail": …}`. The worker-only `POST /api/auth/login/verify` answers `401 {"error":"Invalid or expired verification code"}`, and `GET /api/auth/me` answers `401 {"error":"Not signed in"}`. **CI never builds, lints or tests `service/` at all** — `service/src/test/` exists (`stamping.test.ts`, `e2e-workflow.test.ts`) and no workflow runs it. So the 545 green backend tests and the six e2e specs describe a tree no user reaches. The two trees also **disagree about who may hold an account**: `backend/app/config.py:22` gates on `ALLOWED_EMAIL_DOMAINS` (default `pumasi.ai`) at `auth.py:206`, while the worker's `establishSession` (`service/src/durable.ts:655`) creates an account for any verified email with no domain gate at all — reproduced live, `POST /api/auth/login/request` for `someone@example.com` returns `200`. This is [L-009](lessons/L-009-two-paths-one-claim.md)'s exact shape — the lesson was written from `pumasi-booking`'s Node-vs-Workers split — reappearing in a second product, plus [L-006](lessons/L-006-tests-that-cannot-fail.md) at suite scale: a green gate that cannot fail for the deployment it is read as covering. |
| **Why this is the steward's and not this seat's** | Not a defect with a repair — both trees work. The question is which one *is* the product, and answering it means either retiring a working implementation or committing to maintain two. It also settles who may hold an account, because the two answers differ today. A coder job may fix a bug in either tree; it may not choose which tree the product is. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **The deployed Cloudflare Worker is Pumasi Sign, and the documentation and the gate are corrected to say so — without deleting anything.** Three parts, all reversible: **(a)** `CLAUDE.md` stops describing Railway/FastAPI as *the* deployment and names `service/` on Cloudflare as what serves `sign.pumasi.ai`, with `backend/` marked as a second implementation that is not in production; **(b)** CI gains a job that runs `service/src/test/`, so the code users actually meet is covered before the next behaviour change ships; **(c)** no claim about production is read off the `backend`/`e2e` jobs until (b) exists. What this default deliberately does **not** do is retire `backend/`, re-point the domain, or migrate data — those are irreversible or user-visible and stay the steward's. |
| **Named alternative, if the default is wrong** | FastAPI on Railway is the product and the worker is an experiment that must give up the `sign.pumasi.ai` route. Then the cost is explicit and belongs in `roadmap/STAGE.md` under known gaps: the live host must be re-pointed, and every account, session and document now living in the worker's Durable Object and R2 bucket — real users' data — must be migrated or knowingly dropped. |
| **Relation to Q-012** | Q-012's default names the coder as deployer of a merged, gate-passed build. It is unaffected by this entry except in one practical respect: carrying a `pumasi-sign` build to users means `wrangler deploy` from `service/`, **not** the Railway push that `CLAUDE.md` describes. A run that followed `CLAUDE.md` would deploy a tree no user reaches and report it as shipped. |
| **Status** | open. The default's three parts are documentation and CI only — reversible, and pre-`launched` Part 0 releases them from the window; this job did not take them, because it was scoped to the entry path and the red gate, and (a) touches `CLAUDE.md`'s description of the whole product. Recorded here so the next `pumasi-sign` job does not rediscover it. **Update 2026-08-31, coder job 0032 (`pumasi-sign` `ef851d6`): the default's parts (a), (b) and (c) are now taken**, under CHARTER Part 0 as reversible documentation-and-CI work — `CLAUDE.md` describes both trees and names the worker as what serves `sign.pumasi.ai`; `.github/workflows/ci.yaml` gained a `service` job whose ability to fail was proven on real runs ([break](https://github.com/pumasi-ai/pumasi-sign/actions/runs/33419949879), [unbuilt](https://github.com/pumasi-ai/pumasi-sign/actions/runs/33419950651)); and (c) is carried as a sentence in `CLAUDE.md` rather than by deleting a job. **The entry stays open and its default is unchanged**: nothing was deleted, the domain was not re-pointed, no data moved, and *which tree is the product* — the question this entry actually asks — is untouched and still the steward's. |


### Q-019 · `catalog.json` is the file every agent is told to read first, and no role file owns it

| | |
|---|---|
| **What** | `README.md` tells every arriving agent **"start with `catalog.json`"** and names it the duplication check the charter requires before anything new is built. Verified against `pumasi` main @ `5bd2d81` this tick, not inherited: the file contains **zero occurrences of the string `tunnel`** — **Pumasi Tunnel has no `products[]` entry and no `items[]` entry at all**, though its `roadmap/STAGE.md` records the Stage-1 exit gate **MET 2026-08-31**, it serves two live public surfaces (`http://pumasi.link/` → 200, `pumasi.link:2222` → `SSH-2.0-pumasi-tunnel`), and `pumasi.ai` has carried its catalog card since `c2084a8`. Alongside that: `Pumasi Booking` is `"status": "seed"` in `products[]` and `"maturity": "seed"` in `items[]`, against **`beta`** in its own `roadmap/STAGE.md` (set 2026-08-29, evidence refreshed twice on 2026-08-31); `"updated"` still reads `2026-08-29`; `items[].tests.service` reads `246` against 305 in the 2026-08-31 release note. Surfaced by the commons marketing job `0021`, which was right not to edit it — it is not that seat's file. |
| **Why this is the steward's and not this seat's** | Because **no seat owns it**, and the fix is a role-file amendment rather than a content edit. `roles/ecosystem-manager.md` duty 4 says "Keep `catalog.json` and `pumasi-web` accurate" — but the ecosystem manager is **not** in `roles/project-manager.md`'s closed list of what a tick may enqueue, which ends "No other inventions", so no clock can start it. `roles/marketing-manager.md`'s **May Write** names `pumasi-web` and product `web/`, not `pumasi/catalog.json`. `roles/product-manager.md`'s **May Write** is `roadmap/*`, issue labels, `DECISIONS.md` questions and the ops digest — not `catalog.json`. `roles/product-hunter.md` is explicitly forbidden it. This seat reads it and does not own it. The commons index is the one public file in the project with a stated duty and no assigned owner — the `STAGE_PLAYBOOK.md` Zero-Unassigned-Duty Rule failing on the playbook's own front door. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Split the file along the seam that already exists, and amend the two role files to say so.** **(a) Registering a product** — its first `products[]` and `items[]` entries — becomes part of the **marketing manager's** Day-1 mandate, next to Surface A (`pumasi-web/content/products/<slug>.md`), and `roles/marketing-manager.md` gains `pumasi/catalog.json` in **May Write**. That is the duty that was missed for Pumasi Tunnel: Surface A shipped, the index entry did not. **(b) Keeping a registered product's `status` / `maturity` true** becomes part of the **product manager's** duty 6 (the stage): the run that writes `roadmap/STAGE.md` updates that product's own catalog fields and the top-level `updated` in the same commit and the same reasoning, so the two cannot fork (L-007). `roles/product-manager.md` gains `pumasi/catalog.json`, **that product's entries only**. **(c)** Neither seat may edit another product's entry, and neither touches the `governance`, `structure` or `licence` blocks. The content edits this unblocks are reversible file edits on a pre-`launched` commons, so the first packet under this default proceeds at once and a veto reverts. |
| **Named alternative, if the default is wrong** | Stand the **ecosystem manager** up as a queued role and give it the file whole. Then the cost is explicit and lands in one place: `roles/project-manager.md`'s enqueue list must name it, because that list as written forbids this tick from dispatching it — and a role that no clock may start is a role that does not run, which is the state it is in today. |
| **What retires this entry** | `pumasi-tunnel` present in `catalog.json`, Booking's catalog status matching its own `STAGE.md`, and at least one role file naming `pumasi/catalog.json` in its **May Write**. |
| **Status** | open. The *content* is reversible and pre-`launched`, but the thing that makes it fixable — amending another role's **May Write** — is a register change and not this seat's to make. Meanwhile the index stays wrong in public: an agent running the charter's duplication check today is told Pumasi Tunnel does not exist. |


### Q-020 · Pumasi Tunnel announces the scheme it serves — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-31-pumasi-tunnel-public-scheme.md`](releases/2026-08-31-pumasi-tunnel-public-scheme.md), published 2026-08-31. `pumasi-tunnel` `roadmap/BACKLOG.md` item 1, **build half (a) only**, `spec/0001-public-scheme`, merged at `83fd9f7`. `Registry.PublicURL` hard-coded `https://` while nothing has ever listened on `pumasi.link:443` — verified live at 15:30 and 15:37 UTC, `curl` exit 7 and a refused connect on 443 against an open 80. The relay now takes `-public-scheme` (default `http`, the truth about what it serves), validated once at startup, applied in one place, and read by all three surfaces that show a person an address: the CLI's first line, the console at the relay's apex, and the zero-install ssh banner. An unknown scheme stops the relay rather than being coerced. |
| **Classification** | Can-hurt, by CHARTER Part 4's own default: `pumasi-tunnel` has no `RISK_ZONES.yaml`, and an unmapped path defaults to can-hurt. The cross-family reviewer read the change as *ordinary* (no money, credential or personal data is handled). Both readings are recorded rather than one quietly chosen; the charter's default rule governs, and guessing in the safe direction costs one review and this window. |
| **Window closes** | **2026-09-07** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline; this is the charter's own duration applied, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. It is a net removal of a false statement: the product stops printing an address that cannot be reached. A veto reverts the merge, which restores `https://` on an unserved port. |
| **What it can cost, and who** | One class of operator: someone who already runs a TLS terminator in front of their own relay and upgrades without setting `-public-scheme=https`. Their users would be told `http://` where `https://` worked. The remedy is one word in the unit file; it is in the flag's help text, the package header of `cmd/pumasi-relay`, and the release note's first and last sections. The affected set is today known and empty — `pumasi.link` has no terminator, which is the defect. |
| **Reviews** | Gemini approved the spec (`reviews/20260831-104146-spec-gemini.md`) and the code (`reviews/20260831-104842-code-gemini.md`; an earlier transcript of the same change before the `package.json` gate adapter was added, `reviews/20260831-104546-code-gemini.md`, is kept and cited in the commit). Grok unreachable — HTTP 402, balance exhausted; **D-104 condition live**, so §3's rule that the spec reviewer must not be among the code reviewers cannot bind and is off rather than pretended. Pre-`launched`, review is advisory (Part 0) and P5's single non-builder bar is met. `GATE: PASS` at `83fd9f7`. |
| **Relation to Q-014** | **Not deployed, and this run did not treat it as a judgement call.** Q-014 governs who may restart the relay on the Vultr host, is open, and is explicitly outside Part 0's proceed-on-default rule. The relay keeps no durable state, and its one live tunnel is `sshsteward` — `pumasi.link:20000` → port 22, open **9 h 29 m** and unbroken at 15:47 UTC, which is `RESOURCES.md` §4's route to that machine. `pumasi.link` therefore still announces `https://` and will until someone deploys. Half **(b)** of the backlog item — the wildcard certificate — was not touched, and neither were `ufw`, host keys, ceilings or DNS. |
| **Status** | open — pre-`launched` (Part 0): the window does not hold the (reversible) merge; a veto reverts. |

### Q-021 · Pumasi Sign's public page claims Apache-2.0 and the repository carries no licence

| | |
|---|---|
| **What** | Raised by the `pumasi-sign` product evaluation of 2026-08-31 (ops `DIGEST.md`), while setting `roadmap/STAGE.md`. The landing page merged in `10a523d` states in its stage banner *"Unmetered PDF stamping & SHA-256 audit certificates under Apache-2.0"* and gives the comparison table a **License & Source Code** row reading *"Apache-2.0 (Open Source)"* against *"Proprietary Closed Source"* for both named competitors. **The repository contains no `LICENSE` file**, and `gh repo view pumasi-ai/pumasi-sign --json licenseInfo` returns `null`. A public repository with no licence grants no rights: the claim is not merely uncited, it is not true today. Two sibling products already carry the file — `pumasi-web/LICENSE` and `pumasi-tunnel/LICENSE`, both Apache License 2.0 — so this reads as an omission rather than a decision. **Not yet public:** the page is on `main` and undeployed (the live bundle contains no `landing` route, verified 2026-08-31), which is the whole reason this is a question and not an incident. It becomes public the moment `pumasi-sign` `roadmap/BACKLOG.md` item 1(d) runs. |
| **Why this is the steward's and not this seat's** | Choosing the terms on which the organisation's code is given away is a change to what the product promises — the product-manager role file's own escalation ground — and it binds beyond this product: the answer sets whether "open source" is a claim any Pumasi product may make. This seat may refuse to write the claim into `VALUE.md`, and has ([`roadmap/VALUE.md`](https://github.com/pumasi-ai/pumasi-sign/blob/main/roadmap/VALUE.md) C5 is suspended pending this entry). It may not decide the licence. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Add `LICENSE` to `pumasi-sign`, Apache License 2.0, byte-identical to `pumasi-web/LICENSE`, honouring the claim the page already makes.** Nothing else changes: no header block added to source files, no `NOTICE`, no relicensing of any other repository, no change to what the page says once the file exists. This is the smaller and more reversible of the two answers — adding a permissive licence is additive, and it matches what two of the four products already do. |
| **Named alternative, if the default is wrong** | **Remove the claim instead**, and the cost is explicit and lands in three places: the landing page's banner and comparison row (`frontend/src/views/LandingView.vue`), `roadmap/VALUE.md`'s §1 "not yet: the operator who wants to run it themselves" and claim C5, and `roadmap/MARKET.md` §2–3, whose stated wedge is currently two clauses wide for exactly this reason. Also worth naming: the repository is already public, so the alternative is *"readable, not reusable"* — a coherent position, but one that should be chosen rather than arrived at by never writing the file. |
| **What retires this entry** | Either `LICENSE` present on `pumasi-sign` main, or the three copy locations above no longer claiming an open-source licence. Either way, `BACKLOG.md` item 1(b) closes and the page may deploy. |
| **Blocks** | `pumasi-sign` `BACKLOG.md` item 1(d), the deploy that closes issue [#8](https://github.com/pumasi-ai/pumasi-sign/issues/8) — **only for half (b)**. Halves (a) the stage chip and (c) the uncited competitor pricing need no decision here and proceed at once. |
| **Evidence added 2026-08-31 — recorded by the `pumasi-sign` product-manager seat (job `0045`), which does not close this window, does not resolve the licence, and does not restate the question as settled** | Two corrections and one new consequence, all measured at `main` @ `d18d534` and against the live host on 2026-08-31 20:10–20:12 UTC. **(1) The "not yet public" premise is stronger than this entry states, and it was checked properly for the first time.** This entry says the page "is on `main` and undeployed (the live bundle contains no `landing` route)". That is right, and the route *table* now confirms it rather than a string count: the shipped bundle registers `/`, `/admin/users`, `/branding`, `/envelopes/:id`, `/login`, `/privacy`, `/send/…`, `/signed-out`, `/sign/…`, `/templates`, `/terms` and the catch-all, with route names including `dashboard` and **not** `landing`. In production `/` is the dashboard. `LandingView.vue` has **never** been deployed, so no user has ever seen the Apache-2.0 claim — nor the `BETA` chip beside it. (`pumasi-sign/roadmap/STAGE.md` §5 previously filed that chip as *live in production*; this seat corrected it in the same pass.) **(2) The claim's three sites are unchanged:** `LandingView.vue:43`, `:80`, `:210`, and the repository still carries no `LICENSE`. **(3) New, and the reason this row exists: this entry now blocks more than it says it does.** Its `Blocks` row names only the deploy that closes issue #8, because when it was written #7's repair did not exist. It does now, merged at `d18d534` — and `service/wrangler.jsonc:8` serves `../frontend/dist` as **one** `ASSETS` directory while `frontend/src/router/routes.ts:15` puts `LandingView` at `/`, with `frontend/dist` `.gitignore`d and rebuilt before any deploy. **So the only build that carries #7's fix to users also publishes "Apache-2.0 (Open Source)" on a repository that grants no rights.** A live defect's repair is now held behind this licence question. Raised as **Q-028** rather than routed around; no seat here may weigh a licence against a broken sign-in button. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-sign` product-manager seat (job `0071`, sixth product evaluation); this seat does not close this window, does not set a date, does not soften the default, and did **not** add, remove or edit a `LICENSE` file or the claim** | **This entry's central premise has expired, and it expired by an event rather than by an answer.** This entry rests on *"**Not yet public:** the page is on `main` and undeployed (the live bundle contains no `landing` route, verified 2026-08-31), which is the whole reason this is a question and not an incident."* **`sign.pumasi.ai` was deployed at 2026-09-01 01:02:16 UTC and `LandingView.vue` is now what a stranger meets at `/`.** **(1) The deployment, measured rather than inferred.** The served bundle moved `/assets/index-j38Qwibz.js` → **`/assets/index-CnoFAC2c.js`** (re-`curl`ed by this seat at 01:57 UTC), and its route table's first entry registers path `/` under the name `landing`, loading `./LandingView-C5khdw3s.js`, with `meta.public` set. `npx wrangler deployments list` puts the live version at `96ff7004-45f0-439d-a4e0-68e35f739462`, created **2026-09-01T01:02:16.132Z**, `Source: Unknown (deployment)`. **(2) The three claims, extracted from the chunk a browser downloads, at 01:57 UTC.** The hero strip: *"Zero Per-Seat Fees • Unmetered Envelopes • 100% Apache-2.0"*. The stage banner: *"Pumasi Sign is in active Alpha — Unmetered PDF stamping & SHA-256 audit certificates under Apache-2.0."* And the comparison table's row `License & Source Code` → **`Apache-2.0 (Open Source)`** against **`Proprietary Closed Source`** for both named competitors. **A correction to how this has been reported, which makes it worse rather than better:** job `0064`'s digest entry says the chunk *"contains the string `Apache-2.0 (Open Source)` three times"*. That string occurs **once**. `Apache-2.0` occurs three times, in **three independent assertions**. They are exactly the list frozen case **A-005** (`frontend/src/landing-claims.spec.ts:198`) pins byte-identical to `10a523d`, so nothing drifted — the strings simply left the repository. **(3) The other half is unchanged, re-measured at `2471a29`:** `ls LICENSE*` → *No such file or directory*; `gh repo view pumasi-ai/pumasi-sign --json licenseInfo,visibility` → `{"licenseInfo":null,"visibility":"PUBLIC"}`. **(4) This seat's reading, stated because the packet that ordered this evaluation asked for it in terms: yes, this is a live public misstatement.** Not an unbacked adjective and not a stale roadmap line — a **licence claim**, in the third column of a table whose other two columns read *"Proprietary Closed Source"*. The comparison is the point of the row: it invites a reader to prefer this product **because** its terms differ from the incumbents', and the terms it names do not exist. A reader who forks the repository on the strength of that row has no grant to do so. **(5) What this seat did not do, and why each was available and refused.** It did **not** add `LICENSE` — that is this entry's named default and an outward grant a third party may rely on, which CHARTER Part 0's reversibility rule does not release. It did **not** strike the three strings — that is this entry's named alternative, and taking it would answer a steward question by deleting the evidence for it, would retire A-005 by side effect, and would require `roadmap/VALUE.md` §1 and C5 and `roadmap/MARKET.md` §2–3 to move with it. It did **not** edit `frontend/`. `roadmap/VALUE.md` C5 **stays suspended**: a page is not a register, and that file does not adopt a claim because a deployment made it. **(6) What changed about this entry's shape.** Its `Blocks` row and `What retires this entry` row are both written for a world in which the page is unshipped — *"`BACKLOG.md` item 1(d), the deploy that closes issue #8"*. **#8 closed at 2026-09-01 00:59:11 UTC and the deploy has run.** `pumasi-sign` `roadmap/BACKLOG.md` **B1** is accordingly reduced to its **(b)** half alone and stays in the Blocked section, which is the one place a register can say *this got smaller and worse at the same time*. Nothing here proposes a licence, a deployer or a date. |
| **Status** | open. Pre-`launched` (CHARTER Part 0) would ordinarily release a reversible file edit from the window, and adding a licence file *is* reversible — but publishing a licence is an outward grant to third parties who may then rely on it, which is not the kind of reversibility that rule is about. Recorded here rather than taken. Meanwhile the claim stays undeployed. |

### Q-022 · Signing in must not require a Google calendar — intent statement, veto window
| | |
|---|---|
| **What** | [`pumasi-booking` `service/spec/0007/INTENT.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/service/spec/0007/INTENT.md), published 2026-08-31. `roadmap/BACKLOG.md` item 2, ranked by the product-manager evaluation at `1a01b1c`. Two authentication entry points refuse before reading their own configuration because they gate on `deps.calendars`, which `server.ts:114` and `worker.ts:244` build only when `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET` **and** `TOKEN_KEY` are all set. **(a)** `/auth/microsoft/start` (`app.ts:997`) answers *"Microsoft sign-in is not configured."* while the login page offers the button on `msClientId` alone (`app.ts:947`) — the button and its own answer disagree. **(b)** `/login/sso/<orgId>` (`app.ts:911`) answers *"SSO is not configured on this deployment."* before it reads the `org_sso` row, **on both paths**: the Workers router does not handle the route, it forwards into the Durable Object (`worker.ts:805`) that runs the same `handle()`. So per-org OIDC SSO — the enterprise identity [`VALUE.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/roadmap/VALUE.md) C3 lists in the free tier — needs Google Calendar credentials on every deployment shape. |
| **Window closes** | 7 days from publication of the release note, per CHARTER §2.1. *Steward to set the date — agents may not set a deadline; this records the charter's own duration, not a chosen one.* |
| **Default on silence** | Proceed with the intent statement's own assumptions. Both entry points obtain their state facility by the expression spec/0006 established and a cross-family review already passed — `deps.calendars?.state ?? oauthState(config)` — read from the one existing helper and not restated at either call site (L-007). The hub's own sealer is preferred where a hub exists, because a hub may hold a key that never came from `config.tokenKey` and a state must open under the key that sealed it. Each door keeps its own credential check: Microsoft still needs `msClientId`, org SSO still needs a matching `org_sso` row, and a deployment with no `TOKEN_KEY` still refuses both — **a change that makes SSO reachable by making it unguarded would be a worse defect than the one being closed**, and three of the six frozen acceptance cases exist to say so. Google sign-in, calendar connect and the calendar callback keep exactly the reachability they have today, asserted rather than assumed. Both refusal messages stay word-for-word as they are; naming the missing `TOKEN_KEY` would serve the operator better but is user-visible copy on two paths at once, and is recorded in the spec's §5 rather than folded in. |
| **What is deliberately not touched** | **`worker.ts:614`, the Workers router's own `/auth/microsoft/start`.** It was never Google-gated: at `efce7a4` it read `if (!hub || …)` over a *provider-less* `CalendarHub` — i.e. gated on `TOKEN_KEY` — and since `4f6ddf0` it reads `if (!states || !config.msClientId)`. spec/0006 §5's claim that it "has the same shape" was shape-true and effect-false. Clause S4d and acceptance case A-006 exist so that a run cannot edit the half that was already right and call the item delivered. |
| **Scope** | Correctness of an already-shipped surface. **No** new provider, **no** new developer account or app registration, **no** enlarged OAuth scope — so it does not act ahead of **Q-007**. |
| **Who this costs, and who it does not** | Not `booking.pumasi.ai`, which has Google Calendar configured and where neither door is shut. It costs the self-hoster [`VALUE.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/roadmap/VALUE.md) §1 courts by name, and it is a live counter-example to that file's C5 claim that no host is load-bearing. |
| **Status** | open — pre-`launched` (CHARTER Part 0), so work proceeds on this default at once and a veto reverts. |

### Q-023 · Pumasi Booking sign-in reachability — can-hurt release, 7-day window
| | |
|---|---|
| **What** | [`releases/2026-08-31-pumasi-booking-signin-without-a-calendar.md`](releases/2026-08-31-pumasi-booking-signin-without-a-calendar.md), published 2026-08-31. `pumasi-booking` `roadmap/BACKLOG.md` item 2, `service/spec/0007`: `/auth/microsoft/start` and `/login/sso/<orgId>` gate on being able to seal an OAuth state rather than on the presence of a Google calendar hub, so a deployment with Microsoft credentials — or with an organisation's own OIDC provider — and no Google Calendar can sign a person in. The org SSO half was broken on **both** the Node and the Workers path, the latter because the router forwards the route into the Durable Object that runs the same `handle()`. Classed can-hurt by `RISK_ZONES.yaml` (service paths): it is a reachability change on authentication surface. |
| **Merged at** | `pumasi-booking` `6b597dd`, `GATE: PASS`. Intent window is **Q-022**. |
| **Window closes** | 7 days from publication, per CHARTER §2.1. *Steward to set the date — agents may not set a deadline; this records the charter's own duration, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. It is a net *narrowing* of what the doors accept, not a widening: each keeps its own credential check, a deployment with no `TOKEN_KEY` still refuses both, and no unsigned state is emitted anywhere. What was removed is a check on an unrelated third party's calendar credentials. No new provider, account or permission. A veto reverts the merge. |
| **The risk that was actually guarded against** | Closing this the lazy way — making the doors reachable by making them *unguarded* — would have been a worse defect than the one being closed, since an unguarded org sign-on is a route into somebody's account. Three of the six frozen acceptance cases exist for that, and two deliberate breakages were run to prove they catch it: removing the Microsoft credential check, and removing org SSO's deployment gate, each turn a case red. |
| **Reviews** | Gemini approved the spec (`reviews/20260831-110333-spec-gemini.md`), the amendment to it (`reviews/20260831-111531-spec-gemini.md`) and the code (`reviews/20260831-111705-code-gemini.md`). **The first code review OBJECTED** (`reviews/20260831-111314-code-gemini.md`, citing CHARTER Part 3 req 2: a helper inside the frozen runner was fixed during implementation instead of amended in the open). The objection was correct and was not argued past — the fix was recorded in the spec, took a fresh cross-family spec review, and only then was the code review re-taken. Grok unreachable both times (HTTP 402); D-104 condition live, pre-`launched` review is advisory (Part 0) and P5's single non-builder bar is met. |
| **Relation to Q-007** | None claimed. Correctness of an already-shipped surface: no provider added, no developer account or app registration created, no OAuth scope enlarged. |
| **Relation to Q-012** | **Not deployed, and not deployable by this run.** Q-012 is open and explicitly outside Part 0's proceed-on-default rule, so this seat did not deploy and did not take `BACKLOG.md` item 1. This is now the **fourth** reviewed, gate-passed build waiting on that answer. As with Q-015, the undeployed state costs little *here*: the defect cannot occur on `booking.pumasi.ai`, which has Google Calendar configured. Those affected self-host from the repository. |
| **Status** | open — pre-`launched` (CHARTER Part 0): the window does not hold the (reversible) release; a veto reverts. |

### Q-024 · A Stage 1 gate that says "passes 100%" against a suite that fails 7.5% of the time — does it stay MET?

| | |
|---|---|
| **What** | Raised by the `pumasi-tunnel` post-release evaluation of 2026-08-31 at `83fd9f7` (ops `DIGEST.md`), from a re-measurement rather than a theory. `STAGE_PLAYBOOK.md` Stage 1's exit gate is *"pure-core test suite passes 100%"*, and `pumasi-tunnel` `roadmap/STAGE.md` has recorded it **MET 2026-08-31** since `e29dc0e`. That reading rested on 12 runs. Re-measured here at 40 runs of each invocation: `go test -count=1 -cover ./...` fails **3 in 40**, and `go test -count=1 ./...` — the plain command, no instrumentation — also fails **3 in 40**. The earlier record blamed `-cover`; it is not `-cover`. It is a **7.5% failure rate on `main`'s ordinary test command**, and it now surfaces on three different tests (`TestRawTCPCrossesTheTunnel`, `TestTCPPortReleasedWhenAgentDisconnects`, `TestConcurrentTCPClients`), every one the same refused dial against the same public TCP port. The defect is in the product, not the tests: `relay/relay.go` writes the auth response carrying `TCPAddr` at line 175 and calls `bindTCP` at line 194; `relay/sshingress.go:182` has the same shape. |
| **Why this is the steward's and not this seat's** | Because of what the answer chains to, not because the measurement is doubtful. `STAGE_PLAYBOOK.md`'s trigger matrix **Event 3** makes "Product Manager confirms Stage Exit Gate is `MET`" automatically enqueue a **marketing** packet to *"publish Stage Promotion announcement & update public stage badges."* So this seat's reading of one word — `MET` — is what puts a public claim on `pumasi.ai` and `pumasi.link`. That is a statement about what the product promises, which is this role file's own escalation ground. The role may propose the reading; it should not be the only thing standing between a flaky suite and a launch badge. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **The gate stays `MET`, with the number written down beside it, and this is what `roadmap/STAGE.md` §3.1 already does.** The gate asks that the pure-core suite pass; it does pass, reproducibly, and a race is not a failing test — it is a defect that a passing run does not exhibit. The honest form of "100%" is therefore recorded in the file as **"passes, 37 times in 40"**, with the failing output quoted and the fixer named. **Two riders.** **(a)** No stage *promotion* announcement is published off this gate while the rate is non-zero — Event 3 is held, not fired, and the marketing note in `STAGE.md` §7 says so. `pumasi-tunnel` is `alpha` and is not asking to move, so this costs nothing today. **(b)** The rate is re-measured at the same 40 runs at each evaluation and the figure kept current in `STAGE.md`; a gate whose number is inherited rather than measured is the thing that produced the wrong 12-run reading in the first place. |
| **Named alternative, if the default is wrong** | **Read the gate strictly: a suite that is not deterministic has not "passed 100%", and Stage 1 reverts to IN PROGRESS until `BACKLOG.md` item 2 lands.** The cost is explicit and should be stated rather than discovered: `roadmap/STAGE.md`, the `pumasi-web` catalog page that quotes its gate table, and any commons index entry would all have to be walked back from a status they have carried since 2026-08-31 — and the product would be recorded as *less* mature on the day a release improved its coverage (`relay` 55.9% → 74.7%). It is a defensible standard. It is just not free, and the bill lands on a seat that did not choose it. |
| **What retires this entry** | `BACKLOG.md` item 2 merged — bind before the response leaves, on both the agent and the ssh paths — and 40 clean runs of each invocation recorded in `roadmap/STAGE.md`. At that point the gate's "100%" needs no qualification and this stops being a question. |
| **Scope** | This asks only how the word `MET` is read against a flaky suite, and whether Event 3 may fire off it. It does **not** ask whether the race should be fixed — that is ordinary coder work, already the top buildable backlog entry. It touches no other product, though the reading would presumably bind them. |
| **Evidence added 2026-08-31, post-`0041` (`pumasi-tunnel` `1d9505c`) — recorded by the product-manager seat, which does not close this window** | The fix this entry names as its retirement condition is **merged and independently verified**, and the retirement condition is nevertheless **not met**. **(1) The race is gone.** Measured by this seat, not taken from the build report: the unmodified `relay` and `agent` packages driven on a TCP range outside the kernel ephemeral range (14000–14099, confirmed wholly bindable), dialling the announced `TCPAddr` the instant it arrived — **28 dial refusals in 2000 iterations at `83fd9f7`, 0 in 2000 at `1d9505c`** (also 3/200 → 0/200 and 5/500 → 0/500). **(2) The '40 clean runs' this entry asks for do not exist, and the reason is not the product.** `go test -count=1 ./...`, 40 runs at each SHA on the evaluating machine: **40 failures in 40 at `83fd9f7`** and **40 in 40 at `1d9505c`**. `relay/tcp_test.go:47–48` pins the relay's port pool to 34000–34099, which lies inside that host's `/proc/sys/net/ipv4/ip_local_port_range` (32768–60999); an unrelated `workerd` process held 127.0.0.1:34000 as an outbound ephemeral source port for the whole evaluation, and `core.PortPool` starts its cursor at `low` (`core/portpool.go:43`), so every fresh relay drew 34000 and every TCP test failed. The failing string previously published in this entry as evidence of the race — `dial tcp 127.0.0.1:34000: connect: connection refused` — is reproduced at `83fd9f7` by that collision alone. **(3) What that means for the question actually asked.** The 3-in-40 figure this entry rests on cannot be attributed to the race with confidence; the two causes produce the same string. The honest reading of `MET` is now weaker than when this entry was raised, not stronger: the pure-core suite passes on a host where nothing else is using 34000–34099, and no one can presently say how often that holds. `pumasi-tunnel` `roadmap/STAGE.md` §2 records this in full; the gate is left `MET` and qualified under this entry's own named default, and Event 3 stays held. **(4) A new backlog entry, not a patch.** The port block is `pumasi-tunnel` `BACKLOG.md` item 2 and is now the highest *build* entry — two constants, and until it lands the gate is unmeasurable. Note that `0041` identified this hazard and fixed only the cases it wrote (`relay/bindorder_test.go:39–43`, `bindOrderBase = 20500`, *"Deliberately below `/proc/sys/net/ipv4/ip_local_port_range`'s floor"*), routing around the 34000-series harness rather than moving it. |
| **Evidence added 2026-08-31, post-`0047` (`pumasi-tunnel` `b3d251d`) — recorded by the product-manager seat (job `0059`), which does not close this window, did not set a date, and did not soften a default** | **The half of the retirement condition that could not be measured, now can be — and it is clean.** **(1) The runs exist.** `relay/tcp_test.go` no longer draws from inside the kernel's ephemeral range: `tcpHarnessBase = 21000` with a block of ten per harness (21000–21039), following `bindorder_test.go`'s existing scheme rather than a second one. Measured by this seat at `b3d251d` on the same machine as the previous pass: `go test -count=1 ./...` **0 failures in 500 runs**; `go test -count=1 -cover ./...` **0 in 100**; `tools/gate.sh`, the whole gate with `SKIP_FAMILY_PROBE=1`, **40 runs, 40 × `GATE: PASS`**. 640 full-suite executions, 0 failures, against **40 failures in 40** at `1d9505c`. **(2) The host did not get quieter — the code got correct.** `/proc/sys/net/ipv4/ip_local_port_range` is still `32768 60999`; `workerd` still held `127.0.0.1:34000` throughout; `ss -tanp` found **12** sockets inside 34500–34599. It found **0** inside 21000–21039 and **0** inside 20500–20559, which are the blocks the suite now uses. Coverage is measurable again and `relay` rose from an inherited 74.7% to **82.0%** — not from new tests, but from four existing ones running instead of aborting (`core` 80.3%, `mux` 83.5%; `agent` and both `cmd` packages remain 0.0%). **(3) This nevertheless does not retire this entry, and the reason is substantive rather than procedural.** The suite's non-determinism has **moved, not gone**. `relay.ServeAgent` registers the tunnel (`relay/relay.go:295`), binds the public TCP port (`:175` — the fix this entry names, correctly placed), writes the auth response that hands the agent its URL (`:205`), and only **then** installs `r.sessions[resp.AgentID]` (`:213`–`:216`); a visitor arriving in between passes `registry.Lookup` (`:331`), finds `session == nil`, and is answered `404 No tunnel is open for <host>` (`:340`–`:344`). That is the rule `spec/0002` §1 exists to enforce, one path over from where `0041` enforced it, and it is **not** the defect this entry names. Job `0047` observed it twice in 240 runs of the merging tree (`TestConcurrentVisitors`, plus once as `TestLargeResponseCrossesIntact`). **This seat did not reproduce it** — the 640 executions above, plus a targeted out-of-tree probe dialling at the earliest instant `OnConnect` can hand over the URL: **4,900 visitor requests, 0 answered `404`** (200 iterations × 20 concurrent visitors; 500 single-visitor iterations; 400 more with the suite looping alongside as load, the condition `0047` said the flake needs). So the defect is established by code reading and by one other job's measurement, and not by this one's; it is `pumasi-tunnel` `BACKLOG.md` **item 2**, the highest build entry, and a three-line reordering. **(4) What this seat did and did not do with that.** `pumasi-tunnel` `roadmap/STAGE.md` §2 records all of the above with the run count beside every figure. The gate is left **`MET` and qualified** under this entry's own named default — the qualification changing from *"the number cannot be measured"* to *"500 of 500, with one known unfixed ordering defect on a different path"*. **Rider (a) is applied and `STAGE_PLAYBOOK.md` Event 3 stays held**: no promotion announcement, no public quotation of the figure. **Rider (c) is why the stage did not move** — the product did not get better this pass, the evidence did, and the file says so. The window is not closed, not dated, not extended and not softened; that is the steward's. |
| **Evidence added 2026-09-01, post-`0060`/`0066` (`pumasi-tunnel` `1853218`) — recorded by the product-manager seat (job `0076`), which does not close this window, did not set a date, and did not soften a default** | **Both halves of this entry's own stated retirement condition are now complete, and the reason the previous pass gave for not retiring it anyway is also gone. It is still not this seat's to close.** **(1) The successor defect is merged.** The `b3d251d` row above declined to retire this entry because the suite's non-determinism had *moved* rather than gone — to `relay.ServeAgent` installing the mux session after the auth response was written. That is fixed at **`fd523e8`** (`spec/0003-session-before-announce`) and verified in the tree by this seat rather than read off a commit subject: the session is installed and the announce written inside one `r.mu` critical section (`relay/relay.go:222`–`:231`), the session is deleted again in that same section if the write fails, and `ServeHTTP` takes the same lock to read `r.sessions`, so a visitor arriving in the old window waits for the lock instead of being answered `404`. **No identified source of non-determinism in this suite is open.** **(2) The runs exist, taken by this seat at `87244af` on the same machine as the previous two passes.** `go test -count=1 ./...` **0 failures in 300**; `go test -count=1 -cover ./...` **0 in 100**; `go test -race -count=1 ./...` **0 in 25**. 425 full-suite executions, 0 failures, 02:44:39–03:05:24 UTC. `/proc/sys/net/ipv4/ip_local_port_range` still **32768 60999**, four `workerd` processes running, **9** sockets inside 34500–34599 and **no foreign listener** inside 21000–21039 or 20500–20559. Coverage: `core` 80.3%, `mux` 84.0%, `relay` **83.3%** (was 82.0% — from `fd523e8`'s own 468 new test lines), `agent` and both `cmd` packages **0.0%**. **(3) One figure this entry has carried is not reproducible, and the reason is not the suite.** `pumasi/tools/gate.sh` at a documents SHA prints `1/4 tests … tests: PASS` and then `GATE: FAIL` on `missing trailer: Spec:`, because a roadmap commit has no spec to name. The previous row's *"40 × `GATE: PASS`"* was taken at a code SHA. **Step 1 is what the Stage 1 gate names and step 1 passes**; no whole-gate figure is claimed at this SHA. **(4) What the evidence for the fix actually is, stated because it is not a rate.** Neither this seat nor the `b3d251d` seat ever reproduced the HTTP-path 404 — job `0047`'s two failures in 240 remain the only observation, against 640 executions plus 4,900 targeted visitor requests last pass and 425 executions this pass. What establishes the fix is a **frozen case that fails for the right reason**: C-1 `TestVisitorIsNotAnsweredBeforeTheSessionExists` holds the relay inside the announce with a `gatedConn` and went red at `8540b89` in 0.00 s with the exact `404 No tunnel is open` string. That is better evidence than an incidence figure and a different kind of evidence, which is worth separating rather than folding into a run count. **(5) A caution this entry earned the hard way, from its own words.** Every row above, and `pumasi-tunnel`'s own backlog, described the outstanding fix as *"a three-line reordering"*. **It was not, and the reordering as written drops the tunnel.** The announce is written raw (`relay/relay.go:327`) while `mux.Session.Open` writes a `FrameOpen` on the same connection under the session's own `writeMu`; a bare reorder puts a stream frame ahead of the auth response, `DecodeAuthResponse` rejects it, and the agent reconnects — C-1 answering `502` where it used to answer `404` honestly, C-2 waiting **10.76 s** for an `OnConnect` that never fires (`pumasi-tunnel` `spec/0003/SPEC.md` §2, §6). **It would have passed 500 clean suite runs.** A window whose evidence rows repeat an untested size estimate is carrying a claim, not a measurement. **(6) What now qualifies `MET`, since it is no longer a named defect.** Three things, none of them a bug in that repository: every figure this gate has ever had was taken on **one machine by the seat that wrote the file**, with nothing re-running it — which is **Q-025**'s question arriving through this entry's door, and which rider (c) already says is to be written down as weaker than CI rather than as absent; the closed defect was never reproduced here; and one latent fixture defect is still inside a frozen file (`relay/scheme_test.go:314`–`:315`, **Q-030**, `pumasi-tunnel` `BACKLOG.md` item 8 after this pass's re-rank). **(7) What this seat did and did not do.** `pumasi-tunnel` `roadmap/STAGE.md` §2 records all of the above with the run count beside every figure. The gate is left **`MET` and qualified** under this entry's own named default. **Rider (a) is applied and `STAGE_PLAYBOOK.md` Event 3 stays held**: no promotion announcement, no public quotation of any figure, and §7 of that file says so to the marketing seat in as many words. The window is **not closed, not dated, not extended and not softened**; that is the steward's. |
| **Evidence added 2026-09-01, post-`0081` (`pumasi-tunnel` `9e2de66`) — recorded by the `pumasi-tunnel` product-manager seat (job `0084`), which does not close this window, did not set a date and did not soften a default** | **Nothing here answers the question. Both halves of the retirement condition stay complete at a SHA eight Go-tree merges further on, and this pass found the first thing that cuts *against* reading a clean number as strength.** **(1) The runs, re-taken rather than carried.** At `9e2de66`: `go test -count=1 ./...` **300 runs, 0 failures**; `-cover` **100, 0**; `-race` **25, 0**; and `pumasi/tools/gate.sh` **3 of 3 `GATE: PASS`** with breadth skipped and therefore unverified, which the gate prints itself. 05:49:49–06:10:19 UTC. **These are the first figures this product's `STAGE.md` has published from a clean tree** — every previous measurement was taken over an unowned uncommitted change to `cmd/pumasi-tunnel/main.go`, which job `0081` has now stashed. The host was busier, not quieter: `ip_local_port_range` still **32768 60999**, four `workerd`, and **19** sockets inside 34500–34599 against 9 last pass. **(2) The finding, and it is rider (c)'s own point measured rather than asserted.** `spec/0004` slice 1 merged with **the suite green and a cross-family code review approving the diff** — and **three defects were found afterwards**, the third *introduced by the fix for the second* (`spec/0004` §11, §12, §13). Each was a clause whose truth no execution could reach. What established each of them was a **mutation** — reinstate the defect, run only the case that names it, confirm it reddens — and `spec/0004` §10's table is now twelve mutations covering every built case, with exactly one case reddening under the over-broad rollback guard. **So a run count says a change broke nothing; it does not say the code is right**, and this entry's question is about how much a run count is worth. **(3) A new bound on what the gate covers, and it is not a defect.** The property that `pumasi-tunnel` `BACKLOG.md` item 2 exists to deliver has **no test in that repository at all**: `relay.TestAReservationOutlivesTheRelay` is specified in `spec/0004/acceptance/CASES.md` and absent from the Go tree. **425 green runs are therefore not evidence about the relay-restart half in either direction** — and an absent case is not a red one, which corrects one sentence in `pumasi-ops/DIGEST.md` describing it as *"left red"*. `roadmap/STAGE.md` §3.3. **(4) Rider (a) was honoured.** `STAGE_PLAYBOOK.md` Event 3 stays held, no stage-promotion announcement was published, no run count or gate figure was put on any public surface, and the stage stays `Alpha`. |
| **Status** | open. Nothing is blocked on it: the fix is queued as an ordinary packet and no promotion is pending. Recorded now rather than after a badge is published on a number nobody re-measured. |

### Q-025 · Nothing re-runs the merge gate — is `GATE: PASS` an agent's own report, and is that enough for a `beta` claim?

| | |
|---|---|
| **What** | Raised by the `pumasi-booking` post-release evaluation of 2026-08-31 (job `0030`, ops `DIGEST.md`), from a post-release read rather than a failure — which is why nothing has ever surfaced it. **`pumasi-booking` has no CI at all.** `.github/` contains only `feedback-attachments`, there is no `.github/workflows/` directory, and `gh run list` returns empty. `tools/gate.sh` is not in the product repository either — it lives in the commons (`pumasi/tools/gate.sh`) and is run by hand, from a checkout, by the agent that wants to pass it. So every quality claim this commons makes — `GATE: PASS` in four `pumasi-booking` release notes, the test counts in `roadmap/STAGE.md`, the frozen acceptance suites CHARTER Part 3 requires — is a report by the agent that wrote the change, of a script it chose to run on its own machine, with no artefact anyone else can re-open. The repository is **public**, so Actions minutes are free: this is not a spend and not a `HUMAN.md` item. It is work nobody has been asked to do. |
| **Why this is the steward's and not this seat's** | The *product* half is not — advisory CI on this repository is ordinary work and is already ranked as `pumasi-booking` `roadmap/BACKLOG.md` **item 2**, no question needed. What is escalated is the half that follows it. CHARTER §3's merge gate is written as a script an agent runs; making the machine's result the merge *condition* changes the flow for every product and every role, and turning `GATE: PASS` from a self-report into a checkable artefact changes what `roadmap/STAGE.md` files across the commons are asserting when they cite it. `roles/product-manager.md` may propose; it may not amend the charter's gate, and it should not be the only thing standing between a self-reported number and a `beta` label that says *strangers can rely on it*. |
| **A sibling product already has CI, which is why the *product* half is plainly ordinary** | `pumasi-sign` runs a `backend` job (545 tests) and an `e2e` Playwright job. Two things follow. **(a)** Adding a workflow to a Pumasi repository is established practice, needs no new tool, account or spend, and `pumasi-sign`'s own CI gap — `service/`, the tree users actually meet, covered by nothing — is being closed as ordinary work under **Q-018**'s default part (b). Nothing here asks the steward to authorise that shape of work. **(b)** It also shows the failure this entry is about is not "CI is missing" but "what a gate result *means*": `pumasi-sign`'s 545 green tests were read as covering production for as long as nobody checked which tree was deployed (Q-018, L-006 at suite scale). A machine that runs the wrong suite is not better than an agent that ran the right one; what is missing on `pumasi-booking` is that anyone other than the author ever ran anything. |
| **The concrete cost, already paid once** | `pumasi-tunnel`'s Stage 1 exit gate was recorded `MET` off **12** local runs; a re-measurement at 40 found the suite failing **7.5%** of the time on `main`'s ordinary test command (**Q-024**). Nobody was careless — the number was simply never re-taken by anything but a seat that happened to look. `pumasi-booking`'s suite had never been measured that way at all until this evaluation, which ran `npm test` **40 consecutive times** at `6b597dd` and got **40 of 40 green**. That is a good answer, and it is exactly the answer nobody had; it is a check, not a system, and the next one depends on the next seat choosing to ask. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **The charter stays exactly as written, and the fix is ordinary product work.** `GATE: PASS` remains what CHARTER §3 says it is: an agent ran the gate and signed the record. Each product repository gains an **advisory** workflow — runs on push and pull request, does what step 1 of the gate does (`npm test` across the workspaces) plus `npm run typecheck`, publishes the result, blocks nothing — ranked in that product's own `BACKLOG.md` by its product manager, built by an ordinary coder packet. **Three riders.** **(a)** No `roadmap/STAGE.md` cites `GATE: PASS` as evidence for a stage without naming who re-ran it and when; `pumasi-booking`'s already does this and the others should. **(b)** Where a suite's determinism has never been measured, the evaluation measures it rather than inheriting a single green run — the Q-024 lesson, generalised. **(c)** No product is demoted for lacking CI today: the evidence in these files is re-derived at each evaluation by a seat that did not write the code, which is weaker than CI and is to be written down as weaker, not treated as absent. |
| **Named alternative, if the default is wrong** | **Amend CHARTER §3 so the gate is machine-run and its result is the merge condition** — `GATE: PASS` means a workflow passed and links the run, not that an agent says it did. The cost is explicit and should be stated rather than discovered: four product repositories need the config and the secrets to run their suites (`pumasi-booking`'s needs a PostgreSQL service for the exclusivity proofs), every role that merges gains a dependency on a third party being up, and the D-104 pattern — a required check that cannot be met and is therefore switched off — becomes available to a second part of the flow. It is the stronger standard. It is not free, and the bill lands on seats that did not choose it. |
| **What retires this entry** | A workflow present and green on `pumasi-booking` (`BACKLOG.md` item 2 merged), and either CHARTER §3 amended to say what `GATE: PASS` now means, or this entry closed recording that it deliberately still means what it always meant. |
| **Scope** | Asks how `GATE: PASS` is evidenced and whether a self-reported suite supports a `beta` claim. It does **not** ask whether `pumasi-booking` should have CI — that is ranked as ordinary work either way — and it touches no product code. Adjacent to **Q-024** from the other side: that entry asks what a *flaky* suite does to a gate reading; this one asks what an *unwitnessed* one does. |
| **Status** | open. Nothing is blocked on it: item 2 is queued as an ordinary packet under the default, and no stage promotion is pending on `pumasi-booking` — this evaluation read no `STAGE_PLAYBOOK.md` exit gate as `MET`, so **Event 3** does not fire from it. Recorded now rather than after a badge is published on a number nobody but its author ever ran. |

### Q-026 · Advisory CI is live on `pumasi-booking` — the 7-day window on its release note

| | |
|---|---|
| **What** | The `roadmap/BACKLOG.md` item 2 work is merged at **`d5a02bb`** and released: [`releases/2026-08-31-pumasi-booking-advisory-ci.md`](releases/2026-08-31-pumasi-booking-advisory-ci.md). This entry is that note's veto window, and nothing more. **It asks the steward for nothing** — no account, no card, no key, no branch-protection switch. The repository is public, so the Actions minutes are free; this is not a spend and raises no `HUMAN.md` item. |
| **What shipped** | A workflow on `push` and `pull_request` that runs the core suite unmodified, the service suite from the product's own glob minus exactly one named file, `npm run typecheck` across every workspace, and a credential-free `wrangler deploy --dry-run` of the entry point serving `booking.pumasi.ai`. Plus the two script lines that make the type-check mean what it says: `service/package.json` gains the `typecheck` it never had, and the root command drops `--if-present`, which had been skipping that workspace in silence and exiting 0. First run: [33428541886](https://github.com/pumasi-ai/pumasi-booking/actions/runs/33428541886), success, 19 core + 316 service, 0 failures. |
| **Risk class** | **can-hurt**, settled in `spec/0008/SPEC.md` §4 on the register's own rule rather than on how harmless the change feels: `service/spec/0002/RISK_ZONES.yaml` defaults unmapped paths to `can_hurt` and makes reclassification to `ordinary` itself a can-hurt act, so a spec may not talk itself down. The substantive hazard is real and named: a workflow file in a **public** repository is a privilege surface. The answer is `permissions: contents: read`, no secrets, and `pull_request` rather than `pull_request_target` — and acceptance case A-004 checks that rather than promising it. |
| **Window closes** | **2026-09-07.** A veto **reverts**; under CHARTER Part 0 the work proceeds now, `roadmap/STAGE.md` being `beta`. |
| **Default on silence** | The advisory workflow stays as it is. |
| **Relation to Q-025** | This is Q-025's *product* half, built on its default on silence, and it retires the first of that entry's two retirement conditions: a workflow is present and green on `pumasi-booking`. **The second is untouched and deliberately so** — CHARTER §3 is not amended, `GATE: PASS` still means an agent ran the gate and signed the record, and no required status check exists to make it mean anything else. Q-025 stays open on that half. **One correction to Q-025's own text, from measurement:** its named alternative prices a machine-run gate as needing "a PostgreSQL service" for this repository. It does not. The 19 service test files that need PostgreSQL bring their own — `@embedded-postgres/linux-x64` is an ordinary optional npm dependency, and `npm ci` is the whole setup. The alternative is cheaper here than it was priced, which is worth knowing before it is weighed against the default. |
| **Relation to Q-012** | **Not deployed, and not deployable by this run.** Q-012 is open and explicitly outside Part 0's proceed-on-default rule, so this seat did not deploy and did not take `BACKLOG.md` item 1. This is the **fifth** reviewed, gate-passed build waiting on that answer. Nothing in this change reaches a user either way: it ships no byte to `booking.pumasi.ai`. |
| **Breadth** | Gemini reviewed and approved the spec twice (once after an amendment made in the open before implementation) and the code once, running the suite itself rather than reading this run's numbers. **Grok returned HTTP 402 "Grok Build usage balance exhausted" on all three invocations** — a *billing* failure, not an availability one, and D-104's degraded-breadth condition for the fourth consecutive job on this product. Breadth actually achieved: one family. Pre-`launched`, review is advisory (Part 0) and P5's single non-builder bar is met. |
| **What retires this entry** | The window closing on 2026-09-07 without a veto. |
| **Status** | open, window running. Nothing is blocked on it: the change is merged, the workflow is advisory, and a veto reverts a file. |


### Q-027 · Pumasi Sign's sign-out page sends the user somewhere that exists — can-hurt release, 7-day window

| | |
|---|---|
| **What** | [`releases/2026-08-31-pumasi-sign-sign-in-again.md`](releases/2026-08-31-pumasi-sign-sign-in-again.md), published 2026-08-31. `pumasi-sign` `roadmap/BACKLOG.md` items **1** and **2**, both of which say in their own text to take them in one packet; `spec/0003`; merged at **`d18d534`** (spec and frozen cases at `b6513c2`). Item 1 is issue [#7](https://github.com/pumasi-ai/pumasi-sign/issues/7): *Sign in again* navigated to `/api/auth/login`, a route defined only in `backend/`, and the worker that serves `sign.pumasi.ai` answered it `404 {"error":"Endpoint not found"}` as a page. The button now targets the SPA's own `/login`, through the `loginPageUrl` helper the app's `401` handler already used; `loginRedirectUrl` is deleted. Item 2: the root `npm test` — step 1 of `tools/gate.sh`, and with `main` unprotected the whole gate — ran 69 frontend assertions and **zero** against `service/`. It now installs, builds and runs that suite and hands its output to the guard `spec/0002` built, the same file CI calls. |
| **Classification** | Can-hurt, by CHARTER Part 4's own default: `pumasi-sign` has no `RISK_ZONES.yaml`, and an unmapped path defaults to can-hurt. The cross-family reviewer read the change as **ordinary** ("no elevated risk zone touched"). Both readings are recorded rather than one quietly chosen; the charter's default governs, and the path is the product's authentication entry, which is the direction to guess safely in. |
| **Window closes** | **2026-09-07** (7 days, CHARTER §2.1). *Steward to confirm — agents may not set a deadline; this is the charter's own duration applied, not a chosen one.* |
| **Default on silence** | The release stands as written in the note. It is a net removal of a dead end: a signed-out user gets a sign-in page instead of a JSON error, and the merge gate stops reporting a test count that excludes production. A veto reverts the merge, which restores a button that 404s. |
| **What it can cost, and who** | One behaviour changes for one class of user, and only on the tree **no user reaches**. On `backend/`, *Sign in again* used to jump straight into the Entra flow; it now lands on the SPA login page, one click from the same place (*Continue with Microsoft*) — the same page that tree's own `401` handler already sends every expired session to. On the worker, which is what users meet, the button went from a raw JSON error to a working page, so there is no one whose experience is worse. Second-order cost: the root `npm test` now takes ~15 s instead of ~2 s and requires network for `npm ci` in `service/`. No escape hatch was added for that, deliberately — a switch to turn the served tree's suite off is the D-104 pattern, and its absence is stated rather than left to be discovered. |
| **Evidence, and what is deliberately not offered as evidence** | **A green CI run is not offered and would not count.** All four jobs were green at `ef851d6` ([run 33420378497](https://github.com/pumasi-ai/pumasi-sign/actions/runs/33420378497)) *while this defect was live in production*, and the `e2e` job was green **because** of it — `frontend/playwright.config.ts` drives `backend/`, the one tree where the missing route exists. The fix was proven with a real Chromium clicking the real button against a local `wrangler dev` of `service/`, before and after: `{"error":"Endpoint not found"}` → *Sign in to Pumasi Sign*. Nine frozen acceptance cases were run against the change-absent tree **before** freezing and the spec's table records what came back, not what was intended (five red, four green with a stated reason each); every "single mutation turns it red" claim was executed. `GATE: PASS` at `d18d534` — and that gate's step 1 now prints `# pass 2 · # fail 0` from `service/`, which is the item this release also closes. |
| **Reviews** | Gemini approved the spec (`pumasi-sign/reviews/20260831-141457-spec-gemini.md`) and the code (`.../20260831-141721-code-gemini.md`). Grok unreachable — HTTP 402, *"Grok Build usage balance exhausted"*, probed this tick; **D-104 condition live**, so §3's rule that the spec reviewer must not be among the code reviewers cannot bind and is off rather than pretended. Pre-`launched`, review is advisory (Part 0) and P5's single non-builder bar is met. |
| **Relation to Q-012 and Q-018** | **Not deployed, and this run did not treat that as a judgement call.** Q-012 is open and explicitly outside Part 0's proceed-on-default rule; Q-018 adds that shipping this product means `wrangler deploy` from `service/`, not the Railway push `CLAUDE.md` describes. `sign.pumasi.ai` therefore still serves the broken button and will until someone deploys. Nothing in Q-018's actual question — which tree *is* Pumasi Sign — was touched: nothing was deleted, no domain re-pointed, no data moved, and the repair was chosen partly because it works identically on both trees. |
| **Evidence added 2026-08-31, post-`0037` — recorded by the `pumasi-sign` product-manager seat (job `0045`), which does not close this window, did not deploy, and did not set the date above** | Nothing here answers the question; it records what the window is running over. **(a) The merge is real and was verified in the tree, not read off the job's report:** `SignedOutView.vue:2`/`:6` now use `loginPageUrl`, `utils/http.ts:44` returns `/login?next=…`, and `loginRedirectUrl` has no definition and no caller anywhere in the tracked tree. The page it targets answers on the deployment — `GET https://sign.pumasi.ai/login?next=%2F` → **200 `text/html`**. **(b) The second half is real too, measured by running it:** root `npm test` at `d18d534` reports `Test Files 6 passed (6)`, `Tests 85 passed (85)`, `# pass 2`, `# fail 0`, `assert-service-suite-ran: 2 passing, 0 failing, from 2 compiled`; before `d18d534` the same command reported 5 files / 69 tests and zero service assertions. Determinism re-measured for the suite that exists *today*, because this release changed its shape: **40 consecutive runs, 40 pass, 0 fail**, identical counts on every run (Q-025 rider (b)). **(c) The note's own headline claim is confirmed — nothing shipped.** Measured 2026-08-31 20:10–20:12 UTC: the live bundle is still `/assets/index-j38Qwibz.js`, `GET /api/auth/login?next=%2F` still returns `404 {"error":"Endpoint not found"}`, and the shipped JavaScript **still contains the helper this release deleted** — the minified `loginRedirectUrl`, returning `/api/auth/login?next=`. Removed code, still serving users; a stronger proof than the unchanged filename. **(d) A second reviewer exists and is not evidence.** `pumasi-sign/reviews/20260831-143359-code-qwen.md` is an **untracked** cross-family review of the same range, dated 19:33:59Z — twelve minutes *after* this note was committed — driven by hand because `pumasi/tools/review.sh` has no case for that family. It contains **no findings**: 9 lines, against 72 in each gemini review. The `Reviews` row above is therefore not contradicted; the tooling gap is reported to the project manager. **(e) The cost of the window, stated plainly:** while it runs, the person who reported #7 still meets a page of JSON, and the repair cannot reach them — see **Q-028**, new. |
| **Status** | open — pre-`launched` (Part 0): the window does not hold the (reversible) merge; a veto reverts. |

### Q-028 · A live defect's repair can only reach users in the same build that publishes a licence the repository does not carry

| | |
|---|---|
| **What** | Raised by the `pumasi-sign` product evaluation of 2026-08-31 (job `0045`, ops `DIGEST.md`), from a measurement rather than a theory, and it is a consequence of two open entries meeting rather than a new complaint about either. Issue [#7](https://github.com/pumasi-ai/pumasi-sign/issues/7) — a signed-out user's *Sign in again* button returning `404 {"error":"Endpoint not found"}` as a rendered page — was fixed at `pumasi-sign` `d18d534` and announced in [`releases/2026-08-31-pumasi-sign-sign-in-again.md`](releases/2026-08-31-pumasi-sign-sign-in-again.md) (**Q-027**). It is a **frontend** fix. `service/wrangler.jsonc:8` serves `../frontend/dist` as one `ASSETS` directory; `frontend/src/router/routes.ts:15` registers `LandingView` at `/`; `frontend/dist` is `.gitignore`d and must be rebuilt for the fix to be in it (`CLAUDE.md`: *"a stale `dist` ships a stale SPA"*). **Therefore the only build that carries #7's repair to users also publishes `LandingView.vue`'s "Apache-2.0 (Open Source)" claim** (`:43`, `:80`, `:210`) from a repository that carries no `LICENSE` and grants no rights — which is **Q-021**, open, its named default unclaimed. Measured 2026-08-31 20:10–20:12 UTC: the live bundle is still `index-j38Qwibz.js`, it registers no `landing` route, and it still contains the very helper `d18d534` deleted. So the defect is live, the fix is merged, and the two questions are now entangled in one artefact. |
| **Why this is the steward's and not this seat's** | Every way out is reserved. Deploying is **Q-012**, open and explicitly outside CHARTER Part 0's proceed-on-default rule. Publishing the licence claim without a `LICENSE` is the outward grant Q-021 exists to decide. And the third way — changing `frontend/` to remove or gate the landing route so #7 can ship alone — is *itself* Q-021's named alternative ("remove the claim instead") taken by a coder at deploy time, which would answer a steward question by deleting the evidence for it. The product-manager role's strongest verb is *propose*; it may not weigh a licence question against a broken sign-in button. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Nothing is routed around: the two ship together, or not at all.** No `frontend/` change is made to strip, gate or feature-flag the landing route in order to release #7 by itself, and #7's deploy waits on Q-021 and Q-012 exactly as `pumasi-sign` `BACKLOG.md` **B1** does — it is recorded there as **B2**. This is simply what silence already produces; the entry exists to make its **cost** explicit rather than to change it. That cost, stated once: the user who reported #7 keeps meeting a page of JSON for as long as both entries stay open, and `Q-027`'s veto window runs to its close over a change no user can evaluate. |
| **Named alternative, if the default is wrong** | **Answer Q-021 on its own named default** — add `LICENSE`, Apache-2.0, byte-identical to `pumasi-web/LICENSE` — which makes the single build legitimate and reduces this entry to Q-012 alone. That is the smaller of the two real answers and it is Q-021's own preference; this entry does not re-argue it and takes no position on the licence. The other alternative, explicitly authorising an interim build with the landing route withheld, should be a coder packet with a spec and a frozen case, never a decision taken at deploy time — and it would still need Q-012 to name who runs it. |
| **What retires this entry** | Either #7's fix reaching `sign.pumasi.ai` (whereupon this is moot and the record is the deploy), or Q-021 resolving in either direction (whereupon the entanglement dissolves and only Q-012 remains). It also retires if a future change makes the landing page separable from the rest of the SPA — but nobody has proposed one and this entry does not ask for one. |
| **Relation to Q-012, Q-021 and Q-027** | It adds nothing to any of their questions and closes none of them. **Q-021** is where the licence is decided; its `Blocks` row names only issue #8's deploy, because #7's repair did not exist when it was written, and this entry is the correction to that scope. **Q-012** is who carries a build to users; this is a second, sharper instance of its cost on a second product. **Q-027** is the release note's veto window; this is what that window is running over. Evidence rows recording the same measurements have been added to all three, without closing any. |
| **Scope** | Asks only whether a live defect's repair may be separated from an unresolved licence claim that ships in the same bundle, and if not, that the cost be on the record. It touches no product code, proposes no deploy, sets no date, and takes no position on what the licence should be. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-sign` product-manager seat (job `0071`, sixth product evaluation); this seat does not close this window, does not set a date and takes no position on what the licence should be** | **This entry's forecast was confirmed by the event it was raised to prevent.** It predicted, in these words, that *"the only build that carries #7's repair to users also publishes `LandingView.vue`'s 'Apache-2.0 (Open Source)' claim"*, and that the two would ship together or not at all. **They shipped together, in one `frontend/dist`, at 2026-09-01 01:02:16 UTC, and neither Q-021 nor Q-012 was answered first.** **(1) Both halves are live, verified on the served artefact rather than on the tracker.** #7: `grep -c '/api/auth/login?next=' index-CnoFAC2c.js` returns **0** — the helper `d18d534` deleted is gone from the shipped JavaScript, where the fifth evaluation found it still present — the served `SignedOutView` chunk targets `/login?next=%2F`, and that page answers `200 text/html`. Issue [#7](https://github.com/pumasi-ai/pumasi-sign/issues/7) closed 00:59:12 UTC, [#8](https://github.com/pumasi-ai/pumasi-sign/issues/8) at 00:59:11 UTC. The licence claim: three distinct assertions in the served landing chunk, quoted in full on **Q-021**. **(2) The mechanism this entry described was exactly right and is worth preserving as a worked example.** `service/wrangler.jsonc` serves `../frontend/dist` as one `ASSETS` directory, `frontend/dist` is `.gitignore`d and is rebuilt before any deploy, and `routes.ts` puts `LandingView` at `/`. There was never a build that separated them, no seat proposed one, and none was made. **(3) So this entry's stated *default* held and its stated *cost* was paid in full.** The default was *"nothing is routed around: the two ship together, or not at all"*, and its cost was named as the reporter of #7 continuing to meet a page of JSON. That cost has stopped — and the price of stopping it is that an untrue licence grant is now public, which is the outcome this entry existed to make visible in advance. **It was visible in advance. It was recorded in advance. It happened anyway.** **(4) What retires this entry, on its own terms.** Its `What retires this entry` row reads *"Either #7's fix reaching `sign.pumasi.ai` (whereupon this is moot and the record is the deploy), or Q-021 resolving in either direction."* **The first condition is met.** This seat does not close the window — closing is the steward's, and the entry's Status line explicitly declines CHARTER Part 0's proceed-on-default rule — but it records that the entry has reached its own stated retirement condition, and that the entanglement it named dissolved by being acted on rather than by being decided. **Q-021 is untouched by that and remains the live question.** `pumasi-sign` `roadmap/BACKLOG.md` **B2** is retired; **B1** is not. |
| **Status** | open. **Not** claimed under CHARTER Part 0's proceed-on-default rule: the default here is inaction, and every action it forecloses is reserved by Q-012 or Q-021. Recorded now, while the defect is live and the window is open, rather than after someone resolves it by deploying. |


### Q-029 · Pumasi Booking's reminders and webhooks are delivered — can-hurt release, 7-day window

| | |
|---|---|
| **What** | `pumasi-booking` `0a35ddc`, coder job `0049`, closing `roadmap/BACKLOG.md` items 2 and 3 as one packet. `service/src/worker.ts:303` — inside `PumasiService.alarm()`, the deployed Cloudflare entry point — called `processDueJobs(...)` without importing it, so the org Durable Object's alarm threw `ReferenceError` on its first fire and died on the line *before* it re-armed. Every workflow email and every webhook on `booking.pumasi.ai` has been undelivered since `de4abbe` (2026-08-28); core booking and its on-the-spot confirmation mail were unaffected, and the Node entry point (`server.ts:20`) imported it correctly throughout. Also in this change, because a one-line fix could not be shown complete without it: `service/tsconfig.worker.json` makes `npm run typecheck` cover the deployed entry point for the first time (both existing configs `exclude` it), `service/test/worker-alarm.test.ts` is the first test that executes a line of it, and `tools/ci.sh` step 4/4 stops printing a sentence that this change made false. Release note: [`releases/2026-08-31-pumasi-booking-alarm-import.md`](releases/2026-08-31-pumasi-booking-alarm-import.md). |
| **Risk class** | `can_hurt`, read out of `service/spec/0002/RISK_ZONES.yaml` by this seat rather than carried: that file maps `README.md`, `INTENT.md` and `acceptance/` to `ordinary` and everything else to `can_hurt` under `"*"`, and this touches the deployed entry point. |
| **Window closes** | **2026-09-07** (7 days from 2026-08-31), per CHARTER §2.1. |
| **Default on silence** | The release stands as published. Stage is `beta`, so under CHARTER Part 0 the work has already proceeded and a steward veto **reverts** rather than prevents. |
| **Review** | One approving family other than the builder's, which is what CHARTER §4 asks of a can-hurt change (reduced from two by the steward on 2026-08-29) and more than Part 0 requires below `launched`: **gemini APPROVE** (`reviews/20260831-162058-code-gemini.md` in `pumasi-booking`), which re-ran the suite and `tools/ci.sh` itself. Four families did not review and are not counted as breadth: grok HTTP 402, and qwen/glm/kimi failed in `recruit` before any model saw the diff — `tools/review.sh` passes the context bundle as one argv entry and Linux caps a single argument at 131072 bytes (measured), against this diff's 630964. All five transcripts are committed. |
| **Verification** | By the builder, 2026-08-31 ~16:35 CDT: `npm test` **339/339** (19 engine + 320 service), `tools/gate.sh --can-hurt` printed `GATE: PASS`, `tools/ci.sh` exited 0. The regression net was checked by breaking it: with the import removed the type-check reports `TS2304` and all three new tests fail with `ReferenceError`, so the compiler is not the only thing that would catch a recurrence. |
| **Status** | open — window running. **Merged is not shipped, and this entry does not pretend otherwise: see Q-012.** |



### Q-030 · May a builder use CHARTER §3 requirement 2's own remedy to amend a frozen acceptance case?

| | |
|---|---|
| **What** | Raised by the `pumasi-tunnel` product evaluation of 2026-08-31 at `b3d251d` (job `0059`, ops `DIGEST.md`), escalated to it by the coder job `0047` that met the question and declined to settle it in a test file. CHARTER §3 requirement 2 says the tests **"are frozen when the spec review completes … the builder may not edit them"**, and then names its own remedy in the next sentence: **"If the tests are wrong, amend the spec in the open and take a fresh cross-family spec review."** Job `0047` used exactly that remedy on frozen acceptance case **A-10** (`pumasi-tunnel` `relay/scheme_test.go:314`–`:315`, `spec/0001-public-scheme`), whose fixture pins a TCP range of 34500–34599 — inside the host's `/proc/sys/net/ipv4/ip_local_port_range` of 32768–60999, the same defect the rest of that job fixed. It wrote the amendment in the open as SPEC 0001 §7 and obtained a fresh cross-family spec review, which gemini approved (`pumasi-tunnel/reviews/20260831-161500-spec-gemini.md`). **The code review then objected under the same clause**, on the ground that the *builder* authored the amendment. A cited objection governs and may not be argued past (CHARTER Part 3), so `0047` reverted the whole half — the A-10 fixture, SPEC 0001 §7, the `CASES.md` note and SPEC 0002 §6.6 — rather than proceed, and handed the reading up. |
| **Why the two readings cannot both stand** | **Requirement 1 makes the builder the spec author.** CHARTER Part 3's flow is *"Spec + tests, agent-authored"*, and requirement 1 says the specification is *"authored by an agent and reviewed by an agent of a different model family."* Authorship by the builder is the normal case, not an irregularity. If the objection is right — that a builder may not author the amendment — then requirement 2's remedy is available to **nobody**, because the same agent that would notice a frozen test is wrong is the agent building against it, and the charter names no other author. A remedy that no one may invoke is not a remedy. If the objection is wrong, A-10 could have been moved as `0047` moved it, and the revert cost the commons a fix it had already reviewed and approved. |
| **`SPEC 0002` §6.5 is precedent the other way, on this same product and by this same builder** | Amendment 3 of `pumasi-tunnel` `spec/0002-bind-before-announce`, 2026-08-31, **before merge**: the frozen B-cases all drew the same fixed port 20500 and raced each other's teardown, so `bindOrderPorts(t)` was introduced to give each case a block of its own. Its own text says why it was written down rather than done quietly — *"fixing it quietly inside the freeze would be the builder editing a case on its own judgement"* — and `acceptance/CASES.md` records that it *"changes the fixture, not a single assertion below."* That amendment was reviewed and it **stood**; it is in `main` today. It is the same act, by the same seat, on a frozen fixture, for the same class of reason. **Both cannot be right**, and the commons is currently operating on both. |
| **The reviewers contradicted each other, and that is part of the finding rather than noise** | Given the same clause and the same job: **qwen** approved the smaller change and then objected to the larger; **kimi** objected to the smaller and then approved the larger. Each switched sides on the same requirement across two ranges. All four transcripts are committed under `pumasi-tunnel/reviews/`. Cross-family review is the charter's control on exactly this kind of question, and here it returned an answer that depends on which range a reviewer was shown rather than on the clause. That is worth the steward knowing regardless of how the reading itself is settled. |
| **Why this is the steward's and not the product manager's** | It is a reading of CHARTER Part 3, which Part 7 reserves. The product-manager role's strongest verb is *propose*, and this seat may not amend the charter's merge gate or decide what a requirement means for every product and every builder in the commons. It is raised here rather than settled in a backlog entry because the answer binds well beyond `pumasi-tunnel`: every frozen suite in the fleet has a fixture, and every fixture can turn out to be wrong. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **Requirement 2's remedy is available to the builder, on the conditions the clause itself already states, and `SPEC 0002` §6.5 is the pattern.** A builder who finds a frozen test wrong may amend the spec **in the open** — a numbered amendment in the spec file, saying what changed and why, never a silent edit — and must take a **fresh cross-family spec review** of that amendment before building against it. Two riders, both from what the clause is for rather than added to it. **(a) The freeze protects assertions, not fixtures.** An amendment that changes what a case *asserts* is a change to the standard and the reviewer should weigh it as one; an amendment that changes only how a case is *set up* — a port number, a temporary directory, a block of ten instead of one — changes no standard and is the case that `§6.5` and A-10 both are. The amendment says which of the two it is, and a reviewer who disagrees says so with a citation. **(b) The spec reviewer of the amendment must not be the code reviewer of the change that follows**, where three or more families are available — requirement 1's own breadth rule applied to the amendment, so that the remedy does not become a way for one family to bless both the changed standard and the code meeting it. Under this default `0047`'s A-10 change was legitimate as performed, and moving that fixture is ordinary coder work. |
| **Named alternative, if the default is wrong** | **A frozen case may be amended only on the initiative of an agent other than the builder** — the builder reports the defect, and a different agent (different family) authors the amendment and takes it to spec review. The cost is explicit and should be stated rather than discovered: every fixture defect found mid-build becomes a two-job round trip through the project manager's queue; `SPEC 0002` §6.5 becomes retrospectively irregular and its status has to be settled; and the failure mode it invites is the one requirement 2 exists to prevent from the other side — a builder who knows a fixture is wrong, cannot cheaply fix it, and works around it instead, which is precisely what `0041` did when it routed `bindOrderBase` around the 34000-series harness rather than moving it. It is a defensible standard. It is just not free, and the bill lands on seats that did not choose it. |
| **What retires this entry** | The reading settled in either direction — either recorded here as decided, or written into CHARTER Part 3 requirement 2 as an added sentence. Either way `pumasi-tunnel` `roadmap/BACKLOG.md` item 9 becomes ordinary coder work or becomes formally not-this-builder's, and stops being ranked below everything buildable for want of an answer. |
| **Scope** | Asks only who may author an amendment to a frozen acceptance case and under what conditions. It does **not** ask whether A-10's fixture should move — that is agreed by everyone involved and is `pumasi-tunnel` `BACKLOG.md` item 9 — and it does not touch requirement 3's cross-family code review, requirement 4's signed record, or any product's code. Adjacent to **Q-025** from the other side: that entry asks what a gate *result* is worth when only its author ran it; this one asks who may change what the gate is measuring against. |
| **Evidence added 2026-08-31 — a SECOND instance, recorded by the `pumasi-sign` coder seat (job `0058`); this seat does not close this window, did not set a date, and does not claim the reading is settled** | **This entry's question was met on another product within the hour of its being raised, and the second instance is rider (a)'s HARD case rather than its easy one.** `pumasi-sign` `68e5d08` takes `roadmap/BACKLOG.md` item 1 — three unguarded envelope transitions on the deployed worker — and to do it amends frozen cases **A-404, A-406 and A-407** (`service/src/test/envelope-lifecycle.test.ts`, `spec/0005`). Those cases asserted the absence of the guards *on purpose*: `spec/0005` §S4a marked them `RECORDED, NOT ENDORSED` and stated in advance that **red there means someone took the backlog entry — a decision — and not that the worker regressed.** Measured by that seat: the unamended file run against the repaired worker gives `# pass 18 · # fail 3`, failing exactly those three. **Unlike `pumasi-tunnel`'s A-10, which moved a port range, this amendment changes what three cases ASSERT** — so under the default's rider (a) it is a change to the standard, it says so in `pumasi-sign/spec/0006` §S4-Q030, and the spec reviewer weighed it as one (gemini: the cases move *"from defect characterization … to active guard enforcement"*, `VERDICT: APPROVE`). Rider (b) is satisfied without contrivance: **gemini** reviewed the amendment, **kimi** reviewed the code, and `tools/families.sh` reported 5 of 6 families available. **The reviewer contradiction this entry reports has a second form here, and it has a mechanical cause worth separating from the clause — see Q-032:** gemini APPROVED the amendment and OBJECTED to the code in the same hour on the same work, because on the two runs it read two *different charter files*, only one of which contains Part 0. That is a forked document, not a family switching sides, and it is raised separately rather than folded in here. Nothing in this row proposes an answer to Q-030's question. |
| **Evidence added 2026-09-01 — a THIRD instance, and it is the avoidance case; recorded by the `pumasi-tunnel` product-manager seat (job `0076`), which does not close this window, did not set a date and does not claim the reading is settled** | **The cost of leaving this open is not only the reverted fix; it is also the specs written to route around it.** `pumasi-tunnel` `fd523e8` (job `0060`) fixes the HTTP path's announce-before-serve ordering, and in doing so establishes that **`spec/0002` §2 states something the change contradicts** — that `mux.Server(conn)` cannot be created before the handshake response is written, an argument `spec/0003` §2 shows is wrong about *reads* and right only about *writes*. The builder did **not** amend that frozen spec. It wrote a new one, `spec/0003-session-before-announce`, and said why: folding a contradiction into a frozen document as an *"amendment"* is how a rule forks (**L-007**). So this is neither of the two instances already recorded here — it is a builder declining to invoke requirement 2's remedy at all, at the price of **two specs covering one rule**, in a repository where a future seat must now read both to learn what the rule is. Whether that was the right call is exactly what this entry asks, and this seat does not answer it. **One bookkeeping note, so the entry's own citations still resolve**: this row's product re-ranked its backlog on 2026-09-01 (`1853218`), and the A-10 fixture entry this entry calls *"item 9"* and *"ranked ninth"* is now **item 8** — unchanged in substance, and still ranked below everything buildable for the reason given here. Its cost was re-measured at `87244af` and is still nothing: A-10 binds no port, 9 sockets were held inside 34500–34599 during this pass's runs, and the suite passed **425 of 425**. |
| **Evidence added 2026-09-01 — a FOURTH instance, and the first in which rider (a)'s classification was the part that could have been contested; recorded by the `pumasi-sign` coder seat (job `0080`), which does not close this window, did not set a date and does not claim the reading is settled** | **This entry's default was used a second time on `pumasi-sign`, and what it cost was five review rounds rather than one.** `spec/0009` renames `service/src/test/e2e-workflow.test.ts` — a file that was never an end-to-end test of anything, which `roadmap/BACKLOG.md` item 2 had carried as a finding since its fourth evaluation — and the rename trips frozen case **A-109** (`frontend/src/ci-covers-service.spec.ts`, `spec/0002` §S4b), whose preservation assertion identifies that file **by name**. Amendment 2 was written into `spec/0002`'s amendment log in the open, in its own commit (`af617e1`) with no implementation in it, and built against only after a fresh cross-family spec review — the ordering `spec/0002` amendment 1 established. **What is new here: unlike `pumasi-tunnel`'s A-10 (a port range, plainly a fixture) and unlike `68e5d08`'s A-404/A-406/A-407 (plainly assertions, and declared as such), this one sits on the line.** The builder classified it under rider (a) as a **fixture** change — the string by which the case *locates* what it protects; one `git mv`, nothing deleted, and measured with `git diff -M`, no assertion, fixture value or import altered — and wrote into the spec that a reviewer could reasonably read a filename inside an `existsSync` as an assertion instead, in which case it should be weighed as a change to the standard. **No reviewer contested that classification.** **What three reviews did contest, four times and every time correctly, was the builder's description of what the amendment BOUGHT.** `qwen` objected that the added `>= 2` file-count floor does not catch the deletion of an unlisted file (it does not); then that a claim the spec made about all of its own cases was false of one of them; then that two sections contradicted each other about whether the renamed file's content was byte-identical (it was not — a title and a header comment changed). `kimi` objected that the intent statement still carried the first overclaim after the spec had withdrawn it. All four were taken rather than argued past, and the amendment as merged claims strictly less than the draft did. **Rider (b) was satisfiable without contrivance and was honoured**: `gemini` and `kimi` reviewed the amendment, `glm` reviewed the code, `claude` built. **What this row does not do:** it proposes no answer to this entry's question. It records only that the default was serviceable a fourth time, and that the expensive part was not *who may amend a frozen case* but *whether the amendment's own account of itself is true* — which requirement 2 does not speak to, and which no reading of this entry, in either direction, would have caught. |
| **Status** | open. **Nothing is blocked on it in the ordinary sense** — `pumasi-tunnel`'s A-10 fixture binds no port, was measured to cost nothing today (12 of its 100 ports were held by an unrelated process during the evaluation of 2026-08-31 and the suite still passed 500 runs of 500), and its backlog entry is ranked ninth accordingly. What is blocked is the *general* case: the next builder to find a frozen fixture wrong has two committed precedents pointing opposite ways and no way to tell which governs. |

### Q-031 · Pumasi Sign's finished envelopes stay finished — can-hurt release, 7-day window

| | |
|---|---|
| **What** | `pumasi-sign` `68e5d08`, coder job `0058`, closing `roadmap/BACKLOG.md` item 1. Three transitions on the Cloudflare Worker that answers `sign.pumasi.ai` could reach into an envelope that had already ended and change it, each also appending a fresh audit event: **`cancel`** (`service/src/durable.ts:1239`) tested nothing at all, so a `completed`, `declined` or already-`cancelled` envelope was overwritten and audited again; **`complete`** (`:1434`) guarded `cancelled` and `declined` but not `completed`, so a **CC recipient** — still `pending` because `:1479` counts `AND is_cc = 0` — could sign an executed envelope, re-run `finalize()` and write a **second** `completed` event, re-stamping the PDF where one is present; **`decline`** (`:1490`) carried none of `complete`'s three guards, so an already-signed signer could refuse, a `completed` envelope flipped to `declined`, and the sender was emailed that their executed agreement had been refused. All three now refuse — 409, 410, 409 respectively — and each refusing path writes nothing and audits nothing. Release note: [`releases/2026-08-31-pumasi-sign-finished-envelopes-stay-finished.md`](releases/2026-08-31-pumasi-sign-finished-envelopes-stay-finished.md). Spec: `pumasi-sign/spec/0006`. |
| **Risk class** | `can_hurt`, reasoned rather than read: **`pumasi-sign` carries no `RISK_ZONES.yaml`** (checked, not assumed), so CHARTER Part 4's table was applied directly. The `decline` path sends mail on a real person's behalf and all three paths write the audit record of signed agreements, which is personal data about a legal act. Classified `can_hurt` on that basis; the absence of the file is itself reported to the product manager. |
| **Window closes** | **2026-09-07** (7 days from 2026-08-31), per CHARTER §2.1. |
| **Default on silence** | The release stands as published. Stage is `alpha` (`pumasi-sign/roadmap/STAGE.md`), so under CHARTER Part 0 the work has already proceeded and a steward veto **reverts** rather than prevents. |
| **Review** | **Two approving families other than the builder's, on the two halves the charter asks for separately: gemini APPROVE on the spec amendment** (`pumasi-sign/reviews/20260831-183823-spec-gemini.md`) **and kimi APPROVE on the code** (`reviews/20260831-185008-code-kimi.md`), which satisfies Q-030's rider (b) as well as CHARTER §4's single-family bar. **Three objections were returned and all three are governance, not substance — no family found a defect in the code, the amended cases or the spec.** Two cite CHARTER Part 3 requirement 2 against the frozen-case amendment (`20260831-181757-code-gemini.md`, `20260831-181757-code-qwen.md`); the third objects to the ordering (`20260831-182757-spec-qwen.md`). They are answered by Part 0 and by **Q-030**'s named default, in `spec/0006` §S4-review and §S4-Q030, and the ordering deviation is recorded rather than argued past. **qwen returned no verdict twice**, timing out at `curl`'s 600 s ceiling. **All eight transcripts are committed**, including the empty ones and the objections. |
| **Verification** | By the builder, 2026-08-31 19:01:43 CDT: `pumasi/tools/gate.sh 68e5d08` printed **`GATE: PASS`**, run unskipped. Root `npm test` on both trees, before at `0c043f8` and after, run by that seat rather than quoted: `Test Files 6 passed (6)`, `Tests 85 passed (85)`, `# pass 21 · # fail 0`, `assert-service-suite-ran: 21 passing, 0 failing, from 4 compiled` — **identical either side, so the service assertion count did not fall.** The repair was checked by breaking it the other way: the **unamended** `spec/0005` cases run against the repaired worker give `# pass 18 · # fail 3`, failing exactly A-404, A-406 and A-407 and nothing else. Gate step 4/4 printed `tools/families.sh missing — breadth UNVERIFIED` because it resolves that path inside the product repository; the builder ran `pumasi/tools/families.sh` by hand instead — **5 of 6 families available, grok UNREACHABLE.** |
| **What this does NOT claim** | **`VALUE.md` C1 is not falsified and was not.** The stamped PDF and its audit certificate live in R2 and a status overwrite does not touch them. What the overwrite damaged is narrower and still real: the Durable Object row and the audit log came to say `cancelled` about an envelope whose certificate says `completed` — one product, two records, one claim, [L-009](lessons/L-009-two-paths-one-claim.md) at row scale. The release also **widens no test coverage**; the gate prints the same 21 assertions it printed before, and `service/` remains thin. |
| **Independent confirmation** | *Added 2026-09-01 00:41 UTC by the `pumasi-sign` product-manager evaluation (job after `0058`), which is not the builder. Nothing in this entry's Status, Window, Default or Review rows was touched.* **The repair is in the tree**, re-read at `56a8bf8` rather than taken from the row above: `cancel` `409` at `durable.ts:1252`, `complete` `410` at `:1452`, `decline` `409` at `:1513`, one predicate `isTerminal` at `:109`, each guard the **first statement of its branch** so a refusing path reads no body, writes nothing and audits nothing. **The line numbers in the What row (`:1239`, `:1434`, `:1490`) are the pre-fix ones** — correct as a record of the defect, and they will not locate the repair. **The suite figures reproduce**: root `npm test` Ã3 plus `service/` `npm test` Ã1 at `56a8bf8` gave `Test Files 6 (6)`, `Tests 85 (85)`, `# pass 21 Â· # fail 0`, `assert-service-suite-ran: 21 passing, 0 failing, from 4 compiled` â identical every run and identical to `f7c8d03`, confirming this entry's own claim that the release **widened no coverage**. **The Status row is still true 4 hours after publication**, measured rather than assumed: `curl` at 2026-09-01 00:29 UTC returns the deployed bundle `/assets/index-j38Qwibz.js`, unchanged, and `GET /api/auth/login?next=%2F` â `404`. Nothing has deployed; the three transitions behave the old way for every user. Recorded in `pumasi-sign/roadmap/STAGE.md` Â§2.6(i) and Â§5 state (iii). **On the Risk class row's reported gap:** `pumasi-sign` still carries no `RISK_ZONES.yaml`, and the comparison that prompted it is not quite right â `pumasi-booking` has **two** (`core/spec/`, `service/spec/0002/`) and **neither is at the repository root CHARTER Part 4 names**, while `pumasi`, `pumasi-ops`, `pumasi-web` and `pumasi-tunnel` have none. Ranked as `pumasi-sign/roadmap/BACKLOG.md` **item 7**, deliberately not higher, because Part 4's own default (*unmapped â can hurt someone*) already fails safe. No question is raised on it. |
| **Evidence added 2026-09-01 — recorded by the `pumasi-booking` product-manager seat (job `0082`); evidence only. This seat did not set, move, extend, soften or close this window, did not touch the risk class or the default, and did not close or comment on issue #32** | **Three measurements at `d7bd490`, two of which disagree with rows above. The tree and the live host are the record in both cases, and the disagreement is reported rather than argued.** **(1) The defect is still live, re-measured after the merge and after a deploy.** Headless Chrome 150.0.7871.186, `America/Chicago`, **2026-09-01 05:12:11 UTC**, `https://booking.pumasi.ai/yunyoungmok/abc`: **200**; `#slots-data` carries **24 slots**, first `2026-09-01T14:00:00Z`, last `2026-09-02T21:30:00Z`; day cells **1** and **2** marked `.has`; clicking each gives `#picked-day` *"Tuesday, September 1"* and *"Wednesday, September 2"* with **`#times.children.length` 0** both times and **0 errors captured**. `/version` → `{"version":"0.2.0","commit":"2453adc"}` at 05:11:40 UTC; `git merge-base --is-ancestor d7bd490 2453adc` → **no**. This confirms the Status row's 03:52:59 measurement from an independent session 80 minutes later, and it is why **`pumasi-booking/roadmap/STAGE.md` was demoted from `beta` to `alpha`** at this evaluation, on a condition that file had pre-registered at its previous refresh. **(2) The exposure window in the published note and in the row above is wrong by roughly fourteen times, and the error overstates harm to users.** The release note says the line *"had been deleted four weeks earlier"* and that *"we cannot say how many people met this in the four weeks it was live"*. **`50f911f` is dated 2026-08-30 05:38:43 UTC** and issue #32 was filed **2026-09-01 00:36:56 UTC** — **42 hours 58 minutes**, and that is a **ceiling**, since the build carrying it was deployed later. `git log -S 'times.appendChild' -- service/src/pages.ts` returns exactly three commits — `efb392e` added the statement, `50f911f` removed it, `d7bd490` restored it — measured at this tree. **This repository's first commit is `e761f05`, 2026-08-28**: it is four days old, so a four-week exposure is impossible on its face. **No published note was edited by this seat.** Publishing is not in the product-manager role, and **Q-034**'s rider (a) says a published note is a dated record corrected in the next one rather than in place. Whether a correction is owed, and by whom, is not asserted here. **(3) The Review row understates the breadth this merge actually got.** That row states that kimi *"and qwen were both killed, exit 137"* and that *"their transcripts are committed **empty**"*. At `d7bd490`, measured with `wc -c` and `grep VERDICT` on the committed files: `reviews/20260831-223524-code-kimi.md` is **4457 bytes**, ends **`VERDICT: APPROVE`**, and its body walks B-001…B-006 and S3-001 case by case, including the frozen-acceptance integrity check. `reviews/20260831-223641-code-qwen.md` is **376 bytes** and records `curl: (28) Operation timed out after 600000 milliseconds` — a husk, but a latency failure rather than an exit 137, and not empty either. **So this merge got three real cross-family reviews, not two** — gemini on the spec (7931 bytes, APPROVE), kimi on the code (4457, APPROVE) and glm on the code (7811, APPROVE) — with one husk and **no `Argument list too long` anywhere. That is the best review breadth this repository has ever had, and it is better than this entry credits.** Recorded so the record is not worse than the work. **(4) One risk this entry names was probed and has no mechanism at this tree.** The Review row records glm's finding that the cases *"pin the timezone but not the clock"*, so a reddening after 2026-09-02 should not be misdiagnosed as a regression. Checked here: `service/src/pages.ts`'s booking-page client script contains **no bare clock read** — every `new Date(...)` in the render path takes an explicit argument, `.has` is assigned purely on `byDay[key]` existing (`pages.ts:962`) with no comparison against today, and both `Intl.DateTimeFormat` instances receive an explicit `timeZone` — and `service/test/booking-slots.test.ts` contains no `Date.now()` and no `new Date()` at all. On that reading the fixture's past dates have nothing to act on. **Stated as measured, not as settled:** if the cases do redden after 2026-09-02, this reading is wrong. **(5) The premise finding is endorsed and the lesson is not written here.** This seat re-checked all three facts — `/usr/bin/google-chrome` 150.0.7871.186 present, `puppeteer` a devDependency at `service/package.json:33`, `browser-live.test.ts` in the default suite — and agrees the premise was false and cost four evaluations. **`pumasi/lessons/` is not in the product-manager role's may-write list**, so the lesson is handed to the steward and the project manager rather than written. One piece of evidence is added to it: **the same mistake occurred in the other direction inside this very work** — an asserted *risk* (item 4 above) nobody probed, exactly as an asserted *absence of capability* was not probed. The lesson is better stated about assertions than about capabilities. |
| **Status** | open — window running. **Merged is not shipped, and this entry does not pretend otherwise: see Q-012**, which is open and explicitly outside Part 0's proceed-on-default rule, and **Q-018**, which records that shipping this product means `wrangler deploy` from `service/`. No seat on job `0058` deployed, proposed a deployer, or set a date. As of publication the three transitions still behave the old way for anyone using the live service. |

### Q-032 · The charter exists in two places, they disagree about Part 0, and reviewers are reading whichever they find

| | |
|---|---|
| **What** | Found by the `pumasi-sign` coder seat (job `0058`) while resolving cross-family objections, and reported because it will mislead the next run too. **There are two `CHARTER.md` files on the working machine and they are not the same document.** `pumasi/governance/CHARTER.md` — the copy [`pumasi-ops/roles/coder.md`](https://github.com/pumasi-ai/pumasi-ops/blob/main/roles/coder.md) and `DRIVER.md` name as the one to read — contains **Part 0** (*"Guidance until `launched`"*), added by the steward on **2026-08-30**. `/home/m/dev/governance/governance/CHARTER.md`, a checkout of **`github.com/pumasi-ai/governance`** whose `main` is last committed **2026-08-29 15:51 CDT**, contains **no Part 0 at all** — `grep -c 'Part 0'` returns `0`, and `git log --all -S` finds the clause in none of that repository's history. **Both files carry the identical header `**Version:** 0.4-draft · **Status:** Proposed`,** so nothing in either tells a reader which governs. `pumasi/governance/README.md` states of its own copy: *"Read `CHARTER.md`. It is about one page of actual rules, and **it is the only copy**."* That sentence is false today, and the second copy is the one an agent reaches by the obvious path. |
| **Why it is not a filing detail** | **It changed a review verdict, and this is verifiable in the reviewers' own cited paths rather than inferred.** The same family reviewed the same work twice in one hour and split: `pumasi-sign/reviews/20260831-183823-spec-gemini.md` cites `file:///home/m/dev/pumasi/governance/CHARTER.md`, applies Part 0 with line citations, confirms `alpha` out of `roadmap/STAGE.md`, and returns **`VERDICT: APPROVE`**; `reviews/20260831-185008-code-gemini.md` cites `file:///home/m/dev/governance/governance/CHARTER.md`, quotes Part 3 requirement 2 as **unqualified** because in that file it is, and returns **`VERDICT: OBJECT`**. `reviews/20260831-185008-code-kimi.md`, reasoning from the governing copy, reached the opposite conclusion in writing. **A cited objection governs and may not be argued past (CHARTER Part 3) — which means a stale copy of the charter can block a merge on a clause the charter no longer states unqualified.** `pumasi/tools/review.sh` gives an agentic reviewer no way to be told which copy governs, and the agentic reviewers are the ones that open files for themselves. This is [L-007](lessons/L-007-restating-a-rule-forks-it.md) — a rule restated in a second place forks from its source — applied to the charter itself, and `governance/README.md` records L-007 having already happened once to that very folder's summary. |
| **Relation to Q-030** | **Q-030 asks whether a builder may amend a frozen case; this asks whether reviewers are all reading the same rulebook when they answer.** They are adjacent and neither settles the other. Q-030 reports that *"the reviewers contradicted each other, and that is part of the finding rather than noise"* — qwen and kimi each switching sides across two ranges on `pumasi-tunnel`. **This entry supplies a mechanical cause for one such contradiction and explicitly does not claim it explains Q-030's**, which was a different product, different families and different ranges, and which this seat did not re-examine. If some of that contradiction is two rulebooks rather than two readings, Q-030's evidence is worth re-weighing before its question is answered; if none of it is, Q-030 is untouched. |
| **Window closes** | *(steward to set — agents may not set a deadline. This entry queues a human act and names a default the agents may take, per the coder role's write list.)* |
| **Default on silence** | **The agents make the two copies agree in the direction the commons already treats as authoritative, and change no rule while doing it.** Concretely, and each step reversible: **(a)** copy `pumasi/governance/CHARTER.md` into `pumasi-ai/governance` verbatim, as a propagation commit that adds Part 0 and the 5.1 and P5 amendments of 2026-08-30 and states in its message that it is a copy and decides nothing; **(b)** add one line to the top of both files naming which repository is normative and the date of the copy, so a reader who finds either can tell; **(c)** correct `pumasi/governance/README.md`'s *"it is the only copy"* sentence, which is the claim that made this invisible. **What the default deliberately does NOT do:** delete either checkout, change a single word of any rule, or decide which repository *should* be canonical — only which one currently *is*, evidenced by the role files and `DRIVER.md` pointing at `pumasi/`. **This seat took none of it**: both files are outside a coder's write list and one is another repository entirely. |
| **Named alternative, if the default is wrong** | The standalone `pumasi-ai/governance` repository is canonical and `pumasi/governance/` is the stale copy — in which case the steward's 2026-08-30 amendments live only in a copy, every product has been running on unpropagated rules for two days, and the propagation must go the other way. Nothing this seat found decides between the two: the role files point at `pumasi/`, and the `lessons/` links throughout the commons point at `pumasi-ai/governance`. **That split is itself the evidence that nobody has decided.** |
| **What retires this entry** | One charter that governs, reachable by one path, saying so in its own header — and `governance/README.md`'s claim about copies being true. |
| **Status** | open. Nothing is blocked on it: job `0058` merged with `GATE: PASS` and two approving non-builder families. What is at risk is the next run, which may be blocked by a cited objection quoting a superseded clause and have no way to tell. |

### Q-033 · Nobody knows whether a job may push `pumasi`, and three consecutive jobs each decided differently

| | |
|---|---|
| **What** | The commons repository is written by many seats and pushed by none of them consistently. Within four hours on 2026-08-31, three jobs reached the same fork and took three different roads. Job `0056` (coder, `pumasi-booking`) wrote nothing to `pumasi` and recorded that `main` was ahead 1 unpushed *"from a prior seat"*, treating the push as somebody else's. Job `0059` (product-manager, `pumasi-tunnel`) **wrote** `DECISIONS.md` (Q-030) and deliberately did **not** push, saying in its return block *"NOT pushed — left for the steward as job `0056` did; `pumasi` is now ahead 3, of which one is mine."* Job `0058` (coder, `pumasi-sign`) then pushed **all four** — its own `6fd0d6d` plus `133d337`, `3d2c638` and `7a0892d`, three other seats' commits, one of them the `DECISIONS.md` edit `0059` had explicitly parked for the steward. **No file in the fleet states which was right.** `pumasi-ops/roles/coder.md` and `roles/product-manager.md` both name `pumasi/DECISIONS.md` as writable and neither says a word about `git push`; `DRIVER.md` and `governance/CHARTER.md` are likewise silent. |
| **Why it is not bookkeeping** | Three things follow and each has already happened or is one tick away. **(a)** A seat that parks a commit *for the steward* has no mechanism to keep it parked — the next seat through the repository pushes it as ordinary hygiene, which is exactly what happened to `7a0892d`. Deliberate parking and forgetting to push are indistinguishable in `git status`. **(b)** The wall that makes parking the default is itself a defect: `pumasi/tools/gate.sh` step 1 is an unconditional `npm test` and this repository has had **no `package.json` since `69ecfc1`**, so `GATE: FAIL` is the only result the commons can produce. A seat that will not push an ungated commit therefore never pushes, and the seat that does push is the one that decided the gate does not apply here. That is [`Q-025`](#q-025--nothing-re-runs-the-merge-gate--is-gate-pass-an-agents-own-report-and-is-that-enough-for-a-beta-claim)'s question arriving through a different door. **(c)** Unpushed `DECISIONS.md` entries are invisible to anything reading the remote, which is where a steward looks. Q-030 sat unpushed for 23 minutes; the next one might sit for a day. |
| **Window closes** | *(steward to set — agents may not set a deadline. This entry queues a human act and names a default the agents may take, per the project-manager role's write list.)* |
| **Default on silence** | **A seat that writes `pumasi` pushes what it wrote, and pushes only what it wrote.** Concretely: commit under `flock` on `.lock_pumasi` as job `0059` did; `git push` your own commit; and if `main` is ahead of `origin` with commits that are not yours, **leave them and name them in the return block** rather than pushing or reverting them. A seat that judges its own commit should *not* be pushed says so and says why. This changes no rule and needs one added line in `pumasi-ops/roles/coder.md` and `roles/product-manager.md`, which is a steward or `pumasi-ops` coder edit — **this seat took none of it.** |
| **Named alternative, with its cost** | **Nobody pushes `pumasi`; the steward does, as a review checkpoint.** This is what `0056` and `0059` behaved as though were true, and it has a real argument: the commons carries the charter, the decisions and the release notes, and a human seeing them before the remote does is a feature. Its cost is what this entry documents — parked commits accumulate with no owner, the queue's only signal that a decision was raised is a local file, and the first seat to disagree pushes the backlog of four. If this is chosen, the mechanism has to be stated too (who, how often, and what a seat does meanwhile), or the same drift returns under a different name. |
| **Explicitly not asked here** | Whether `tools/gate.sh` should be runnable in `pumasi` — that is a defect with a packet against it this tick, and it is orthogonal: the push rule is needed whether or not the gate is fixed. And whether the *product* repositories have the same ambiguity — they do not; every product seat has pushed its own work all week without anyone hesitating. |
| **A defect in this entry's own default, found by following it** | *"Push only what you wrote"* **is not always implementable, and the project-manager tick hit that within the hour.** `git push` sends ancestors: if another seat's unpushed commit sits **beneath** yours, you cannot push yours without pushing theirs, short of cherry-picking onto a fresh branch — which rewrites nothing but makes the shared history diverge from what every seat's working copy holds. On 2026-08-31 the tick could follow the default in `pumasi` only because it happened to arrive when `main` was in sync at `6fd0d6d`; in **`pumasi-ops`**, where the same tick's commit sat on top of **four** commits from earlier seats, the default gave no legal move and the tick left all five unpushed rather than pick one. So the default holds when you are first to the fork and is silent when you are not, which is the majority case in a repository many seats write. Whichever way this entry is answered, the answer has to say what a seat does when someone else's work is underneath its own — the honest candidates being *push the stack and name whose commits you carried*, or *never push, and give the parking a named owner*. |
| **What retires this entry** | One sentence in the coder and product-manager role files that a seat can follow without guessing, including the case where another seat's commit is an ancestor of yours, and a `pumasi` `main` whose relationship to `origin/main` is somebody's stated decision rather than an artefact of who arrived last. |
| **Status** | open. Nothing is blocked on it today — `pumasi` is in sync at `6fd0d6d` as this is written. Raised by the 2026-08-31 19:00 CDT project-manager tick, from job `0058`'s handover, which asked for it in these words: *"0056, 0058 and 0059 each made a different call on pushing pumasi and no file states one. Cheap to settle in the role file."* |

### Q-035 · Pumasi Sign keeps the expiration date it asks for — intent (24 h) and can-hurt release (7-day window)

| | |
|---|---|
| **What** | `pumasi-sign` `712a600`, coder job `0065`, taking `roadmap/BACKLOG.md` item **1** at `ba1cea7` in full, option **(A)**. The Send wizard asks a sender for a deadline, refuses one in the past, displays it back, and states in two places — `SendView.vue:1332`, `EnvelopeDetailView.vue:1104` — *"Without an expiration date, the envelope stays open until completed or voided."* **The Cloudflare Worker that answers `sign.pumasi.ai` stored the date and did nothing else**: a recipient holding a token link could sign an envelope long past its deadline and it completed normally. Frozen case **A-409** drove exactly that and asserted the `200`. An hourly cron trigger now drives a `scheduled` export in `worker.ts` which sweeps past-deadline `pending` envelopes to `expired`, writing one system-authored audit event each; `expired` joins `isTerminal()` so job `0058`'s three guards cover it with no new code (cancel `409`, complete `410`, decline `409`), and the token surface refuses it too. `PATCH /api/submissions/{id}` also starts saving the `expires_at` and reminder fields the *correct expiration & reminders* dialog has always sent and that route has always discarded — a precondition, not a bonus, since a binding deadline plus a pencil that silently does nothing is a trap this change would otherwise create. Intent: `pumasi-sign/spec/0007/INTENT.md`. Spec: `pumasi-sign/spec/0007`. Release note: [`releases/2026-09-01-pumasi-sign-expiration-dates-bind.md`](releases/2026-09-01-pumasi-sign-expiration-dates-bind.md). |
| **Both windows, in one entry** | CHARTER §2.1's **24-hour intent window** and Part 4's **7-day can-hurt release window**, filed together as Q-031 did for the same product. The longer window governs the date below. |
| **Risk class** | `can_hurt`, by CHARTER Part 4's own rule rather than by anyone's judgement: **`pumasi-sign` carries no `RISK_ZONES.yaml`** — re-checked at `ba1cea7`, and it is that repository's `BACKLOG.md` item 7 — and Part 4 says the classification *"defaults to can hurt someone when unmapped or unclear"*. It would be `can_hurt` on the merits too: the sweep decides that a real person may no longer sign an agreement. **Sub-question (3) resolved to silence, so this change sends no mail** — the one act in it that CHARTER Part 0 never suspends is not in it. |
| **Window closes** | **2026-09-08** (7 days from 2026-09-01), per CHARTER §2.1. |
| **Default on silence** | The release stands as published. Stage is `alpha` (`pumasi-sign/roadmap/STAGE.md`), so under CHARTER Part 0 the work has already proceeded and a steward veto **reverts** rather than prevents. |
| **What it takes away, named rather than buried** | **Somebody who could have signed a lapsed document can no longer sign it.** That is what a deadline is, it is what the sender asked for by setting one, and it is what the SPA already told both parties was happening — but it is a capability removal and it is why this entry exists. An expired envelope also cannot be revived; the sender's route is the existing `POST /{id}/copy`, which makes a fresh draft with a null deadline. Drafts never expire. |
| **The residual, chosen rather than discovered** | The sweep is **hourly**, so for up to one hour past a deadline the API would still accept a signature the SPA already refuses to offer. Hourly was chosen over `backend/`'s daily 09:00 UTC precedent precisely because that interval *is* the length of time the product's promise stays false — and **Q-018** is open, so that tree was read for shape and not treated as the answer. This is in the release note in its own section rather than implied. |
| **Review** | **Four transcripts, four approvals, and the spec and code reviewers share no family** — CHARTER §3 requirement 1's breadth rule, which binds because `tools/families.sh` reported **5 of 6 available** (grok UNREACHABLE). **Spec: gemini APPROVE** (`pumasi-sign/reviews/20260831-203202-spec-gemini.md`) and **glm APPROVE** (`…-spec-glm.md`), both before the build. **Code: kimi and qwen** (`…-code-kimi.md`, `…-code-qwen.md`). **glm returned three non-blocking notes and all three were taken into the build rather than filed**: the sweep's per-row `UPDATE` now re-carries its `SELECT`'s predicate, `§S2d` states what its bind parameter is, and `§S3e`'s claim that a frontend comment *"becomes true"* was **wrong as written and is corrected in the spec** — glm was right that the comment moves from always-false to false-for-up-to-an-hour. A fourth glm note, that nothing server-side rejects a past `expires_at` on create, is **deliberately not taken** and is handed to the product manager with its reasoning. |
| **Verification** | By the builder: root `npm test` at `ba1cea7` before, and after: `Test Files 6 passed (6)`, `Tests 85 passed (85)`, and **`# pass 21 → 28`, `# fail 0`, `assert-service-suite-ran: 21 → 28 passing, from 4 → 5 compiled`.** Unlike Q-031, **this release widens the deployed tree's coverage** — seven new assertions, and **A-415 is the first test in the repository to drive `service/src/worker.ts` at all.** `pumasi/tools/gate.sh` output is recorded in the ops digest verbatim. |
| **No frozen case was amended, and Q-030 is not reached** | **A-409 is untouched and still passes.** That is a property of the design rather than luck: the sweep is the only writer of `expired` and A-409 never invokes it, so a deadline *by itself* still transitions nothing and every one of A-409's assertions — including `POST /api/jobs/daily` → `404` — remains true. `pumasi-sign/spec/0007` §S6 checks that assertion by assertion. **No reading of Q-030 is taken in either direction and no evidence is added to that entry.** One honest debit: A-409's *header comment* now says something stale (*"no `scheduled` handler and no cron trigger"*); it is a comment rather than an assertion, correcting it would be a builder editing a frozen file for no gain in what the suite measures, and it is recorded in the spec — *"amend the spec in the open"* — instead. |
| **Q-032, and one thing that went right because of it** | `pumasi-sign/spec/0007` **§S10 names the governing charter in the spec itself** — `pumasi/governance/CHARTER.md` at `pumasi` `46896b6`, the copy containing Part 0 — because `tools/review.sh` gives an agentic reviewer no way to be told which of the two copies governs. **It worked**: gemini cited `file:///home/m/dev/pumasi/governance/CHARTER.md`, and glm opened its review by naming that file as the standard §S10 pointed it at. On job `0058` the same family read the other copy on a second pass and objected on a clause that copy states unqualified. **This resolves nothing about Q-032** — two copies still exist, still carry the same version header, and neither says which governs. It is one product's workaround, offered as evidence that the mechanical cause is real and cheap to neutralise. |
| **Status** | open — window running. **Merged is not shipped, and this entry does not pretend otherwise: see Q-012**, which is open and explicitly outside Part 0's proceed-on-default rule, and **Q-018**, which records that shipping this product means `wrangler deploy` from `service/`. **Q-028** records that two earlier repairs already wait in the same undeployed bundle; this is the third. No seat on job `0065` deployed, proposed a deployer, or set a date. **As of publication every deadline in the live service still does nothing, and a past-due envelope is still signable there.** |

### Q-036 · A subprocessor reached production without appearing in the published register — was the prior-announcement clause owed, and what is owed now?

| | |
|---|---|
| **What** | The subprocessor register served at **`https://booking.pumasi.ai/subprocessors`** names five parties and no sixth: *Cloudflare, Inc.*, *Google LLC (Gmail API)*, *Google LLC (Google Calendar)*, *Microsoft Corporation (Microsoft Graph)*, and — under "contacted, but sent no personal data" — *date.nager.at*. Version 1.0, effective 2026-08-29. **The build serving that page sends Zoom the booker's name and email address on any booking for an event type whose location is set to Zoom.** Established by job `0068` (marketing-manager, `pumasi-booking`) and re-verified by the 2026-08-31 20:45 CDT project-manager tick against the **live page**, not against source. |
| **The facts, each measured rather than inherited** | **(a)** The live page was fetched at 01:47–01:53 UTC 2026-09-01 and names no sixth party. **(b)** `curl https://booking.pumasi.ai/version` → `{"version":"0.2.0","commit":"2453adc"}`, unmoved since 01:11 UTC. The commit that stores a Zoom connection is an ancestor of it. **(c)** On a Zoom-location booking that build sends Zoom the booker's **name** (meeting title), the booker's **name and email address** (agenda line), and the meeting start, duration and the account holder's timezone. **(d)** It can do this on **either** the account holder's own Zoom grant **or** a server-to-server credential belonging to the operator — **the second on the operator's authorisation, for an account holder who never connected Zoom.** **(e)** `pumasi-booking/SUBPROCESSORS.md` was corrected at `c4b1159`, now names Zoom, and says in its own text that the served page does not. |
| **The clause this turns on** | The same register's "Adding one" section states, on the live page: *"Account holders are told before an addition takes effect, and may object under the data processing terms."* |
| **A measurement trap, recorded so the next seat does not repeat it** | `curl … /subprocessors \| grep -ic zoom` returns **4**, and all four are the CSS class `pf-shot-zoom-hint`. `grep -io "zoom video"` returns nothing. The tick nearly read those four hits as a disclosure. Any evidence added to this entry must survive that. |
| **The question** | Does a provider that was put into production **without appearing in the published register** trigger that prior-announcement clause, and if so what is owed to account holders now — a notice, a retrospective disclosure carrying the date the provider actually went live, an objection window opened after the fact, or nothing beyond publishing the corrected register? |
| **Window closes** | *(steward to set — agents may not set a deadline. This entry queues a human act and names a default the agents may take, per the project-manager role's write list.)* |
| **Default on silence** | **Publish the corrected served register as soon as the coder packet lands, *and* send account holders a plain retrospective notice** that names Zoom, states what it receives, states when it began receiving it, and states that the announcement was owed before the fact and was not given. **Publishing the corrected register does not on its own discharge the clause.** This default is deliberately the **more onerous** of the two readings: the cheaper reading is available to the steward at any time, and the expensive one cannot be taken back later. |
| **Named alternative, with its cost** | **Publishing the corrected register discharges it** — the clause governs the register, the register is being corrected, and a retrospective notice to every account holder over a provider most of them never invoked is noise that devalues the next notice. Its cost is that the clause then means "we will tell you, unless we forget, in which case we will quietly amend the page", which is not what it says and not what an account holder reading it would understand. If this is chosen, say so in the register's own text rather than leaving the clause standing unqualified. |
| **Route-around while this is open** | **None is needed for the register fix itself.** The coder packet (`pumasi-ops` `jobs/proposed/0072`) lands independently and must not wait on this question — it is queued, bounded, and reversible. **Only the notice to account holders waits.** |
| **Related and separate — deliberately not folded in** | The same live page states that the mail allowlist *"is enforced by the software: the service will not send mail through a provider that is not on it"*, with no scope note, while on the deployed Cloudflare build nothing checks it. That is a **correctness defect in published text**, not a disclosure question, and it is item 2 of the same coder packet. Also separate: **`Q-012`**, which is open and explicitly outside CHARTER Part 0's proceed-on-default rule, governs whether the corrected register can be *deployed* at all. **Merged is not shipped, and this entry does not pretend otherwise.** |
| **Status** | open. Raised by the 2026-08-31 20:45 CDT project-manager tick from job `0068`'s handover, which asked for it in these words and **deliberately declined to answer it**: *"the marketing seat deliberately did not answer it and must not."* **As of this writing the register a customer is actually pointed at omits a provider the deployed build sends their name and email address to.** No seat has notified anyone, and nothing here authorises a seat to. |

### Q-037 · Pumasi Sign stops deleting the sender's message to signers — intent (24 h) and can-hurt release (7-day window)

| | |
|---|---|
| **What** | `pumasi-sign` `9659e69`, coder job `0073`, taking `roadmap/BACKLOG.md` item **1** at `3edd06f` in full. `PATCH /api/submissions/{id}` kept an omitted `title` — `String(body.title ?? sub.title)` — and had **no counterpart for `message`**, so `body.message != null ? … : null` wrote `NULL` whenever a body did not mention it. `EnvelopeDetailView.vue:428`'s *correct expiration & reminders* dialog sends exactly three fields and never mentions it. **So every use of that pencil deleted the sender's covering note to the signers** — the note `durable.ts:1593` returns to every recipient on the token view — silently, and closed on *"Envelope settings updated."* The repair distinguishes **absent** (keep) from **present-and-null** (clear); the `corrected` audit row's `changed` list now names `title` and `message` when they actually changed (§S3, and see the next row). Intent: `pumasi-sign/spec/0008/INTENT.md`. Spec: `pumasi-sign/spec/0008`. Release note: [`releases/2026-09-01-pumasi-sign-message-survives-settings.md`](releases/2026-09-01-pumasi-sign-message-survives-settings.md). |
| **Both windows, in one entry** | CHARTER §2.1's **24-hour intent window** and Part 4's **7-day can-hurt release window**, filed together in the shape **Q-035** established for this product one job ago. The longer window governs the date below. |
| **Risk class** | `can_hurt`, by CHARTER Part 4's own rule rather than by anyone's judgement: **`pumasi-sign` still carries no `RISK_ZONES.yaml`** — re-checked at `3edd06f`, and it is that repository's `BACKLOG.md` item 5 — and Part 4 says the classification *"defaults to can hurt someone when unmapped or unclear"*. **`BACKLOG.md` item 1's author reads it as not `can_hurt` on the merits** — *"it repairs a destructive write rather than creating one"* — **and this seat agrees with that reading and took the window anyway**, because reclassification is itself a can-hurt change (Part 4) and no seat here is making one. On the merits there is still something to point at, stated so the disagreement stays cheap: the route handles a sender's own text about an agreement, and the change alters what a past-facing audit record says. |
| **Window closes** | **2026-09-08** (7 days from 2026-09-01), per CHARTER §2.1. |
| **Default on silence** | The release stands as published. Stage is `alpha` (`pumasi-sign/roadmap/STAGE.md`), so under CHARTER Part 0 the work has already proceeded and a steward veto **reverts** rather than prevents. |
| **The question item 1 declined to answer, answered here rather than passed on** | *Does the `corrected` audit row record a message change?* **Yes, and `title` goes with it.** `2471a29` gave that row a `changed` list carrying the three **settings** and nothing else, so the history named a reminder interval and said nothing about the words of the agreement. **This change is what makes the answer yes**: before it, half of all message changes were accidents and auditing them would have recorded noise; after it, every one is deliberate. Both are compared against the stored row rather than against presence in the body, because both dialogs re-send fields the sender did not touch, and a history line claiming a change that did not happen is its own defect. **The 409 guard stays keyed on `settings`, never on the widened list** — spec/0007 §S3d promised that a body with no settings field is unaffected on every status, and A-419 asserts both halves of that. |
| **What it takes away, named rather than buried** | **On the merits, nothing.** Nobody asked that dialog to delete their message and nobody could have relied on it doing so. **One honest debit, about records rather than people:** the `corrected` row starts carrying a `changed` list on the correct-details path where it carried none, so the envelope history begins naming a title or message change where it previously named nothing. Strictly more informative, and still a change to what a past-facing document says. **And one thing the repair cannot do:** messages already deleted are gone — overwritten with `NULL`, no shadow copy. The note says so in the sender's own words rather than letting "fixed" imply otherwise. |
| **Review** | **Four transcripts, four approvals, and the spec and code reviewers share no family** — CHARTER §3 requirement 1's breadth rule, which binds because `tools/families.sh` reported **5 of 6 available** (grok UNREACHABLE), measured at this job. **Spec: gemini APPROVE** (`pumasi-sign/reviews/20260831-214423-spec-gemini.md`) and **glm APPROVE** (`…-spec-glm.md`), both before the build. **Code: qwen and kimi** (`…-215359-code-qwen.md`, `…-code-kimi.md`). **glm's three non-blocking notes were all taken into the build rather than filed**: A-418's no-op body shape is now pinned in the case and named in the spec (it discriminates a comparison-keyed audit list from a presence-keyed one *only if the body carries the keys* — the L-006 failure mode exactly), A-419 now drives a body carrying settings and content together, and §S7's mutation table gained a column saying which substitution was run. **One glm inference was wrong and is corrected in the spec rather than quietly dropped**: it reasoned the first mutant should give `# fail 1`, missing A-419's title-only PATCH, whose omitted `message` is what makes the measured count 2. |
| **Verification** | By the builder: root `npm test` at `3edd06f`, **twice before and twice after, identical each time** — `Test Files 6 passed (6)`, `Tests 85 passed (85)`, and **`# pass 28 → 31`, `# fail 0`, `assert-service-suite-ran: 28 → 31 passing, from 5 → 6 compiled`.** **Three wrong builds were made and run rather than argued about**, and each reddens a different case: the defect restored (A-417, A-419 fail), the `??` shortcut that would make a message unremovable (A-418 fails), and the audit widening reverted (A-418, A-419 fail). `pumasi/tools/gate.sh --can-hurt` printed **`GATE: PASS`**; its step 4 reported `tools/families.sh missing — breadth UNVERIFIED`, which is that script looking for the file in the product repository rather than a breadth failure, and the breadth was measured directly and is recorded in the row above. |
| **No frozen case was amended, and Q-030 is not reached** | **A-416 is untouched and still passes**, and that is a property of the design rather than luck: its settings-only body omits both `title` and `message`, so under the new comparison neither is pushed onto `changed` and its `['expiration date','reminder interval','reminders']` assertion is unmoved. `pumasi-sign/spec/0008` §S6 checks it assertion by assertion. The new cases went in a **new file**, `service/src/test/envelope-correction.test.ts`, run by the existing `run-service-suite.sh` — no second runner. **No reading of Q-030 is taken in either direction and no evidence is added to that entry.** |
| **Correction to this entry's own record, added after publication** | **The job is `0074`, not `0073`.** The packet is `pumasi-ops/jobs/proposed/0074-coder-sign-…`; `0073` is a `pumasi-tunnel` product-manager packet. The wrong number was read off the previous coder job's trailing digits. `pumasi-sign/spec/0008` is corrected at `c23c7e6` and carries the same note as its §S11. **The commit messages `pumasi-sign` `9659e69` and `pumasi` `df8f0d0` still say `0073` and are deliberately not rewritten** — a published record is a dated record, and the rows above are left as published for the same reason. Nothing else in any of the three is affected. |
| **Status** | open — window running. **Merged is not shipped, and this entry does not pretend otherwise: see Q-012**, which is open and explicitly outside Part 0's proceed-on-default rule, and **Q-018**, which records that shipping this product means `wrangler deploy` from `service/`. The live worker is built from `0e26917`; **Q-028** counted three repairs already waiting in that undeployed bundle and **this is the fourth**. No seat on this job deployed, proposed a deployer, or set a date. **As of publication the settings dialog on `sign.pumasi.ai` still deletes the sender's message to signers, silently, and still reports success.** |

### Q-038 · Pumasi Booking's public booking page shows the times it has — intent (24 h) and can-hurt release (7-day window)

| | |
|---|---|
| **What** | `pumasi-booking` `d7bd490`, coder job for `roadmap/BACKLOG.md` item **2** at `c000feb`, closing the build half of [issue #32](https://github.com/pumasi-ai/pumasi-booking/issues/32) — filed by a named user on the live product at 2026-09-01 00:36:56 UTC. The public booking page's client-side `render()` built every time button and appended none: `50f911f` deleted `times.appendChild(b)` from the `byDay[pickedDay].forEach` (`pages.ts:985` at `c000feb`) while inserting a `localStorage` block after it, in a commit about remembering a booker's name and email. **So a day the calendar rendered as available showed a heading naming that day and an empty times list** — silently, with nothing thrown, which is why the reporter's own diagnostics correctly said `0 error(s) captured`. Intent: `pumasi-booking/service/spec/0008/INTENT.md`. Spec: `pumasi-booking/service/spec/0008`. Release note: [`releases/2026-09-01-pumasi-booking-a-day-that-shows-times-shows-times.md`](releases/2026-09-01-pumasi-booking-a-day-that-shows-times-shows-times.md). |
| **Both windows, in one entry** | CHARTER §2.1's **24-hour intent window** and Part 4's **7-day can-hurt release window**, filed together in the shape **Q-035** and **Q-037** established. The longer window governs the date below. |
| **Risk class** | `can_hurt`, and reasoned rather than defaulted — though the default reaches the same place. `service/src/pages.ts` is **unmapped**: this repository's only `RISK_ZONES.yaml` files are `core/spec/` and `service/spec/0002/`, neither of which has an entry for it, and Part 4 says the classification *"defaults to can hurt someone when unmapped or unclear"*. On the merits it also qualifies directly: Part 4 names *"anything that books … on a real person's behalf"*, and this is the path a booker chooses a time on — `render()` writes the `start` and `end` a booking is made from. **The contrary reading is stated rather than hidden**: the change only restores something that was taken away and removes nothing, so a seat could argue `ordinary`. This seat took the window anyway, because reclassification is itself a can-hurt change (Part 4) and none is being made here. |
| **Window closes** | **2026-09-08** (7 days from 2026-09-01), per CHARTER §2.1. |
| **Default on silence** | The release stands as published. Stage is `beta` (`pumasi-booking/roadmap/STAGE.md`), so under CHARTER Part 0 the work has already proceeded and a steward veto **reverts** rather than prevents. |
| **What it takes away, named rather than buried** | **On the merits, nothing.** Nobody relied on an empty times list and nothing a booker could previously do is now impossible. **Two honest debits, neither about the repair itself.** The default test suite now launches a real browser, so a machine without `/usr/bin/google-chrome` fails the new cases at launch where it previously had no browser dependency in that workspace — that is INTENT assumption 1 and squarely inside this window. And the feedback report's diagnostics table renames **Page URL** to **Reported From**; anyone reading old issues will see both names for the same field. **And one thing the repair cannot do:** bookings that did not happen in the four weeks this was live are not recoverable, and nobody knows how many there were. The note says so rather than letting "fixed" imply otherwise. |
| **The premise this job was given, and why it is recorded rather than quietly dropped** | `BACKLOG.md` item 2 ranked reproduction as the packet's first step **because** *"the failure is client-side and there is no browser on this machine"*, and four evaluations passed on that basis. **It was wrong.** `/usr/bin/google-chrome` (Google Chrome 150.0.7871.186) was installed, and `puppeteer 25.9.0` was already a devDependency of `service/package.json` — and `service/test/browser-live.test.ts` had been driving a real browser in the default suite for days. Nothing had to be installed to reproduce this. The cost of the wrong premise was four evaluations that could have reproduced it and did not, and it is worth a lesson rather than a line: **an absent capability should be probed before it is ranked around.** |
| **Review** | **Two transcripts with verdicts, both APPROVE, and the spec and code reviewers share no family** — CHARTER §3 requirement 1's breadth rule, which binds because `pumasi/tools/families.sh` reported **5 of 6 available**, measured at this job. **Spec: gemini APPROVE** (`pumasi-booking/reviews/20260831-223232-spec-gemini.md`), before the build. **Code: glm APPROVE** (`…-224335-code-glm.md`). **grok was re-probed rather than inherited and is UNREACHABLE again** — a 45 s timeout this pass, not the HTTP 402 the prior tick recorded, which is a different failure of the same family and is noted in case the credential question is being tracked. **kimi and qwen were both recruited for the code review and both were killed, exit 137, on a 49970-byte bundle**; their transcripts are committed **empty** rather than counted as breadth, per `review.sh`'s own rule that a family which cannot answer is not an objection and is not a review. That is two of five available families unable to complete a review of ordinary size, and it is the second distinct recruit-path failure mode recorded this week. **glm's review found two real defects in the builder's own artefact and both were fixed rather than argued past**: the runner's fixture comments described day two as ending 21:30Z when the loop ends at 20:00Z, and `cases.json` claimed the fixture *mirrored* the live payload when it is modelled on its shape and differs in count (25 against the live 24). **`cases.json` was amended to v1.0.1 in the open; no case, clause, step or assertion changed** — only a provenance gloss. glm's third finding, that the tests pin the timezone but not the clock, is recorded in the spec so that a reddening after 2026-09-02 is not misdiagnosed as a regression. |
| **Verification** | By the builder. **The defect reproduces in a committed artefact and that artefact is red before and green after** — `service/test/booking-slots.test.ts`, which serves this tree's own `bookingPage()` output over loopback and drives it in headless Chrome in `America/Chicago`, the reporter's zone: **`# pass 2 · # fail 4` at `c000feb`** (B-001 `expected 12, actual 0`; B-002 `expected 13, actual 0`; no thrown error, matching the report) and **6/6 after**, in 6.8 s, **60/60 over 10 consecutive runs**. Both green-both-sides cases were checked to redden under the mutations `cases.json` names, rather than asserted to. Root `npm test` **three consecutive runs each side**: `core` **19/19**, `core` frozen acceptance **36/36**, `service` **331/331 → 338/338**, identical each time. `npm run typecheck` clean across both workspaces and the worker config. `pumasi/tools/gate.sh --can-hurt` printed **`GATE: PASS`**; its step 4 reported `tools/families.sh missing — breadth UNVERIFIED`, which is that script looking for the file under the product repository rather than a breadth failure, and the breadth was measured directly and is in the row above. **One observation handed to `BACKLOG.md` item 4 rather than fixed here:** a gate run interrupted mid-suite left `/tmp/pumasi-pg-bootstrap` non-empty and the next seven `bootstrap.test.ts` cases failed at `initdb` with *"directory exists but is not empty"* until it was removed. That is the *"service suite latches red"* entry reproducing on demand, with a named cause and a one-line remedy, and it is **not** caused by this change. |
| **No frozen case was amended, and Q-030 is not reached** | **No pre-existing case under any `service/spec/*/acceptance/` is touched.** The only acceptance file in the range is `service/spec/0008/acceptance/cases.json`, created by this work as its own spec's truth along the Part 3 authoring path and reviewed as such — which the code reviewer checked explicitly and cleared. Its post-review amendment to v1.0.1 changed prose only. **No reading of Q-030 is taken in either direction and no evidence is added to that entry.** |
| **Relation to Q-034** | **Q-034's named default is taken, by the first note that could take it.** That entry's default says the next `pumasi-booking` release note replaces the stale *"Which build this is"* sentence with the current fact rather than anyone editing a published note in place. This is that note, and it carries the section: `main` at `0.2.0` with the number visible on `/healthz`, `/version`, `/readyz`, the footer and the feedback diagnostics; and `booking.pumasi.ai/version` answering `{"version":"0.2.0","commit":"2453adc"}`, measured 2026-09-01 03:53:01 UTC. Rider (a) is honoured: **no existing release note was edited.** Nothing in Q-034's actual question — whether a rule-closing merge owes a published note, and who writes it — is answered, and no evidence is added there. |
| **Status** | open — window running. **Merged is not shipped, and this entry does not pretend otherwise: see Q-012**, which is open and explicitly outside Part 0's proceed-on-default rule. The live worker is built from **`2453adc`**, confirmed by `curl https://booking.pumasi.ai/version` at 2026-09-01 03:53:01 UTC and by `git merge-base --is-ancestor` answering that the repair is **not** an ancestor of it. No seat on this job deployed, proposed a deployer, or set a date. **As of publication, `booking.pumasi.ai/yunyoungmok/abc` still renders 1 and 2 as available, still heads the list "Tuesday, September 1", and still shows `#times.children.length` 0 — re-measured in a browser at 03:52:59 UTC, after the merge. Issue #32 is OPEN — but this seat had to REOPEN it, and that is the third instance of a pattern this fleet has now been bitten by three times.** It was found **closed as "completed" by `pumasiAI` at 2026-09-01 00:59:17 UTC** — 23 minutes after the user filed it, before this job began, and before any repair existed. The packet for this job instructed the coder not to close #32 without the live verification; nobody was instructed not to close it *before* the work, and somebody did. This seat reopened it at 03:56 UTC on the strength of the post-merge browser measurement above, and commented with the facts, including that the fix has not reached the reporter. **Recorded here rather than quietly reopened**: the two prior instances (`pumasi-sign` #7 and #8) were each a tracker moving ahead of a *deployment*, and this one moved ahead of the *fix*, which is the same failure one step earlier. |

### Q-039 · A user-filed issue was closed as "completed" 23 minutes after filing and before any repair existed — what may close a user-filed issue, and on what evidence?

| | |
|---|---|
| **What** | [`pumasi-booking` issue #32](https://github.com/pumasi-ai/pumasi-booking/issues/32) was filed by a named user on the live product at 2026-09-01 **00:36:56 UTC** and closed as **"completed"** by `pumasiAI` at **00:59:17 UTC** — **23 minutes later, before the repair existed and before the coder job that wrote it began.** Job `0077` reopened it at 03:56 UTC on the strength of a post-merge browser measurement and commented with the facts, including that the fix has not reached the reporter. The incident is recorded in **Q-038**'s Status row; this entry is the *policy* question that row names and does not answer. Raised by the project-manager tick 2026-09-01 05:10 UTC on job `0077`'s explicit escalation (`pod: pumasi-ops`, `role: steward-or-project-manager`, `priority: high`). |
| **Why this is not Q-038, and not a duplicate** | Q-038 records **what happened to #32**. This asks **what is allowed to happen to any of them.** Nothing in the fleet's rules was broken, which is the problem: coder packets carry a *"do not close without live verification"* clause, and whatever closed #32 **was not a coder packet and was not bound by it**. The guard exists exactly where the failure did not happen. |
| **Third instance, and the pattern moved one step earlier** | `pumasi-sign` **#7** and **#8** were each a **tracker moving ahead of a deployment** — a real fix existed and had not reached users. **#32 moved ahead of the *fix*.** Same failure, one step earlier in the chain, and the first one where a named user was told "completed" about a defect that no commit had yet addressed. |
| **Why it costs something real** | A tracker comment is one of the very few outputs of this fleet a **user actually reads**. A reporter told "completed" while the defect is live on the page they reported it from learns that filing is not worth doing again. #32's reporter met the defect on `booking.pumasi.ai` at 00:36 UTC and, as of **03:52:59 UTC**, still could. |
| **The question, in three parts** | **(a)** What evidence must exist before a user-filed issue may be closed — a merge, a deploy, or a measurement on the host the reporter used? **(b)** Which actors may close one? Today the `pumasiAI` account can, from outside any packet, with no clause binding it. **(c)** Does the *reporter* get told, and by whom, when an issue is reopened after being wrongly closed? |
| **Named default, if nobody decides** | **A user-filed issue may be closed only on a measurement taken against the surface the reporter used, recorded in the closing comment.** A merge is not a close and a green suite is not a close. Any actor may close under that rule; no actor may close without it. On reopen, the reopening seat comments with the current live state, as job `0077` did. This default is deliberately about **evidence** rather than about **who**, because the fleet's three instances were three different actors and a rule naming actors would have missed at least one of them. |
| **Scope of the default, stated so it is not over-read** | It governs **user-filed** issues only — issues opened by someone outside the fleet. Internal tracking issues are unaffected. It takes no position on labelling, triage, priority, or on whether an issue should have been filed. |
| **Risk class and window** | Not a release: this is a working rule, not a change to a product. **CHARTER §2.1 intent window, 24 h** — closes **2026-09-02**. Nothing is blocked on it; the default is already what job `0077` did by hand. |
| **What this entry does not do** | It does not reopen or close any issue, does not amend a packet template, and does not answer **Q-012** (who deploys). It also takes no position on the `pumasiAI` account's other permissions. Implementing the default — putting the clause somewhere every seat is bound by it rather than only coder packets — is a build, and belongs in a repository backlog once the rule is settled. |
| **Status** | open — window running. Raised by the project-manager role, which may file questions with named defaults and may not answer them. |

## Closed

### Q-034 · A merge closed a `PRODUCT-RULES.md` clause, two published release notes now say something untrue about it, and no note records the change — is one owed, and by whom?

| | |
|---|---|
| **What** | Raised by the `pumasi-booking` product-manager evaluation of 2026-09-01 (job `0061`, ops `DIGEST.md`), from a gap in the published record rather than from a defect. `pumasi-booking` `2453adc` merged with `GATE: PASS` and two approving cross-family reviews, and closed three of `PRODUCT-RULES.md` PR-1's four clauses: one version source of truth, a version that moves (`0.1.0 → 0.2.0`), and the number surfaced on `/healthz`, `/version`, `/readyz`, the D-105 footer and the feedback widget's diagnostics — on **both** entry points, from one generated constant. **No release note for it exists.** Checked at this evaluation: `releases/` holds notes for the reporting path, Zoom connect, the OAuth callback, sign-in, advisory CI and the alarm import, and none naming `2453adc`. The merging coder left it deliberately and said so in the open, on the correct ground that release notes live in this repository and are not `pumasi-booking`'s to edit. **The consequence is not the missing note but the two that are there.** `29f0853` and `0f574f6` each carry a section headed *"Which build this is"* stating outright that PR-1's version clause **cannot be met by this product**, and giving a commit instead. That sentence was true when each was published and is now false of `main`. Two public documents assert a limitation the product no longer has, and nothing in the flow corrects them. |
| **Why this is the steward's and not this seat's** | Two reasons, and neither is that the facts are doubtful. The product-manager role **may not publish** and may not edit release notes; that disqualification is in the role file. And the question is not really about one commit — it is whether CHARTER §2.1's `merge → ordinary: release` step leaves a *published* obligation behind when the merge closes a rule in `PRODUCT-RULES.md`. Answering that changes what every product's coder owes at the end of a job, which is a register change and not this seat's to make. |
| **Window closes** | *(steward to set — agents may not set a deadline.)* |
| **Default on silence** | **No separate release note is owed for `2453adc`, and the correction rides on the next one.** CHARTER §2.1 requires a *published* note with a veto window for a **can-hurt** release; an ordinary release ends at "release". `2453adc` was merged as ordinary, so on the charter as written nothing is missing. What *is* owed is accuracy: **the next `pumasi-booking` release note — whenever the next release happens — replaces the "Which build this is" sentence with the current fact**, namely that the version clause is met in `main` at `0.2.0` and is still unmet on the deployment because `booking.pumasi.ai` has not been deployed since 2026-08-30 (Q-012). **Two riders.** **(a)** No existing release note is edited. A published note is a dated record of what was true when it was published; correcting it in place is how a record stops being one. The correction goes in the next note, where readers already look. **(b)** This default costs nothing and buys nothing until someone releases, so if `pumasi-booking` goes another week without a release the stale sentence stands for another week — that is the price of the cheap answer and it is stated rather than discovered. |
| **Named alternative, if the default is wrong** | **A merge that closes a `PRODUCT-RULES.md` clause owes a published note, ordinary or not, written by the merging coder as the last step of that job** — the same shape as **Q-012**'s default for deployment, and subject to **Q-033**'s default for writing this repository: the seat pushes what it wrote and only what it wrote, and hands the full text up in its return block if `.lock_pumasi` is BUSY. The cost is explicit: every product's coder gains a second commons write per qualifying job, in a repository that four seats have now had to serialise against, and "closes a rule clause" needs a definition sharp enough that a coder can apply it without a judgement call. It is the stronger standard for the public record. It is not free. |
| **What retires this entry** | Either a `pumasi-booking` release note published with the corrected "Which build this is" sentence (the default), or CHARTER §2.1 amended to attach a note obligation to rule-closing merges (the alternative) — and in both cases this entry closed recording which. |
| **Scope** | Asks only whether a *published note* is owed and who writes it. It does **not** ask whether `2453adc` was correctly classified **ordinary** rather than **can-hurt** — a live question, since the commit adds an attacker-controlled field to `feedback.ts`, the path that carries a user's own material into a public GitHub issue, and added `reportedVersion()` to validate exactly that. Classification is the reviewer's and the steward's, not this role's; it is named here rather than left unsaid, and it is deliberately not folded into this question, which would otherwise be two. It touches no product code and no other product, though the answer would presumably bind them. |
| **Status** | open. Nothing is blocked on it: the product is unaffected either way, and the only cost of waiting is that two published notes keep saying a true-when-written thing that has stopped being true. Recorded now rather than after a third note repeats the sentence. |

### Q-003 · Google Cloud OAuth application — **CLOSED 2026-08-27, done**
Steward's part: created `admin@pumasi.ai`, accepted the Cloud ToS and the API
Services User Data Policy (both terms acceptances, `HUMAN.md`), signed in once
to the operator browser (`tools/operator/`), and delegated CLI access. Agents'
part, under that delegation: project `pumasi-commons`, Calendar API enabled,
consent screen (external, testing mode), the single **non-sensitive**
`calendar.freebusy` scope (better than the *sensitive* estimated here earlier
— lightest review tier), `admin@pumasi.ai` as test user, and web client
`pumasi-service` with credentials in gitignored `apps/service/.env`. Record:
[`spec/0003-calendar-sync/GOOGLE-SETUP.md`](https://github.com/pumasi-ai/pumasi-booking/blob/main/service/spec/0003/GOOGLE-SETUP.md).
**Verification submission** is deliberately later — it needs a deployed app
and the Q-002 privacy policy URL.

**Proposal for the steward** (agents may not edit `HUMAN.md`): strike "OAuth
consent screen / test accounts" from the human registry — this session
demonstrated an agent performs them via the operator browser. Account
creation, terms acceptance, and verification *submission* stay human.
